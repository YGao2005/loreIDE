# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-24)

**Core value:** A developer or PM can locate any piece of the product by intent, edit its contract, and have the agent produce the corresponding code change — without touching the file tree.
**Current focus:** Phase 15 Plan 01 complete (2026-04-26). TRUST-02 + TRUST-04 closed — Migration v8 lands chained-version self-FK (`prev_version_uuid`) + `invalidated_reason` on substrate_nodes, `substrate_edits` audit table with CHECK(kind IN ('refine','delete','restore')), replacement `substrate_nodes_au` trigger (FTS tombstone fix: WHERE new.invalid_at IS NULL guard stops tombstoned rows leaking back into FTS), and `receipts.substrate_rules_json` nullable column. Substrate hit UUID call chain threaded: `ComposeResult::substrate_rules_json()` helper in composer.rs → `substrate_rules_json: Option<String>` param on `delegate_execute` → `run_agent` → `parse_and_persist` → `?16` bind in INSERT INTO receipts. Integration smoke test `migration_v8_chain_smoke.rs` (193 lines) asserts: (5a) FTS MATCH old text returns ZERO rows post-tombstone, (5b) new head appears in FTS, (5c) WHERE invalid_at IS NULL returns only new head, (5d) substrate_edits committed atomically, Phase 12 predicate parity confirmed. receipts_substrate_rules_json.rs 2-test suite proves populated+NULL round-trips. Full Rust suite 155 pass + 1 pre-existing unrelated failure (commands::demo_orchestration::tests::fixture_dir_falls_back_to_crate_relative_seeds — pre-dates phase 15, out-of-scope). Commits 575d4ca (Task 1), a0824b5 (Task 2), 3d4a3b2 (Task 3). Phase 15 progress: 1/6 complete; 15-02..15-06 outstanding.

**Previous focus:** Phase 14 Plan 05 complete (2026-04-26). BOOTSTRAP-02 + BOOTSTRAP-03 + BOOTSTRAP-04 [proposed] all closed — Stage 5a (`synthesize-flows.mjs`) + Stage 5b (`emit.mjs` + `install-babel-plugin.mjs` + `validate.mjs`) shipped end-to-end. Stage 5a walks each L3 trigger's import graph + AST call-sites in source order, recurses one level into each member's imports, dedups by UUID preserving first-call ordering, and emits flow contracts (format_version=5, kind=flow, level=L2) with deterministic UUIDv5 from `(repoName, 'flow-<slug>', 'L2:flow')`. Slug includes HTTP-method prefix for API triggers so multi-verb route.ts files (GET/POST/PUT/DELETE on `api/notes/[id]/route.ts`) emit distinct flow contracts. Single `claude -p --json-schema flow.json` LLM verification per flow with 60s SIGKILL watchdog + cross-check (returned members must be subset of proposed candidates + members[0]==trigger) + static-chain fallback on any LLM error. Stage 5b composes .md sidecars from .staging/ JSON intermediates via `writeFrontmatter` (Phase 9 round-trip parity), runs the validator gate BEFORE atomic rename, archives staging .json artifacts to `.contracts/.bootstrap-staging-archive/`, and atomically promotes `.staging/*.md` → `.contracts/*.md` ONLY on validator-pass. `install-babel-plugin.mjs` byte-copies the Babel scaffold + idempotently patches `next.config.{ts,js,mjs}` between BOOTSTRAP-INSERT-START / -END markers (re-running REPLACES the block, never duplicates). `validate.mjs` 3-layer dispatcher: (1) `contract-ide validate-repo <path>` subprocess preferred (Rust source of truth — falls through gracefully on "unrecognized subcommand" since it's not yet shipped); (2) JS-side fallback with stderr "Skill is using degraded JS-side validators" warning per RESEARCH Open Question 7; (3) **schema allOf re-assertion** per Plan 14-04 SUMMARY mitigation — re-applies the top-level allOf guards from `schemas/frontmatter.json` that `claude-cli-bridge.mjs::stripUnsupportedTopLevel` strips at the API boundary (kind=flow ⇒ format_version=5 + level=L2 + members[]≥2; kind≠flow ⇒ format_version=3 + members absent). JSX-01 + BACKEND-FM-01 enforced via the JS fallback. **Watchdog timeout in `callClaude()`** (default 120s SIGKILL, override via BOOTSTRAP_CLAUDE_TIMEOUT_MS env or per-call `timeoutMs`) — Plan 14-04 SUMMARY's recommended mitigation lands as Rule 2 hardening; converts the 25/40 silent-hang failure mode (path-mode bug) into surfaceable errors. Three new test suites pass clean: synthesize-flows (6 tests — invocation-order extraction NOT import-order + format_version=5 frontmatter shape + flow UUID determinism + shared-service dedup + slug derivation including HTTP-method prefix) + emit (4 tests — compose+promote happy path + plugin install + idempotent re-install no duplicate BOOTSTRAP-INSERT block + validator-failure abort) + validate (5 tests — BACKEND-FM-01 missing-Outputs + JSX-01 empty code_ranges + both schema allOf branches + all-good staging passes). Full skill suite 42/42 green. **Smoke against bootstrap-demo-target (Marginalia, BOOTSTRAP_TEST_MODE=1):** 16 flow contracts synthesized in `.staging/`, all 16 with unique slugs + UUIDs (was 13 pre-fix; surfaced + corrected the slug-collision Rule 1 bug). Final staging composition: 18 UI + 12 API + 6 data + 2 lib + 2 external + 16 flow = 56 frontmatter files. Two Rule deviations auto-fixed in-flight: (1) [Rule 2 missing-critical] watchdog timeout on callClaude — Plan 14-04 SUMMARY recommended mitigation; (2) [Rule 1 bug] flow slug collisions on multi-verb route.ts — discovered when 16 flows synthesized but only 13 unique UUIDs landed (3 silently overwritten); fix prepends lowercased HTTP method to slug. Open Question 7 STILL DEFERRED — JS fallback ships as v1 default; user-friction recorded in Plan 14-06 will inform Yang's ratification. Commits ae2dc7e (Task 1 synthesize-flows + watchdog), 61cd49a (Task 2 emit + install-babel-plugin + validate), 0107f1a (in-flight slug-collision fix). Wave 4 progress: 14-05 done; 14-06 + 14-07 + 14-08 outstanding.

**Previous focus:** Phase 13 Plan 10b complete (2026-04-25). DEMO-04 UI orchestration shipped — `commands/sync.rs` extended via `load_blast_radius_fixture` to read `blast-radius.json` from disk (replaces 13-09 hardcoded placeholders; preserves `SyncTriggerResult` shape; honors `CONTRACT_IDE_DEMO_FIXTURE_DIR` env override; graceful empty-array fallback + stderr log on read/parse failure). New `commands/demo_orchestration.rs` ships `load_beat3_verifier_fixture` (returns parsed `beat3-verifier.json` to TS) + `emit_beat4_harvest` (extracts `harvested_nodes` from `beat4-harvest.json`, emits `substrate:nodes-added` Tauri event for HarvestPanel's primary subscription path including `attached_to_uuid` per N9 green-halo wiring). TS wrappers `loadAndApplyBeat3Verifier` + `triggerBeat4Harvest` added to `src/lib/demoOrchestration.ts` alongside 13-09's `window.__demo`. New `DemoOrchestrationPanel` (bottom-left z-50 amber-bordered with `bg-background/95` backdrop-blur; three single-click triggers "Beat 3: Sync" / "Beat 3: Verifier" / "Beat 4: Harvest"; dismissible × button + error surface) gated on `import.meta.env.DEV`. Mounted in AppShell additively alongside 13-08 PRReviewPanel + 13-09 SyncButton/VerifierPanel/HarvestPanel; non-colliding z-indexes (top-right z-30 / top-right top-16 z-30 / bottom-right z-40 / bottom-left z-50). Wave 5 serialization: `load_beat3_verifier_fixture` + `emit_beat4_harvest` appended to `lib.rs` `generate_handler!` AFTER 13-09's `trigger_sync_animation`; modified existing 13-09 files (sync.rs, demoOrchestration.ts) without locking placeholders; created new demo_orchestration.rs + DemoOrchestrationPanel.tsx; AppShell mount additive. **Task 3 full visual + cmdp-precision verification DEFERRED to plan 13-11 by user direction** — 13-11 rehearsal is the natural test surface where real Phase 9 uuid substitution into 13-10a's `PLACEHOLDER-*` fixtures happens, where the 4-beat demo runs end-to-end 3 times with rehearsal-log entries, and where the cmdp-precision ≥8/10 gate naturally executes against the seeded substrate. Commits 4d648fd, f6f6f8d. Wave 5 COMPLETE.

**Previous focus:** Phase 13 Plan 09 complete (2026-04-25): SUB-09 mocked-Sync clause + SUB-08 verifier/harvest clauses shipped — SyncButton (top-right z-30) + animateSyncBlastRadius (50ms stagger via citation halo + substrate state setter) + trigger_sync_animation Rust IPC (placeholder uuids today; 13-10b extends to read blast-radius.json with response shape locked); VerifierPanel (top-right top-16 z-30) renders 6 ✓ honors + ImplicitDecisionsGroup (3 hand-crafted rows: 24h email-link / audit_log / async cleanup) + 1 ⚠ orange flag halo on parent SCREEN CARD (8s halo via useCitationStore.highlight); HarvestPanel (bottom-right z-40) dual-channel subscription — substrate:nodes-added Tauri event primary + 2s setInterval poll fallback against list_recent_substrate_additions IPC (silent try/catch swallow per 13-RESEARCH.md Open Question 3) — renders [⌃ promoted from implicit] amber badge for promoted_from_implicit nodes. window.__demo.loadBeat3VerifierResults(parentSurfaceUuid) defined in src/lib/demoOrchestration.ts — 13-10b's loadAndApplyBeat3Verifier calls this after reading beat3-verifier.json. DEFAULT_BLAST_STAGGER_MS + BLAST_PULSE_DURATION_MS exported from syncBlastRadius.ts for 13-11 rehearsal pacing tuning. Wave 4 serialization compliance: trigger_sync_animation appended to lib.rs generate_handler! AFTER 13-08's analyze_pr_diff. Visual verification DEFERRED to plan 13-10b by user direction — natural test surface via DemoOrchestrationPanel + fixture-loaded uuids (chain-pulse fidelity, halo-on-screen-card semantics, promoted-from-implicit badge styling, z-index coexistence with PRReviewPanel from 13-08 all naturally exercised when 13-10a's blast-radius.json + beat3-verifier.json + beat4-harvest.json fixtures load). Phase 12 dependency: orange-flag verifier output is fixture-staged via beat3-verifier.json (no Phase 12 supersession engine cascade required at demo time). Phase 10 dependency: substrate:nodes-added event channel never fires without Phase 10 watcher; 13-10b's emit_beat4_harvest IPC manually emits for Beat 4 staging. Commits 8c4165d (Task 1 SyncButton + animateSyncBlastRadius + trigger_sync_animation IPC), 83cf126 (Task 2 VerifierPanel + ImplicitDecisionsGroup + HarvestPanel + verifier store + window.__demo + loadBeat3VerifierResults). Wave 4 COMPLETE (3 plans landed: 13-07 SourceArchaeologyModal, 13-08 PRReviewPanel, 13-09 Sync/Verifier/Harvest). Wave 5 next: 13-10a (data layer — SQL seed + JSON fixtures + reset script) and 13-10b (UI orchestration — IPCs + DemoOrchestrationPanel) — zero file overlap, full parallel.

**Previous focus:** Phase 13 Plan 06 complete (2026-04-25): CHAIN-01 + CHAIN-02 + CARD-01 + CHIP-01 + CHIP-02 — vertical participant chain layout shipped end-to-end. `assembleFlowChain(memberUuids, allNodes, focusedFlowUuid, thisFlowUuid)` pure function with 15 vitest cases (deterministic positions, screenCard-vs-serviceCard kind dispatch, missing-uuid skip, deriveCallShape matched/mismatched/missing). `CallShapeEdge` smooth-step + EdgeLabelRenderer with matched-vs-muted styling. `FlowChainLayout` reads useSidebarStore.selectedFlowUuid + useGraphStore.nodes, mounts ReactFlow with `nodesDraggable={false}` deterministic chain. GraphCanvasInner branch swaps default rendering for FlowChainLayout when selectedFlowUuid set. `captureIframeScreenshot` three-tier never-throws fallback (Rust IPC → SVG-foreignObject canvas → null); `useScreenshotStore` Zustand cache; ScreenCard.isFocused branch (live iframe vs cached screenshot vs placeholder). Three Rule 1 deviations caught + fixed during user verification: (1) AtomChipOverlay infinite render loop (commit 24f1b40 — memoized Zustand selector to break useSyncExternalStore retry loop; sole-offender audit confirmed), (2) cross-origin postMessage chip rects (commit 1e0d9b6 IDE-side + 6b0ceb1 demo-side — requestId nonce + 500ms timeout + leak-safe listener cleanup; demo responder script `public/contract-chip-responder.js` loaded via `<Script strategy="afterInteractive">`; replaces 13-05's same-origin shortcut blocked across :1420/:3000), (3) cross-origin canvas screenshot (combined in 1e0d9b6 — three-tier never-throws fallback). Wave 2 → Wave 3 serialization compliance: `capture_route_screenshot` appended to lib.rs handler list AFTER 13-03's `find_substrate_by_intent`; nodeTypes.ts UNTOUCHED. Phase 9 contract gaps surfaced for plan 13-11 rehearsal: BABEL-01 (chips render only if demo build emits `data-contract-uuid`; absent → empty rects, no crash), BACKEND-FM-01 (call-shape labels show `?` muted when sections missing). Phase 14 handoff: ServiceCard renders raw uuid (e.g. e7000000-0000-4000-8000-000000000000) when contract `name` frontmatter field absent or equals uuid — correct defensive fallback; Phase 14 codebase-to-contracts bootstrap will populate human-readable names. Demo repo (contract-ide-demo) commit 6b0ceb1 currently on detached HEAD (preexisting condition, NOT caused by this session — user to fast-forward master if desired). vitest 82 pass + 1 skipped baseline preserved across all fix commits; tsc clean throughout. User visually verified and approved. Commits cab5679, c4a1388, 24f1b40, 1e0d9b6. Wave 3 COMPLETE; Wave 4 next: 13-07 SourceArchaeologyModal (autonomous), 13-08 PRReviewPanel (autonomous), 13-09 SyncButton+Verifier+Harvest (checkpoint).

**Previous focus:** Phase 13 Plan 05 complete (2026-04-25): CARD-01 + CHIP-01 ScreenCard + atom-chip overlay shipped (visual end-to-end verification deferred to plan 13-06 by user direction — natural test surface via FlowChainLayout). UI-mode L3 trigger card with iframe at the screen contract's `route` (Phase 4 PreviewTab probe + retry pattern reused; Rust reqwest probeRoute IPC, NOT frontend fetch — CORS-blocked because tauri://localhost ≠ http://localhost:3000). AtomChipOverlay layered in PARENT layer (NOT inside iframe — sidesteps cross-origin script injection AND pan/zoom interference); pointer-events-none container with pointer-events-auto chips. requestChipRects helper prefers same-origin iframe.contentDocument.querySelectorAll('[data-contract-uuid]') (works under Phase 4 frame-src CSP for http://localhost:* — same-origin Tauri WebView), graceful postMessage fallback with 250ms timeout for cross-origin scenarios. AtomChip uses canonical selectNode + setFocusedAtomUuid setters per checker N7 (NEVER setSelectedNode, NEVER raw setState); data-atom-uuid + data-state DOM attributes mirror plan 13-04 ServiceCardChips for plan 13-07 citation halo selector compatibility. Inspect (default, iframe pointer-events: none → chips intercept clicks) / Interact mode toggle (iframe pointer-events: auto + chips fade to 0.4 opacity but still clickable). screenCardStyles CVA: separate scope from cardKindStyles but identical state hex values (orange-600 + 8px glow halo for intent_drifted matches ContractNode + ServiceCard exactly per 13-RESEARCH.md Pitfall 6 bitrate compression). nodeTypes.ts: appended `screenCard: ScreenCard` AFTER plan 13-04's `serviceCard` entry per Wave 2 serialization_hint; both registrations preserved. lib.rs UNTOUCHED (no Rust IPC commands added — probeRoute already shipped in Phase 4). 5 vitest cases for iframeChipPositioning + 8 vitest cases for ScreenCard. tsc clean; vite production build succeeds. Visual verification deferred to plan 13-06's checkpoint where ScreenCard naturally mounts inside an L2 vertical flow chain (ScreenCard at top + ServiceCards below) — that becomes the integration test surface. Empty-element fallback (atom contract exists but JSX element doesn't render `data-contract-uuid` yet) renders nothing today — section-bottom placement deferred to plan 13-10b once seeded fixture exists. Phase 9 BABEL-01 dependency surfaces as Phase 9 contract gap to plan 13-11 rehearsal if iframe content lacks `data-contract-uuid` annotations. Commits d66cbc9, 4089fda. Wave 2 COMPLETE (4 plans landed: 13-04 ServiceCard + 13-02 Sidebar + 13-03 Cmd+P + 13-05 ScreenCard). Wave 3 next: plan 13-06 FlowChainLayout (assembles vertical chain with ScreenCard at top + ServiceCards below).

Phase 13 Plan 02 complete (2026-04-25): SIDEBAR-01 shipped — repository area-tree sidebar replacing Phase 3 Journey/System/Ownership lens switcher. New `get_sidebar_tree` Tauri IPC walks `.contracts/` inside spawn_blocking (because nodes.file_path is intentionally NULL post-Phase 2 DATA-01 pivot — sidecar path is the canonical source for area derivation), cross-references the nodes table for canonical name + kind. `useSidebarStore` Zustand store with tree + expandedAreas Set + selectedFlowUuid slices. `SidebarAreaItem` renders per-area rows with three optional badges (drift / rollup-stale / intent-drifted counts) computed via reducer-style Zustand selectors against drift/rollup/substrate stores; badge hex values mirror plan 13-01 CVA variants exactly (red-500 / amber-500 / orange-600). Flow click writes to BOTH sidebar.setSelectedFlow AND graph.pushParent so canvas drill-in works today before plan 13-06's FlowChain. `SidebarTree` subscribes to graph nodes.length for refresh + listens for `sidebar:refresh` CustomEvent (`SIDEBAR_REFRESH_EVENT` exported) so plan 13-10a reset script can force re-fetch. Lens switcher DELETED (not feature-flagged); currentLens slice retained on graphStore for Phase 14 cleanup. ROOT_AREA = '_root' sentinel exported from ipc/sidebar.ts (italic "Root" rendering). 5 Rust unit tests pass; full lib suite 109/109 green; cargo clippy clean; tsc clean; vitest 51/51; vite production build succeeds. Commits 0b863be, 283978a.

Phase 13 Plan 04 complete (2026-04-25): ServiceCard + EndpointCard + ServiceCardChips + serviceCard nodeTypes registration shipped (CARD-02 + CARD-03 + CHIP-02). Stripe-API-docs-style backend participant card rendering all 7 backend kinds (api / lib / data / external / job / cron / event) with method-colored badges (POST green / GET blue / PUT orange / PATCH yellow / DELETE red), monospace path/signature headers, syntax-highlighted JSON schemas, multi-status outputs (### 200 OK / ### 401 Unauthorized), bulleted side-effects list, and L4 atom chips on the side. Body content is 100% driven by parseBackendSections (## Inputs / ## Outputs / ## Side effects from Phase 9 BACKEND-FM-01) with defensive empty-schema fallback ("No schema declared (BACKEND-FM-01 not populated)"). Three new CVA factories (methodBadgeStyles / cardKindStyles / chipStyles) with state keys matching plan 13-01 NodeVisualState union exactly so resolveNodeState output is valid in either CVA. ServiceCardChips uses canonical selectNode setter (not setSelectedNode per checker N7). 12 vitest cases (5 parser + 7 ServiceCard render-decision/registration); full project suite 51/51 pass; vite production build succeeds; tsc clean. Commits 02d6dd3, 3698fac.

Phase 13 Plan 01 complete (2026-04-25, prior session). Foundational substrate-state plumbing landed (SUB-08 + CHIP-03): useSubstrateStore extended additively with nodeStates Map<uuid, SubstrateNodeState> alongside Phase 11's footer counter slice; useGraphStore extended with focusedAtomUuid slice; two new Tauri commands (get_substrate_states_for_canvas, get_substrate_node_detail) reading the actual substrate_nodes schema with defensive table + intent_drift_state column existence checks; CVA intent_drifted (orange-600 + 8px glow + pulse) and superseded (orange-400 + opacity 0.75, no pulse) variants; resolveNodeState compositor with explicit precedence (drifted > intent_drifted > rollup_stale > superseded > rollup_untracked > healthy) exported as load-bearing import for Wave 2 plans; GraphCanvasInner.buildFlowNodes rewired through resolveNodeState; AppShell hydrates substrate state on mount. 6 unit tests + full lib suite (104/104) pass. Commits 2ca959d, 7e8c471.

Phase 12 VERIFIED ✓ (2026-04-25). All 4 ROADMAP success criteria PASSED via gsd-verifier (`12-VERIFICATION.md`): SC1 fact-level supersession (engine + harness 1.00/1.00); SC2 intent-level cascade (engine + harness 10/10, d8 = NEEDS_HUMAN_REVIEW @ 0.50); SC3 no v1 regression (162+ cargo tests); SC4 adversarial recall ≥80% / precision ≥85% (perfect score). Yang approved Beat 3 dual-path GREEN live in-app on 2026-04-25 (engine path + backstop path produce equivalent persistence; runtime + feature-flag gates fire correctly; idempotency invariant holds). Harness numbers locked as regression baseline. Walker.rs `rollup_inputs` → `rollup_inputs_json` bug discovered + fixed inline during UAT (commit `358a252`). Phase 12 ships fact-level + intent-level supersession (the moat) — the Beat 3 orange-flag claim is now live-engine-backed.

## Current Position

Phase: 15 of 15 (Substrate Trust Surface) — IN PROGRESS
Plan: 1 of 6 complete in current phase (15-01 foundation shipped)
Status: Phase 15 Plan 01 COMPLETE (2026-04-26). Migration v8 + substrate_edits + FTS tombstone fix + receipts.substrate_rules_json + full call-chain threading + 2 integration test suites. See Current focus above.

**Previous phase:** Phase 14 of 14 (Codebase-to-Contracts Bootstrap Skill + Demo Application) — Plan 6 of 7 complete (14-05 done). Status: Phase 14 Plan 05 COMPLETE (2026-04-26). BOOTSTRAP-02 + BOOTSTRAP-03 + BOOTSTRAP-04 [proposed] all closed — Stage 5a synthesize-flows.mjs + Stage 5b emit.mjs + install-babel-plugin.mjs + validate.mjs shipped. See previous focus above for the full debrief; commits ae2dc7e, 61cd49a, 0107f1a. Wave 4 progress: 14-05 done; 14-06 + 14-07 + 14-08 outstanding.

**Previous status:** Phase 14 Plan 04 COMPLETE (2026-04-25). BOOTSTRAP-02 [proposed] further satisfied — Stage 3 (`derive-body.mjs`) + Stage 4 (`align-jsx.mjs`) of the bootstrap pipeline shipped + the prose-quality gate cleared on iteration 1. Stage 3 fills `## Intent` / `## Role` per kind branch (UI L3: Intent+Role; UI L4: Intent+Role+Examples; backend: Intent+Role+Inputs+Outputs+Side effects per BACKEND-FM-01) via `claude -p --output-format json --json-schema schemas/contract-body.json` per node, grouped by L3 surface for context coherence, parallelized at concurrency 5. Hash-skip via `_progress.json.stage_3_completed_for` Set membership. CLI flag `--sample=N` processes only the first N nodes (auto-spans UI L3 / UI L4 / backend) for cheap prompt iteration ($0.30 + <30s vs $1.50 + 3min for full run). `prompts/derive-body.txt` (151 lines) embeds **4 verbatim few-shot exemplars** extracted from contract-ide-demo/.contracts/ (api-account-delete.md, lib-begin-account-deletion.md, ui-l3-account-settings.md, ui-l4-danger-zone.md) — NOT just an instruction to "match Phase 9 prose density." Stage 4 walks each L4 UI atom's parent .tsx with @babel/parser using EXACTLY the Phase 9 webpack-loader config (`sourceType: 'module', plugins: ['jsx', 'typescript']`), picks the outermost JSX element fully contained in the heuristic candidate range, sets `code_ranges`. Multi-match resolves via single bare `claude -p` LLM tiebreak call (no schema, integer back). Zero-match writes `_stage4_failures.json` + `process.exit(1)` — refuse-to-emit, never fabricate. Backend kinds (API/lib/data/external/job/cron/event) skip alignment per JSX-01 exemption. Two unit-test suites pass: derive-body (4 tests — exemplar interpolation makes "## Intent" appear ≥4 times + "## Side effects" ≥1 time in the assembled prompt; kind-branch shape; hash-skip; --sample=N) + align-jsx (4 tests — outermost-detection, zero-match abort, multi-match tiebreak, helper invariants). **Prose-quality gate (Task 3) APPROVED on iteration 1** — Yang reviewed 3 sample derivations (data L2 Session model `5280d411-...`; UI L4 LoginPage atom `7abba625-...`; UI L3 /login page `99af0629-...`), all 3 passed all 3 axes (decision specificity, cross-reference density, prose density) without prompt edits; level differentiation (L3 product-level vs L4 implementation-level) correct on first try; verbatim approval note: "approved — all 3 pass all 3 axes (decision specificity, cross-reference density, prose density) on first iteration. Level differentiation correct: L3 stays product-level, L4 goes implementation-level. Approved without prompt edits." Plan 14-06 inherits this prompt unchanged. **One Rule-3 in-flight deviation**: discovered during checkpoint preparation that `claude-cli-bridge.mjs::callClaude()` was passing `--json-schema <PATH>` and the schema's top-level `allOf` (the if/then/else flow-contract guard from `flow.json`) caused the structured-output API to reject calls with `tools.N.custom.input_schema: input_schema does not support oneOf, allOf, or anyOf at the top level`; path-mode itself silently hung instead of erroring. Patched in-flight to (a) read schema file, (b) strip top-level `allOf`/`oneOf`/`anyOf` via `stripUnsupportedTopLevel()`, (c) JSON.stringify + pass inline; also added `--dangerously-skip-permissions` so the headless subprocess doesn't stall on a permissions prompt. Full schema constraint re-asserted post-hoc by Plan 14-05's `validate.mjs` (defense in depth, not bypass). This bug was retroactively masking Plan 14-03's Stage 2: only 15/40 frontmatter files had been emitted (the remaining 25 calls had silently hung); after the bridge fix, Stage 2 was re-run end-to-end and **all 40/40 frontmatter files now exist in `.contracts/.staging/`**. Mitigation note for Plan 14-05: add a watchdog timeout in `callClaude()` to convert silent hangs into errors. Commits a739a35 (Task 1 derive-body + 4 exemplars + system prompt + tests), 2c4fd37 (Task 2 align-jsx + outermost matcher + tiebreak + tests + 2 fixtures), 13f96ac (in-flight bridge fix attributed to 14-04 — the plan whose checkpoint surfaced it). Wave 3 progress: 14-04 done; 14-05 + 14-06 outstanding.

**Previous status:** Phase 14 Plan 03 COMPLETE (2026-04-25). BOOTSTRAP-02 [proposed] partially satisfied — Stage 1 (`discover.mjs`) + Stage 2 (`derive-frontmatter.mjs`) of the bootstrap pipeline shipped. discover.mjs walks Next.js + Prisma sources via path heuristics (UI L3 from page.tsx, API L3 per HTTP method on route.ts, data L2 per `model X { }` block, lib/external/data dispatch on lib/**/*.ts by import shape + size threshold), with single-call `claude -p --json-schema schemas/classify.json` fallback for ambiguous files. derive-frontmatter.mjs emits `.staging/<uuid>.frontmatter.json` per node via `claude -p --output-format json --json-schema schemas/frontmatter.json` at concurrency 5, temperature 0, model pinned via BOOTSTRAP_CLAUDE_MODEL. Hash-skip via `_source_sha256` staging marker — re-run on unchanged source reports "0 derived, N skipped". preflight.mjs lands as Stage 0: verifies Next.js + TS shape, hard-ceilings >500 .tsx, detects prior staging (resume/restart/abort prompt), surfaces soft cost prompt above $5. Two unit-test suites pass: 3 discover tests + 3 derive-frontmatter tests — all 6 pass via `node --test`. Smoke against bootstrap-demo-target: 40 candidate nodes (UI:L3 × 9, UI:L4 × 9, API:L3 × 12, data:L2 × 4, data:L3 × 2, external:L3 × 2, lib:L2 × 2); all 6 canonical patterns covered; re-run produces byte-identical nodes.json. Three Rule-classified deviations auto-fixed inline: (1) [Rule 3 - Blocking] demo target `src/` layout vs plan-spec `app/**` globs → added `detectSourceRoot()`; (2) [Rule 2 - Missing Critical] no deterministic sort on nodes.json → added explicit `(file, level, uuid)` sort; (3) [Rule 3 - Blocking] plan-spec `import.meta.url.replace('file://', ...)` invalid → used `fileURLToPath` + SCRIPT_DIR. Pre-existing `schema-rust-parity.test.mjs` failure (greps wrong struct name) logged to deferred-items.md per scope-boundary. Stage 2 LLM smoke + cost-estimate accuracy DEFERRED to Plan 14-06's end-to-end demo. Bootstrap-defaults pre-fill every required schema field so degenerate LLM output still yields valid sidecar; uuid/kind/level/route NEVER overridden by LLM (Stage 1 authoritative). Output is staging-only; atomic emit happens in Plan 14-05's Stage 5b. Commits 2ca76da (Task 1), 387d760 (Task 2). Wave 3 next: plans 14-04 + 14-05 + 14-06.

**Previous status:** Phase 14 Plan 02 COMPLETE (2026-04-25). BOOTSTRAP-05 satisfied — `bootstrap-demo-target/` Marginalia micro-SaaS scaffolded at `/Users/yang/lahacks/bootstrap-demo-target/` as runnable Next.js 16.2.4 + Prisma 7 + argon2 sessions + Stripe 22.1 + Resend 6.12 + shadcn/ui (8 components) app. 51 source files (15 .ts + 36 .tsx), 4 Prisma models (User/Session/Note/Subscription), 9 API routes, 8 pages, founder-voice README, real landing page (hero + features + pricing), visibly-stubbed Danger Zone (disabled button + `TODO: wire up delete-account flow — drafted in #issues/12`), seed creates yang@marginalia.app with 5 realistic notes + random password to seed-credentials.txt. Yang ratified option-a (Yang-built micro-SaaS) at Task 1 checkpoint with product name **Marginalia** + tagline "an annotation-first notes app for builders who think in the margins". 16 organic-feeling commits backdated over 21 days via GIT_AUTHOR_DATE/COMMITTER_DATE (5a65e39 → 253ecb0; 16 distinct dates; conventional-commits-ish lowercase scrappy messages incl. `wip: account settings + danger zone (TODO delete-account flow)` as the most recent feature commit). NO `.contracts/` directory (cardinal: bootstrap input must lack contracts; Plans 14-03/04/05 produce it). NO `contract-uuid-plugin` in next.config.ts (Plan 14-05 wires it). Stack matches contract-ide-demo 1:1 on load-bearing pieces (Prisma 7 better-sqlite3 adapter pattern, prisma.config.ts datasource convention) so Phase 9 plumbing reuses. Three Rule-classified deviations auto-fixed inline: (1) Lucia v3 deprecation + Prisma 7 peer-dep block → rolled argon2id sessions in-house in `src/lib/auth.ts` (~80 lines, same surface — login/signup/logout + getSession + requireUser); the deprecation is referenced in commit message `feat: argon2 sessions ... (rolled in-house, lucia v3 is eol)` which actually plays well with believability; (2) Prisma 7 moved datasource url out of schema.prisma → mirrored contract-ide-demo's prisma.config.ts pattern + PrismaBetterSqlite3 adapter; (3) `useSearchParams()` not wrapped in Suspense boundary broke `pnpm next build` → split /checkout page into CheckoutInner + outer Suspense. Believability self-check passed all 11 gates: package.name=marginalia (not bootstrap-demo-target), no demo@example/password123/lorem ipsum/welcome-to-my-app strings, README never references "demo target"/"bootstrap input"/"skill input", Danger Zone TODO present, no .contracts/ dir, no contract-uuid-plugin import, 51 files in 35-55 target, 8 ui components ≥7, 16 commits in 10-15±1 target, 16 distinct commit dates, build clean, tsc clean. Five "interesting" patterns deliberately planted for the skill to surface in Plans 14-03/04/05: lib/stripe.ts (external classifier test), lib/notes.ts (lib classifier test with non-trivial searchNotes tokenized OR query + FTS5 migration comment), account/settings Danger Zone (sectioned-L4 atom test for stubbed regions; api/account intentionally lacks DELETE), lib/auth.ts (in-house lib with founder-voice header), api/webhooks/stripe (data/external event handler with switch over event.type). Subsequent plans use `<repo-path> = /Users/yang/lahacks/bootstrap-demo-target/` and operate on commit `253ecb0` (clean working tree, branch=main, no remote). Commits 5a65e39 → 253ecb0 (16 total inside bootstrap-demo-target git repo, NOT the contract-ide repo). Wave 3 next: plans 14-03/04/05 (codebase-to-contracts skill operations against Marginalia) + 14-06 (demo recording).

**Previous status (sibling wave-1 plan):** Phase 14 Plan 01b COMPLETE (2026-04-25). BOOTSTRAP-01 executable half landed alongside 14-01a's documentation half. `.agents/skills/codebase-to-contracts/` is now self-contained at clone time. Skill package.json declares 5 runtime deps (tinyglobby, js-yaml, @babel/parser, @babel/traverse, @babel/generator); pnpm install ran successfully; node_modules (~8.5MB, 580+ files) committed per RESEARCH Pattern 5 bundled-deps strategy — root .gitignore patched with negation override (`!.agents/skills/codebase-to-contracts/node_modules/**`) so global `node_modules/` rule doesn't strip them. Four helper modules: `deterministic-uuid.mjs` (UUIDv5 from (repo, path, ast_anchor) using RFC 4122 §C.2 URL namespace — single source for all node UUIDs across plans 14-03/04/05), `claude-cli-bridge.mjs` (spawn-based async wrapper for `claude -p` with BOOTSTRAP_TEST_MODE=1 short-circuit + BOOTSTRAP_CLAUDE_MODEL pinning to claude-sonnet-4-6), `frontmatter-writer.mjs` (js-yaml writer matching Phase 2 serde_yaml_ng fence convention `\n---\n` on both sides), `babel-parser-bridge.mjs` (3-tier resolution: skill-bundled → target node_modules → target pnpm virtual store; tier 3 mirrors `resolvePnpmDep` from contract-ide-demo/contract-uuid-plugin/index.js verbatim). Three Babel-loader templates: `contract-uuid-plugin-loader.js` (BYTE-IDENTICAL copy of contract-ide-demo's loader modulo a 5-line provenance header — single diff hunk at top), `contract-uuid-plugin-package.json` (verbatim + `_comment` provenance), `next-config-snippet.ts` (insertion-ready webpack hook with BOOTSTRAP-INSERT-START/-END markers for idempotent re-run splicing). Three test suites: deterministic-uuid (6 tests — determinism + input-discrimination + UUIDv5 regex), frontmatter-writer (4 tests — round-trip parity against TWO real Phase 9 fixtures: UI L4 `a1000000-...` + backend L3 ambient `api-account-delete-001` + fence pattern + empty-array serialization), schema-vs-Rust parity (1 conditional test — parses Rust Frontmatter struct via regex, asserts every non-Option field appears in JSON Schema required[], prints parity table; SKIPS gracefully when 14-01a's schemas/frontmatter.json is absent so wave-1 parallel execution works). `pnpm test` reports 10 pass / 0 fail / 1 skip. Phase 14 revision Issue 2 closed (skill has its own deps); Issue 12 closed (parity smoke ships). Two Rule 3 deviations auto-fixed: (a) Node 24 `--test` directory regression — package.json `scripts.test` updated to glob `'scripts/helpers/__tests__/*.test.mjs'`; (b) root .gitignore negation override (called out in plan as conditional anti-pattern guard). Open Question 6 (symlink vs copy for plugin sync): v1 = copy + manual sync; deferred for Yang's ratification. Commits 07f2964 (package.json + bundled deps), fd1b1eb (helper modules), 19d4be7 (Babel templates), bfb819d (tests + glob fix). Wave 2 next: plans 14-02/03/04/05/06.

**Previous status (sibling wave-1 plan):** Phase 14 Plan 01a COMPLETE (2026-04-25). BOOTSTRAP-01 documentation half landed — Anthropic Agent Skills v1 package (.agents/skills/codebase-to-contracts/) with SKILL.md (157 lines, well under 500-line guideline; trigger phrases "bootstrap contracts" / "set up Contract IDE for this repo" / "generate contracts from this codebase" / "make this repo Contract IDE-ready" front-loaded in folded-scalar description; paths restriction to **/.contracts/** + Next.js shapes), four progressive-disclosure references docs (output-schema with annotated exemplar excerpts, classification-rules with heuristic kind/level/parent_hint table, jsx-alignment with Babel parser config + outermost-contained matcher pseudocode, flow-synthesis with import-resolution + invocation-order walk), and four draft-07 JSON Schemas (frontmatter mirroring all 17 ContractFrontmatter Rust fields with $comment block documenting Rust-vs-JSON parity contract for Plan 14-01b's writer test; contract-body with intent>=50 chars + role>=30 chars + inputs/outputs items minLength:5 (Issue 6 parity with backend_section_validator.rs); flow with members minItems:2; classify with confidence>=0.6 gate). Refusal-on-ambiguity codified — Stage 4 zero-match aborts emit rather than fabricating ranges. Atomic emit pattern documented — skill writes under .staging/ until every gate passes, then atomic mv to .contracts/. Plan 14-01b (parallel) shipped scripts/ + templates/ + package.json + node_modules under same skill dir; 14-01a deliverables strictly limited to SKILL.md + references/ + schemas/. Commits 6fdb5a0, c8af53f.

**Phase 13 Plan 10b COMPLETE (2026-04-25):** DEMO-04 UI orchestration layer shipped — Wave 5's UI half (13-10a is the data half). Plan 13 progress: 11/12 plans complete; plan 13-11 (Wave 6, final plan) outstanding. `commands/sync.rs` extended via `load_blast_radius_fixture` to read `blast-radius.json` from disk (replaces 13-09 hardcoded placeholders; preserves `SyncTriggerResult` shape; honors `CONTRACT_IDE_DEMO_FIXTURE_DIR` env override; graceful empty-array fallback + stderr log on read/parse failure). New `commands/demo_orchestration.rs` ships `load_beat3_verifier_fixture` + `emit_beat4_harvest` (the latter emits `substrate:nodes-added` Tauri event with `harvested_nodes` array, each carrying `attached_to_uuid` per N9 for HarvestPanel's primary subscription path + green-halo wiring). TS wrappers `loadAndApplyBeat3Verifier` + `triggerBeat4Harvest`. New `DemoOrchestrationPanel` (bottom-left z-50; three single-click triggers; dismissible; gated on `import.meta.env.DEV`) mounted additively in AppShell alongside 13-08 PRReviewPanel + 13-09 SyncButton/VerifierPanel/HarvestPanel. Wave 5 serialization compliance: `load_beat3_verifier_fixture` + `emit_beat4_harvest` appended to `lib.rs` AFTER 13-09's `trigger_sync_animation`. **Task 3 verification DEFERRED to plan 13-11 by user direction** — 13-11 rehearsal is the natural test surface for real Phase 9 uuid substitution + 4-beat end-to-end (3 runs) + cmdp-precision ≥8/10 gate. Phase 14 data realism (in flight in parallel) feeds 13-11. Pre-existing tsc error at `AppShell.tsx:425` flagged out-of-scope per Rules scope-boundary; logged to phase deferred-items.md for 13-11 to resolve. Commits 4d648fd (Task 1), f6f6f8d (Task 2). Wave 5 COMPLETE; Wave 6 next: plan 13-11.

**Previous status:** Phase 13 Plan 10a COMPLETE (2026-04-25): Demo data layer shipped — pure shell + SQL + JSON fixtures, zero UI/Rust/React touches per Wave 5 SF5 split. `contract-ide/demo/seeds/substrate.sqlite.seed.sql` (defensive `CREATE TABLE IF NOT EXISTS substrate_nodes` + `l0_priority_history`; 5 verbatim demo rules from scenario-criteria.md § 6 + 1 parent-surface `con-settings-no-modal-interrupts-2025-Q4` with `state='intent_drifted'` for orange-flag § 8 fixture + priority-history rows for `reduce-onboarding-friction` superseded `2026-04-01` by `compliance-first` + 12 ambient padding nodes spanning Tailwind/Postgres/Zod/Radix/etc. so substrate doesn't read as 1-shot demo data; `PRAGMA user_version = 13`). Three JSON fixtures use `PLACEHOLDER-*` uuid strings for unambiguous Phase 9 substitution by 13-11 rehearsal: `blast-radius.json` (trigger + 5 participants), `beat3-verifier.json` (6 honor rows + 3 implicit decisions + 1 flag with `parentSurfaceUuid`), `beat4-harvest.json` (3 harvested nodes — one `promoted_from_implicit: true` + each carrying `attached_to_uuid` per checker N9 for green-halo wiring). `contracts/README.md` documents Phase 9 sidecar contract list for plan 13-11 `--verify-contracts` rehearsal flag. `reset-demo.sh` 5-step script (pkill app + dev-server → git reset demo repo to `DEMO_LOCKED_COMMIT` → backup `.before-reset.bak` + apply sqlite seed → open `.app` bundle or fallback `tauri dev` → 3s boot wait); env-overrideable `REPO_ROOT`/`DEMO_REPO_DIR`/`DEMO_LOCKED_COMMIT`; budget ~11s nominal under 15s ceiling. End-to-end timed run deferred to plan 13-11 rehearsal. Sibling 13-10b runs in parallel (UI orchestration); zero file overlap. Commits eace1b3 (SQL seed + 3 JSON fixtures + README), 11d58ba (reset-demo.sh).

**Previous status:** Phase 13 Plan 09 COMPLETE (2026-04-25). SUB-09 mocked-Sync clause + SUB-08 verifier/harvest clauses shipped — SyncButton + animateSyncBlastRadius + trigger_sync_animation Rust IPC (placeholder uuids today; 13-10b extends to read blast-radius.json); VerifierPanel + ImplicitDecisionsGroup + 8s orange-flag halo on parent screen card; HarvestPanel dual-channel substrate:nodes-added subscription + 2s poll fallback + [⌃ promoted from implicit] amber badge; window.__demo.loadBeat3VerifierResults debug helper. Wave 4 serialization: trigger_sync_animation appended to lib.rs AFTER 13-08's analyze_pr_diff. Visual verification DEFERRED to plan 13-10b by user direction — natural test surface via DemoOrchestrationPanel + fixture-loaded uuids. Commits 8c4165d, 83cf126. Wave 4 COMPLETE (13-07 + 13-08 + 13-09 landed). Wave 5 next: 13-10a (data layer) + 13-10b (UI orchestration) — zero file overlap, full parallel.

**Previous status:** Phase 13 Plan 08 COMPLETE (2026-04-25). SUB-09 PR-review intent-drift mode shipped — `parseDiffHunks` (pure TS, 4 vitest cases) + `mapDiffToNodes` + `analyze_pr_diff` Tauri IPC (sqlx join over `nodes.code_ranges` JSON ∩ `substrate_nodes.intent_drift_state == 'DRIFTED'`, defensive on missing table/column, 2 unit tests) + `PRReviewPanel` (right-edge 420px sliding panel; raw-diff textarea + Analyze button + Cancel) + `PRReviewExplanation` (affected nodes grouped by parent_uuid with ⚠ markers for intent_drifted subset; stable Zustand selector + useMemo derivation per Phase 13-06 lesson) + Cmd+Shift+P toggle binding (defensive non-collision with Cmd+P IntentPalette). Snapshot-and-restore overlay via `useRef<Map<uuid, SubstrateNodeState | null>>` — Cancel/unmount restore is leak-safe; re-Analyze without Cancel restores prior overlay before snapshotting new one. Apply intent_drifted overlay to ALL affected uuids for demo simplicity (every diff-touched atom orange-glows); explanation distinguishes substrate-drifted subset via ⚠. Defensive substrate intersection mirrors 13-01 pattern (substrate_table_exists + intent_drift_column_present checks). JSON field naming bridges Rust/TS — DiffHunkInput uses `#[serde(rename_all = "snake_case")]`; `nodes.code_ranges` JSON read via snake_case `start_line`/`end_line` per Rust serde default. Wave 4 serialization compliance: `analyze_pr_diff` appended to lib.rs `generate_handler!` AFTER 13-06's `capture_route_screenshot`; 13-09's `trigger_sync_animation` will append AFTER this. Sibling 13-07 ran in parallel (zero file overlap — citation halo wiring on Inspector/ServiceCard/ScreenCard/AtomChip). vitest 86 pass + 1 skipped (was 82+1, +4 new diffToNodeMapper); tsc clean; cargo check + 2 pr_review unit tests pass. Commits 1fa66b3, 69a296f.

**Previous status:** Phase 13 Plan 05 COMPLETE (2026-04-25). CARD-01 + CHIP-01 ScreenCard + atom-chip overlay shipped (visual verification deferred to plan 13-06 by user direction). ScreenCard react-flow node: probe via Phase 4 probeRoute IPC, iframe at fullUrl with parent-layer AtomChipOverlay, Inspect/Interact mode toggle. requestChipRects async helper prefers same-origin iframe.contentDocument.querySelectorAll('[data-contract-uuid]') (works under Phase 4 frame-src CSP), graceful postMessage fallback with 250ms timeout. AtomChip canonical selectNode + setFocusedAtomUuid (checker N7) + data-atom-uuid/data-state attributes for plan 13-07 citation halo. screenCardStyles CVA matches plan 13-01 NodeVisualState exactly. nodeTypes.ts: append-only — `screenCard: ScreenCard` after plan 13-04's `serviceCard` entry per Wave 2 serialization_hint. lib.rs UNTOUCHED. 5 + 8 = 13 vitest cases pass; tsc clean; vite build succeeds. Visual verification deferred to plan 13-06's checkpoint (FlowChainLayout naturally mounts ScreenCard inside L2 vertical chain — natural integration test surface). Empty-element + section-bottom fallback deferred to plan 13-10b. Commits d66cbc9, 4089fda.
Last activity: 2026-04-26 — Phase 14 Plan 05 complete (BOOTSTRAP-02 + BOOTSTRAP-03 + BOOTSTRAP-04 [proposed] all closed: Stage 5a synthesize-flows.mjs + Stage 5b emit.mjs + install-babel-plugin.mjs + validate.mjs shipped. Watchdog timeout in callClaude (Plan 14-04 SUMMARY mitigation), idempotent Babel-plugin install via BOOTSTRAP-INSERT marker-bracketed find-and-replace, validator gate w/ 3-layer dispatcher (Rust subprocess → JS fallback w/ degraded warning → schema allOf re-assertion), 16 flows synthesized on Marginalia smoke. In-flight Rule 1 fix: flow slug HTTP-method prefix to disambiguate multi-verb route.ts. 42/42 skill tests green. Commits ae2dc7e, 61cd49a, 0107f1a). Phase 14 progress: 6/7 (waves 1+2+3+4 done; 14-06 + 14-07 + 14-08 outstanding).

Previous activity: 2026-04-25 — Phase 14 Plan 04 complete (BOOTSTRAP-02 [proposed] continued: Stage 3 derive-body.mjs + Stage 4 align-jsx.mjs shipped; prose-quality gate APPROVED on iteration 1 — Yang verified 3 sample derivations spanning data L2 + UI L4 + UI L3 all pass on decision specificity + cross-reference density + prose density without prompt edits; in-flight bridge fix to claude-cli-bridge.mjs (top-level allOf stripping + path→inline schema mode + --dangerously-skip-permissions) unblocked all stage-2/3 LLM calls and retroactively completed Plan 14-03's Stage 2 from 15/40 → 40/40 frontmatter files; commits a739a35, 2c4fd37, 13f96ac). Phase 14 progress: 5/7 (waves 1+2+3a done; 14-05 + 14-06 outstanding).

Previous activity: 2026-04-25 — Phase 14 Plan 02 complete (BOOTSTRAP-05: Marginalia bootstrap demo target scaffolded at /Users/yang/lahacks/bootstrap-demo-target/ — 51 src files, 16 organic backdated commits over 21 days, Next 16 + Prisma 7 + argon2 sessions in-house + Stripe + Resend + 8 shadcn components + visibly-stubbed Danger Zone + founder-voice README; Yang ratified option-a + product name Marginalia at Task 1 checkpoint; build + tsc + seed all clean; no .contracts/ or contract-uuid-plugin yet — plans 14-03/04/05 produce them). Phase 14 progress: 3/7 (waves 1+2 done; wave 3 next: 14-03/04/05/06).

Previous activity: 2026-04-25 — Phase 13 Plan 10b complete (DEMO-04 UI orchestration: sync.rs reads blast-radius.json + new demo_orchestration.rs IPCs + DemoOrchestrationPanel mounted dev-only; Task 3 verification deferred to plan 13-11 by user direction; commits 4d648fd, f6f6f8d). Phase 13 progress: 11/12 (Wave 6 plan 13-11 outstanding).

Previous activity: 2026-04-25 — Phase 14 Plan 01b complete (BOOTSTRAP-01 executable half: skill package.json + bundled node_modules ~8.5MB committed + 4 helper modules + 3 Babel-loader templates + 3 test suites; 10 pass/1 skip via pnpm test; Phase 14 revision Issues 2 + 12 closed; commits 07f2964, fd1b1eb, 19d4be7, bfb819d). Wave 1 COMPLETE (14-01a + 14-01b shipped in parallel, zero file overlap).

Previous activity: 2026-04-25 — Phase 14 Plan 01a complete (BOOTSTRAP-01 documentation half: SKILL.md 157 lines + 4 references docs + 4 draft-07 JSON Schemas; commits 6fdb5a0, c8af53f).

Previous activity: 2026-04-25 — Phase 13 Plan 09 complete (SUB-09 mocked-Sync clause + SUB-08 verifier/harvest clauses).

Earlier activity: 2026-04-25 — Phase 13 Plan 08 complete (SUB-09 PR-review intent-drift mode — parseDiffHunks pure TS + analyze_pr_diff Rust IPC + PRReviewPanel sliding panel with snapshot-and-restore Cancel + Cmd+Shift+P toggle binding).

Earlier activity: 2026-04-25 — Phase 13 Plan 05 complete (CARD-01 + CHIP-01 ScreenCard + atom-chip overlay; visual verification deferred to plan 13-06 per user direction). Wave 2 COMPLETE (4 plans: 13-04 ServiceCard, 13-02 Sidebar, 13-03 Cmd+P, 13-05 ScreenCard).

Progress: [██████████] 96% (7/13 phases complete + Phase 10 4/4 + Phase 11 Wave 2 complete + Phase 12 4/4 + Phase 13 9/12 — Phases 1, 2, 3, 5, 6, 7, 10 complete; Phase 4 partially landed; Phase 8 implementation done pending UAT; Phase 9 code-shipped 8/8 plans; Phase 11 Plans 01–04 complete; Phase 12 VERIFIED ✓ (verifier passed 4/4 SCs 2026-04-25; report at `.planning/phases/12-conflict-supersession-engine/12-VERIFICATION.md`); Phase 13 Plans 01–09 + 10a + 10b complete (11/12); plan 13-11 outstanding; UAT execution (09-UAT.md) is the Phase 9 closure gate)

## Performance Metrics

**Velocity:**
- Total plans completed: 13
- Average duration: ~30min
- Total execution time: ~324min

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01-foundation | 4 | ~195min | ~49min |
| 02-contract-data-layer | 3 | ~76min | ~25min |
| 05-mcp-server-sidecar | 1 | ~5min | ~5min |
| 03-graph-canvas | 3 | ~65min | ~22min |

**Recent Trend:**
- Last 5 plans: 01-04 (~30min, 2 tasks incl. human-verify across dual launch, 7 files), 01-03 (~90min, 3 tasks incl. human-verify, 11 files), 01-02 (~15min, 3 tasks, 10 files), 01-01 (~60min, 3 tasks, 11 files)
- Trend: 01-04 fast because the hard work (day0 validation) was pre-landed; mostly wiring Rust commands + a dev panel and running the human verification matrix. Pill-gate bug caught in verification; fixed inline.

*Updated after each plan completion*
| Phase 02-contract-data-layer P01 | 25 | 2 tasks | 6 files |
| Phase 02-contract-data-layer P02 | 6 | 3 tasks | 14 files |
| Phase 02-contract-data-layer P03 | 45+ | 2 tasks | 8 files |
| Phase 05 P01 | 5min | 2 tasks | 22 files |
| Phase 03-graph-canvas P01 | 15 | 2 tasks | 11 files |
| Phase 03-graph-canvas P02 | 5 min | 2 tasks | 11 files |
| Phase 03-graph-canvas P03 | 45 min | 2 tasks (+drive-by) | 12 files |
| Phase 06-contract-derivation P01 | ~3 min | 2 tasks | 6 files |
| Phase 04-inspector-monaco P01 | ~45 min | 4 tasks (Task 3 split 3a+3b) | 15 files |
| Phase 04-inspector-monaco P02 | ~4min | 2 tasks | 7 files |
| Phase 04-inspector-monaco P03 | ~2min | 2 tasks | 7 files |
| Phase 07-drift-detection-watcher-path P01 | ~5min | 3 tasks | 6 files |
| Phase 07-drift-detection-watcher-path P02 | ~3min | 2 tasks | 5 files |
| Phase 07-drift-detection-watcher-path P03 | ~2min | 2 tasks | 5 files |
| Phase 07-drift-detection-watcher-path P04 | ~15min + UAT | 3 tasks (T3=UAT) | 4 files |
| Phase 08 P01 | 11 | 3 tasks | 14 files |
| Phase 08 P05 | 14 | 2 tasks | 18 files |
| Phase 08 P01 | 11 | 3 tasks | 14 files |
| Phase 08 P02 | 11 | 2 tasks | 12 files |
| Phase 08 P04a | 13 | 2 tasks | 10 files |
| Phase 08 P04b | 12 | 2 tasks | 17 files |
| Phase 08 P06 | 15 | 2 tasks (T3=UAT) | 21 files |
| Phase 10-session-watcher-filter-pipeline P01 | 4min | 2 tasks | 8 files |
| Phase 10-session-watcher-filter-pipeline P02 | ~8min | 2 tasks | 5 files |
| Phase 10-session-watcher-filter-pipeline P03 | ~10min | 3 tasks | 7 files |
| Phase 10-session-watcher-filter-pipeline P04 | ~55min wall (UAT-heavy) | 3 tasks (T3=UAT) | 11 files |
| Phase 09-mass-edit-non-coder-mode-demo-repo-seeding P03 | 5 | 2 tasks | 11 files |
| Phase 09 P01 | 9 | 2 tasks | 13 files |
| Phase 09-mass-edit-non-coder-mode-demo-repo-seeding P04 | 19 | 2 tasks | 39 files |
| Phase 09 P02 | 9 | 2 tasks | 11 files |
| Phase 11-distiller-constraint-store-contract-anchored-retrieval P01 | 25 | 2 tasks | 7 files |
| Phase 09 P04c | 17 | 2 tasks | 28 files |
| Phase 11 P03 | 13 | 3 tasks | 9 files |
| Phase 11 P04 | 30 | 3 tasks | 18 files |
| Phase 09-mass-edit-non-coder-mode-demo-repo-seeding P05 | 8 | 2 tasks | 9 files |
| Phase 09-mass-edit-non-coder-mode-demo-repo-seeding P06 | 25 | 2 tasks (T4 deferred) | 4 files |
| Phase 12-conflict-supersession-engine P01 | 5min | 2 tasks | 5 files |
| Phase 12-conflict-supersession-engine P02 | 7min | 2 tasks | 9 files |
| Phase 11-distiller-constraint-store-contract-anchored-retrieval P05 | 5 | 2 tasks | 9 files |
| Phase 12-conflict-supersession-engine P03 | 8min | 2 tasks | 5 files |
| Phase 13 P01 | 8 min | 2 tasks | 9 files |
| Phase 12-conflict-supersession-engine P04 | 11min + UAT | 3 of 3 (Task 3=live UAT approved 2026-04-25) tasks | 13 files |
| Phase 13-substrate-ui-demo-polish P04 | 6 min 25 sec | 2 tasks | 8 files |
| Phase 13-substrate-ui-demo-polish P02 | 8 min | 3 tasks | 8 files |
| Phase 13-substrate-ui-demo-polish P03 | ~30 min wall (Tasks 1+2 ~28min initial, Task 3 fix on resume) | 3 tasks (incl. checkpoint:human-verify) | 7 files |
| Phase 13-substrate-ui-demo-polish P05 | 18 min | 2 of 3 (Task 3 checkpoint:human-verify deferred to plan 13-06 per user direction) tasks | 8 files |
| Phase 13-substrate-ui-demo-polish P06 | ~95 min wall (initial Tasks 1+2 ~50min, three Rule 1 deviation fixes during checkpoint ~30min, doc finalization ~15min) | 3 of 3 (incl. checkpoint:human-verify approved after 3 deviation fixes) | 15 files (8 created + 7 modified) |
| Phase 13-substrate-ui-demo-polish P07 | 2 min 30 sec | 2 tasks | 8 files |
| Phase 13 P08 | 4 min | 2 tasks | 8 files |
| Phase 13-substrate-ui-demo-polish P09 | 12 min | 2 tasks | 11 files |
| Phase 14 P01b | 5min | 4 tasks | 13 files |
| Phase 14 P01a | 5min | 2 tasks | 9 files |
| Phase 13 P10a | 10 | 2 tasks | 6 files |
| Phase 13 P10b | 12 | 2 tasks (T3=deferred to 13-11) | 7 files |
| Phase 14-codebase-to-contracts-bootstrap-skill-demo-application P02 | 85min | 2 tasks | 51 files |
| Phase 14-codebase-to-contracts-bootstrap-skill-demo-application P03 | 6 min | 2 tasks | 8 files |
| Phase 14-codebase-to-contracts-bootstrap-skill-demo-application P04 | ~95 min wall (Tasks 1+2 ~60min, in-flight bridge-fix debug ~25min, checkpoint review + approval ~10min) | 3 tasks (incl. checkpoint:human-verify approved iteration 1) | 12 files (11 created + 1 modified bridge fix) |
| Phase 14-codebase-to-contracts-bootstrap-skill-demo-application P05 | 13 min | 2 tasks (+1 in-flight Rule 1 fix) | 11 files (10 created + 1 modified bridge watchdog) |
| Phase 14-codebase-to-contracts-bootstrap-skill-demo-application P05 | 13 min | 2 tasks | 11 files |
| Phase 15 P01 | 9 | 3 tasks | 7 files |

## Accumulated Context

### Roadmap Evolution

- Phase 14 added: Codebase-to-Contracts Bootstrap (Skill + Demo Application)
- Phase 15 added (2026-04-25): Substrate Trust Surface — Cmd+P substrate search, refine with chained-version audit, delete with mandatory reason + impact preview. Architecture locked: chained immutable versions (preserves Phase 12 "facts at time T" model). Reassigns Phase 15 from the previously-noted decisions-manifest scope (deferred to v2). Requirements TRUST-01..04 added to REQUIREMENTS.md.

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Plan 14-04: Verbatim exemplar embedding (NOT abstract "match Phase 9 prose density" instruction) is the prose-quality strategy — 4 real .md bodies interpolated into the system prompt at startup
- Plan 14-04: Prose-quality gate via `--sample=N` flag — $0.30 + <30s for 3 nodes vs ### Decisions

.50 + 3min for full pipeline; passed on iteration 1 with zero prompt edits
- Plan 14-04: Stage 4 outermost-JSX matcher (not first-or-deepest) — when candidate range contains nested JSX, parent JSXElement is the correct anchor for chip overlay positioning
- Plan 14-04: Refuse-to-emit on Stage 4 zero-match — `process.exit(1)` + `_stage4_failures.json` diagnostic; never fabricate code_ranges (a fake range = false-positive verifier signal at demo time, cardinal sin)
- Plan 14-04: Multi-match LLM tiebreak via single bare `claude -p` call (no schema, integer back) — capped at 1 LLM call per ambiguity, far cheaper than re-deriving the body
- Plan 14-04: Schema-API-boundary stripper in `claude-cli-bridge.mjs` — top-level `allOf`/`oneOf`/`anyOf` removed before calling claude -p; full schema constraint re-asserted post-hoc by Plan 14-05's `validate.mjs` (defense in depth, not bypass)
- Plan 14-04: Path→inline schema mode in claude-cli-bridge — `--json-schema <PATH>` silently hangs the subprocess; reading + JSON.stringify + passing inline works reliably
- Plan 14-04: Bridge fix retroactively unblocked Plan 14-03 — Stage 2 had silently completed only 15/40 frontmatter calls; after fix, all 40/40 present in `.contracts/.staging/`
- Plan 14-04 → 14-05 handoff: validate.mjs MUST re-include the top-level `allOf` guards that the bridge strips; otherwise the strip becomes silent constraint-relaxation rather than defense-in-depth
- Plan 14-04 → 14-05 mitigation: add a watchdog timeout in `callClaude()` so silent hangs surface as errors rather than infinite waits
- Plan 13-10b: verification deferred to plan 13-11 — natural test surface for real uuid substitution + 4-beat end-to-end + cmdp-precision gate
- Plan 13-10b: Wave 5 complete — 13-10a (data layer: SQL seed + JSON fixtures + reset-demo.sh) + 13-10b (UI orchestration: fixture-loading IPCs + DemoOrchestrationPanel) shipped in parallel, zero file overlap
- Plan 13-10b: Wave 6 next — 13-11: runbook v2 + live-scenario.md replacement + real Phase 9 uuid substitution + 3-rehearsal validation + 13-UAT.md covering all 11 SCs
- Plan 13-10b: Pre-existing AppShell.tsx:425 tsc error flagged for plan 13-11 to resolve
- Plan 13-06: 13-06 approved with three Rule 1 deviations caught during user verification (AtomChipOverlay infinite render loop, cross-origin postMessage chip rects, cross-origin canvas screenshot)
- Plan 13-06: Phase 14 handoff — ServiceCard uuid-as-name fallback when contract `name` field is missing (Phase 14 codebase-to-contracts bootstrap will populate human-readable names)
- Plan 13-06: Demo repo (contract-ide-demo) commit 6b0ceb1 is on detached HEAD — preexisting condition, NOT caused by this session; user to fast-forward master if desired
- Plan 13-06: Wave 4 next — 13-07 SourceArchaeologyModal (autonomous), 13-08 PRReviewPanel (autonomous), 13-09 SyncButton+Verifier+Harvest (checkpoint)
- Roadmap: Phase 5 (MCP sidecar) can run in parallel with Phase 3 (graph canvas) after Phase 2 completes — explicit parallelization window
- Roadmap: Phases 8+9 of ARCHITECTURE.md consolidated into ROADMAP Phase 9 (mass edit + demo polish share the same demo-day delivery boundary)
- Critical: Monaco CSP (`blob:` in `script-src`) and `vite-plugin-monaco-editor` must be installed at Phase 1 scaffold time — not retrofitted later
- Critical: `onlyRenderVisibleElements` on the react-flow canvas must be set at Phase 3 scaffold time
- Critical: No `#[tokio::main]` anywhere in Rust source — Tauri owns the async runtime
- Critical: JSONL receipt parser must be defensive with mock fallback; every demo beat ends on a receipt card
- Plan 01-01: shadcn v4 CLI uses `--template vite --base radix --preset nova` (`--base-color slate` deprecated in v4)
- Plan 01-01: Tailwind v4 + `@tailwindcss/vite` + `@types/node` must be installed BEFORE `shadcn init` (v4 prereq)
- Plan 01-01: `macOSPrivateApi: true` belongs in the same task as the `macos-private-api` Cargo feature — split across tasks creates transient inconsistency
- Plan 01-01: Retain `tauri-plugin-opener` from scaffold alongside `tauri-plugin-shell` — zero conflict, avoids unnecessary churn
- Plan 01-01: shadcn's `@layer base { bg-background }` overrides transparent html/body/#root — place the transparent-override block AFTER `@layer base` with `!important` (required for vibrancy)
- Plan 01-01 follow-up (non-blocking): Tauri bundle identifier `com.contract-ide.app` conflicts with the `.app` macOS extension convention — cosmetic rename deferred
- Plan 01-02: `tauri-plugin-sql` migrations only run at startup if the DB URL is listed in `tauri.conf.json` plugins.sql.preload — `add_migrations()` in the Rust builder alone defers execution to the frontend's first `Database.load()`
- Plan 01-02: `tauri::generate_handler!` cannot resolve `#[tauri::command]` functions through `pub use` re-exports — the generated `__cmd__<name>` shim lives where the fn is defined; always use fully-qualified paths like `commands::nodes::get_nodes`
- Plan 01-02: RESEARCH.md Pattern 2's `CREATE INDEX idx_receipts_node_uuid ON receipts(id)` was a bug (indexes the PK); corrected to `receipt_nodes(node_uuid)` on the join table DATA-02 already requires
- Plan 01-02: Migration v1 description `"create_core_tables"` is declared immutable — all future schema changes ship as numbered v2+ migrations (Pitfall 5)
- Plan 01-03: react-resizable-panels v4 requires PERCENTAGE STRINGS (`"18%"`), not bare numbers, for defaultSize/minSize/maxSize/collapsedSize — bare numbers are interpreted as pixels per the internal `bt()` dispatcher. Caught during human-verify when sidebar rendered as 18px strip. Add to v4-dep-upgrade checklist.
- Plan 01-03: Blur-triggered autosave is per-spec for Phase 1 (SHELL-05); debounced-while-typing autosave deferred to Phase 4 (Monaco integration owns the change-event cadence). Documented as known UX limitation, not a deviation.
- Plan 01-03: zundo temporal middleware uses `partialize({ contractText })` so Cmd+Z doesn't yo-yo the `isDirty` flag and flicker the save-status line.
- Plan 01-03: `?force-error` URL-param override in GraphPlaceholder is demo/verification infrastructure — marked for removal in Phase 2 when real IPC failure modes exist.
- Plan 01-04: `import.meta.env.DEV` is FALSE under `tauri build --debug` — vite always builds in production mode regardless of Tauri's `--debug` flag. Gating dev-only widgets on `DEV` hides them in the exact `.app` bundle used for Finder-launch verification. Removed the gate on the Day-1 Validation pill/panel and marked the widget for explicit deletion in Phase 9 polish instead of quarantining it behind a flag that breaks the intended test surface.
- Plan 01-04: Pitfall-4 (subprocess env inheritance under Finder-launched `.app`) cleared on this machine with zero workaround — default `tauri-plugin-shell` `output()` carries HOME + PATH correctly. Documented as load-bearing: if a different machine later fails Check A under Finder launch, first debug step is "which env var is missing on THAT machine," not "rebuild the subprocess pipeline."
- Plan 01-04: `tauri build -- --debug` DMG packaging (`bundle_dmg.sh`) fails but `.app` bundle builds fine. `.app` is the artifact used for Phase 1 criterion (6); DMG failure deferred to Phase 9 distribution work as known build noise.
- [Phase 02-contract-data-layer]: serde_yaml_ng uses underscores in Cargo.toml (not serde-yaml-ng hyphenated); code references as serde_yaml_ng
- [Phase 02-contract-data-layer]: parse_sidecar closing fence guard uses \n---\n (newline on both sides) to prevent in-body --- misparse (Pitfall 6); parse/write confirmed round-trip safe
- [Phase 02-contract-data-layer]: tauri-plugin-fs requires watch feature in Cargo.toml at declaration time — omitting it omits the notify backend and panics watch() at runtime (Pitfall 7)
- [Phase 02-contract-data-layer]: DbPool enum match required — upsert_node_pub takes &DbPool and matches DbPool::Sqlite(pool); plan skeleton &Pool<Sqlite> was wrong
- [Phase 02-contract-data-layer]: edges table uses source_uuid/target_uuid not from_uuid/to_uuid; edge_type NOT NULL bound as 'neighbor'; deterministic id as '{from}->{to}'
- [Phase 02-contract-data-layer]: sqlx 0.8 added as direct dep — tauri-plugin-sql does not re-export sqlx::query or sqlx::Row
- [Plan 02-03]: Critical — Tauri v2 fs plugin defaults requireLiteralLeadingDot=true on Unix; ** globs can NEVER match .contracts/ or any dot-directory without plugins.fs.requireLiteralLeadingDot=false in tauri.conf.json. This config is now present and must NOT be removed.
- [Plan 02-03]: Belt-and-braces scope strategy for dot-directory watch: requireLiteralLeadingDot=false (load-bearing) + fs:scope **/**/* globs in capabilities + runtime allow_directory() with /tmp→/private/tmp canonicalization in open_repo
- [Plan 02-03]: Dup detection is scan-level only (open_repo/scan_contracts_dir) — refresh_nodes intentionally does not cross-check other files; it only upserts the changed file. Cross-file dup detection requires a full directory view.
- [Plan 02-03]: Delete-event deferral — refresh_nodes skips non-existent paths with inline TODO(Phase 7); Phase 2 watcher handles create/modify only
- [Phase 02-contract-data-layer]: Plan 02-03: requireLiteralLeadingDot=false is load-bearing for .contracts/ watcher on Unix — Tauri v2 fs plugin default blocks ** globs from matching dot-directories
- [Plan 05-01]: @modelcontextprotocol/sdk pinned to ^1.29.0 after `npm show` confirmed v1 still current stable (v2 pre-alpha not shipped). Import paths isolated to mcp-sidecar/src/index.ts so a future v2 migration is a single-file change.
- [Plan 05-01]: @yao-pkg/pkg --targets node20 worked on aarch64-apple-darwin with no ABI override — better-sqlite3 12.x prebuilt for Node 20 on arm64-darwin was picked up cleanly. If a future build hits ABI mismatch (sidecar starts then immediately emits stopped), try --targets node22 per Pitfall 2.
- [Plan 05-01]: Both string-form AND object-form `shell:allow-execute` entries kept in capabilities/default.json — object-form narrows sidecar exec; string-form preserves Phase 1/2 shell usage. Removing the string form risks commands::validation shell paths.
- [Plan 05-01]: McpStatusIndicator landed in fixed-position bottom-right footer OUTSIDE the ResizablePanelGroup so it survives panel resize/collapse. Phase 9 polish may relocate — placement intentionally minimal per plan guidance.
- [Plan 05-01]: launch_mcp_sidecar is non-panicking — emits mcp:status{stopped, reason} on any failure path (no app_data_dir / sidecar() Err / spawn() Err) so the app still boots with an `MCP offline` pill if the binary is missing.
- [Plan 05-01]: McpStatusIndicator seeds from get_mcp_status on mount AND subscribes to mcp:status — race confirmed during smoke test where Vite was still optimising @tauri-apps/api/event when Rust emitted the ready event. Do NOT remove either path.
- [Plan 05-01]: Target triple observed on dev machine: aarch64-apple-darwin. Binary output: src-tauri/binaries/mcp-server-aarch64-apple-darwin (54 MB Mach-O arm64).
- [Plan 05-01]: CommandChild MUST be stored in Tauri managed state (McpSidecarHandle) — dropping it kills the sidecar (Pitfall 3). Verified live via `ps aux` showing the sidecar alive 7+ seconds after setup() returned.
- [Plan 05-01]: `shell:allow-execute` generic string permission does NOT cover sidecar binaries — object form with `sidecar: true` is required (Pitfall 4).
- [Phase 03-graph-canvas]: [Plan 03-01]: cva variant keys for booleans must use actual boolean defaults (canonical: true) not strings — TS treats variant keys 'true'/'false' as boolean literals and rejects string passthrough with TS2322
- [Phase 03-graph-canvas]: [Plan 03-01]: @xyflow/react v12 custom nodes should use plain NodeProps (no generic) in the signature and narrow via 'const d = data as T' in the body — parameterising NodeProps<T> triggers a variance error through memo() + nodeTypes. Add [key: string]: unknown to the data type to satisfy xyflow's Record constraint
- [Phase 03-graph-canvas]: [Plan 03-01]: bare 'fitView' JSX prop on ReactFlow races with onlyRenderVisibleElements (Pitfall 9) — always use defaultViewport + useEffect(setTimeout(fitView, 100)) pattern when virtualization is on. Documented inline in GraphCanvasInner.tsx
- [Phase 03-graph-canvas]: [Plan 03-01]: GraphPlaceholder drives AsyncState via useGraphStore.getState() in async flows (not the hook) — the canvas subscribes to s.nodes directly, so the placeholder component doesn't re-render on every node refresh
- [Phase 03-graph-canvas]: [Plan 03-01]: ReactFlowProvider currently wraps only GraphCanvas; Plan 03-03 Task 1 will promote it to AppShell so global Cmd+K can call useReactFlow(). Doc comment in GraphCanvas.tsx flags the upcoming move
- [Phase 03-graph-canvas]: [Plan 03-02]: DATA-05 ghost.parent_uuid MUST be nf.flow_uuid (additional flow's L1 anchor), NEVER n.parent_uuid — the latter collapses the multi-flow visual story into a same-parent duplicate. Runtime sanity check in rebuild_ghost_refs prints a warning if regressed
- [Phase 03-graph-canvas]: [Plan 03-02]: Primary flow selection uses lex-min MIN(flow_uuid) — deterministic, no schema change, stable across re-scans (flow_uuids are content-hashed). Avoided adding an is_primary BOOL to node_flows
- [Phase 03-graph-canvas]: [Plan 03-02]: rebuild_ghost_refs idempotency comes from the BEGIN/DELETE WHERE is_canonical=0/INSERT/COMMIT transaction; deterministic ghost UUID 'ghost-{canonical}-{flow}' is defence-in-depth so re-runs collapse cleanly
- [Phase 03-graph-canvas]: [Plan 03-02]: hydrate_node_rows extracted as pub fn in commands/nodes.rs so get_lens_nodes reuses the 14-column decode; any future SELECT returning the ContractNode tuple must project the full column set
- [Phase 03-graph-canvas]: [Plan 03-02]: parentUuidStack[0] is always L0 once anything is pushed — getCurrentFlowUuid(stack, nodes) walks the stack looking for level === 'L1'. Passing stack[0] to get_lens_nodes blanks the journey lens
- [Phase 03-graph-canvas]: [Plan 03-02]: Race-safe async store actions pattern — monotonic integer counter (fetchGeneration) captured before await, checked before set(). Cheaper than AbortController for Tauri invoke() (no cancellation API). Last-click-wins semantics across all mixed actions (lens toggle + drill + refresh)
- [Phase 03-graph-canvas]: [Plan 03-02]: Hierarchical layout via parentId + extent:'parent' sub-flows (NOT nested ReactFlow instances which break inner zoom); MUST sort parents-before-children by LEVEL_ORDER before handing to ReactFlow or it logs 'Couldn't find node' warnings and renders children at root (Pitfall 3)
- [Phase 03-graph-canvas]: [Plan 03-02]: rebuildGhostRefs called in three places (pickAndOpenRepo, watcher onRefreshed, graphStore.refreshNodes) — defence-in-depth; transaction makes extras harmless. O(corpus) per call, sub-ms at hackathon scale
- [Phase 03-graph-canvas]: [Plan 03-03]: ReactFlowProvider lives in AppShell (not GraphCanvas) so global Cmd+K palette can call useReactFlow() without (a) nested-provider desync or (b) try/catch around hook (undefined behavior — React hook errors aren't catchable that way). Single atomic promotion commit so no intermediate state where palette mounts without provider in scope.
- [Phase 03-graph-canvas]: [Plan 03-03]: cmdk 1.1.1 React 19 override in package.json overrides field (persistent across fresh npm installs) — NOT `--legacy-peer-deps` (one-off, doesn't survive checkout on fresh clone).
- [Phase 03-graph-canvas]: [Plan 03-03]: Cmd+K keydown listener calls preventDefault BEFORE setOpen to consume WebKit's "Search in page" binding (Pitfall 7). Document-level listener so it fires regardless of focus.
- [Phase 03-graph-canvas]: [Plan 03-03]: Drop `extent: 'parent'` entirely — clamping was the 500-node stack-collapse bug. Dagre's bottom-up sizing ensures parents are always ≥ children's bbox + padding, so children never need clamping. If a future change undersizes a parent, children render outside visibly (preferred failure mode vs. silent stacking).
- [Phase 03-graph-canvas]: [Plan 03-03]: Two-pass dagre layout (bottom-up bbox sizing → top-down LR root distribution) + grid fallback above 12-child threshold. Dagre without edge input puts all children at rank 0 (single-row 22,000px strip at 100 children × 220px) — grid fallback at `sqrt-cols` keeps 100-child groups orderly.
- [Phase 03-graph-canvas]: [Plan 03-03]: `group` is a separate node type from `contract` — leaves are fixed 180×56, containers are dagre-sized with absolute-positioned headers. Splitting keeps memo invalidation boundaries clean (container resize doesn't invalidate leaf render and vice versa).
- [Phase 03-graph-canvas]: [Plan 03-03]: Drive-by dagre fix landed inside Plan 03-03 envelope (not a new plan) because (a) files-modified surface was already scoped to `contract-ide/src/components/graph/`, (b) Task 2 checkpoint hadn't been closed, and (c) GRAPH-03 perf criterion was unreachable without it. RESEARCH §Don't Hand-Roll had flagged auto-layout as deferrable; 500-node UAT lifted that assumption.
- [Phase 03-graph-canvas]: [Plan 03-03]: Graph visual design polish (typography, group headers, edge styling, palette) deferred to Phase 9 demo polish per user decision at UAT checkpoint — explicitly NOT a Phase 3 gap. Phase 3 ships functional graph with correct dagre layout; polish is demo-day concern.
- [Phase 06-contract-derivation]: [Plan 06-01]: reqwest 0.12 with default-features=false + rustls-tls required; native-tls default conflicts with Tauri's TLS stack on macOS. Verified: cargo tree | grep openssl = empty.
- [Phase 06-contract-derivation]: [Plan 06-01]: derive_contracts uses tauri::async_runtime::spawn (not tokio::spawn, not spawn_blocking) — Tauri owns the runtime; upsert is async sqlx so spawn_blocking would deadlock.
- [Phase 06-contract-derivation]: [Plan 06-01]: Single reqwest::Client per derive_contracts invocation, shared across the uuid batch — per-call clients are the anti-pattern (Pitfall in RESEARCH.md).
- [Phase 06-contract-derivation]: [Plan 06-01]: Guard order in derive_one: human_pinned (DERIVE-03) → hash-skip (DERIVE-02) → LLM call (DERIVE-01) — pinned guard must precede hash computation to avoid touching anything when user has locked the contract.
- [Phase 06-contract-derivation]: [Plan 06-01]: compute_code_hash(empty_ranges) → None; caller emits `skipped` (not `error`) for conceptual L0/L1 nodes with no file backing.
- [Phase 04-inspector-monaco]: [Plan 04-01]: `repoPath` lives on `useGraphStore`, not fetched per-tab via `getRepoPath()` IPC — avoids N-way duplication across Inspector / CodeTab / PreviewTab and races with scan events. Populated in `pickAndOpenRepo` + `openRepo` BEFORE `invoke('open_repo')` so subscribers see the path even if the scan fails.
- [Phase 04-inspector-monaco]: [Plan 04-01]: Monaco `setHiddenAreas` + `createDecorationsCollection` MUST be applied in a `useEffect` gated on `[monaco, content]`, not inside `onMount` — `useMonaco()` returns null on first render in `@monaco-editor/react` v4+; calling `new monaco.Range(...)` before the global namespace is ready throws.
- [Phase 04-inspector-monaco]: [Plan 04-01]: `setHiddenAreas` is a RUNTIME method on `IStandaloneCodeEditor` but is absent from the public typings (lives on internal `CodeEditorWidget`). Cast through a named `EditorWithHiddenAreas & { setHiddenAreas: (ranges: IRange[]) => void }` interface rather than `as any` so the typing gap is documented in one place.
- [Phase 04-inspector-monaco]: [Plan 04-01]: Expand-handle click callbacks MUST read from `hiddenAreasRef.current` (a ref), NOT a captured const — a snapshot goes stale after the first click and the second click re-adds the region the first one just expanded.
- [Phase 04-inspector-monaco]: [Plan 04-01]: Cmd+R / Cmd+O keyboard shortcuts live at the Inspector level, not CodeTab — tab switches unmount/remount CodeTab and would thrash the listener. preventDefault MUST fire BEFORE invoke on Cmd+R or WebKit reloads the whole app (same pattern as Plan 03-03 Cmd+K).
- [Phase 04-inspector-monaco]: [Plan 04-01]: `read_file_content(repo_path, rel_path)` takes both args SEPARATELY — the Rust command canonicalizes both and asserts `target.starts_with(repo)` so symlink/`..` escapes return `Err`. Frontend must never pre-join into an absolute path; that bypasses the containment guard.
- [Phase 04-inspector-monaco]: [Plan 04-01]: `monaco-range.css` MUST be `@import`-ed BEFORE `@import "tailwindcss";` in `index.css` — `@tailwindcss/vite` silently drops any `@import` statement that appears AFTER the tailwindcss directive. The `.monaco-context-dim` class is applied via Monaco line decorations injected at the DOM level, so the CSS must live in a global (non-module) stylesheet.
- [Phase 04-inspector-monaco]: [Plan 04-01]: Button-based tab strip pattern is the project convention — shadcn Tabs is NOT installed and MUST NOT be retrofitted. The existing `TABS.map((tab) => <button onClick={() => setActiveTab(tab)}>...)` pattern in Inspector.tsx matches every other tab surface in the app and keeps bundle weight down.
- [Phase 04-inspector-monaco]: [Plan 04-01]: Drive-by Rule-3 fix — Phase 6 pivot commit 71029c6 deleted `derive_contracts` fn but left `commands::derive::derive_contracts` in `lib.rs`'s `generate_handler!`. Required to unblock `cargo check` for plan 04-01 Task 1 verification; not a scope expansion, just catches a leftover from the pivot.
- [Phase 04-inspector-monaco]: [Plan 04-01]: `open_in_editor` $EDITOR dispatch table handles VS Code family (`--goto path:line`), Vim family (`+{line} path`), Sublime/Zed/Mate/Atom (`path:line`), Emacs family (`+line path`), Helix (`path:line`). Unknown editors fall back to `opener.open_path` (no line number, but file opens). Never panics — any failure is `Err(String)` so the frontend can toast.
- [Phase 04-inspector-monaco]: [Plan 04-02]: saveContract merge-read pattern is load-bearing — readContractFrontmatter before every write_contract preserves server-derived neighbors / format_version / derived_at; hardcoded [] would wipe outgoing edges on every human-pinned save because upsert_node_pub DELETEs edges before re-inserting from fm.neighbors
- [Phase 04-inspector-monaco]: [Plan 04-02]: Hash contract bodies via Rust hash_text (sha2 crate) not JS crypto.subtle — derivation pipeline and inspector MUST agree byte-for-byte on Unicode normalization. Single Rust entry point eliminates NFC/NFD divergence risk
- [Phase 04-inspector-monaco]: [Plan 04-02]: zundo temporal.clear() lives in loadNode (not setContractText) — Cmd+Z stays per-node without every keystroke wiping undo. loadNode(null) is a valid deselection call that correctly clears to empty buffer without stacking history
- [Phase 04-inspector-monaco]: [Plan 04-02]: Global Cmd+S in useKeyboardShortcuts reads selectedNode + repoPath inline via getState() — kept global shortcut alive (option A) rather than disabling in favor of ContractTab-local listener. Both global and local Cmd+S coexist; saves are idempotent so double-fire is harmless. Local listener exists so Cmd+S pre-empts 400ms debounce timer
- [Phase 04-inspector-monaco]: [Plan 04-02]: DriftBadge reads contract_hash vs code_hash as the source of truth — NOT human_pinned flag. Pinned is a write-guard for Phase 6; drift is a read-only visual. Badge rendered at ml-auto right of Inspector header so all four tabs see status
- [Phase 04-inspector-monaco]: [Plan 04-03]: Rust reqwest probe (not frontend fetch) is the CORS-safe path — tauri://localhost to http://localhost:3000 is cross-origin; probeRoute IPC bypasses browser origin entirely (Pitfall 6)
- [Phase 04-inspector-monaco]: [Plan 04-03]: reqwest 0.12 with rustls-tls + json + default-features=false — load-bearing even when dev build succeeds without it; native-tls defaults would conflict with Tauri TLS on clean CI/fresh-clone (Pitfall 5)
- [Phase 04-inspector-monaco]: [Plan 04-03]: probe_route returns bool (not Result<bool, String>) — UX question is binary (iframe vs prompt); failure modes collapse to 'unreachable' uniformly. 1s timeout; status < 500 counts as reachable (404 still means dev server up)
- [Phase 04-inspector-monaco]: [Plan 04-03]: CSP frame-src narrowed to http://localhost:* + http://127.0.0.1:* (both — CSP host-match is literal; Next.js binds to one or the other). Phase 9 polish may widen for remote preview targets
- [Phase 04-inspector-monaco]: [Plan 04-03]: PreviewTab iframe sandbox='allow-scripts allow-same-origin allow-forms' — minimum set for Next.js dev (hot reload + same-origin XHR + forms). Dropping allow-same-origin blanks the render. iframe key={probeCount} forces unmount/remount on Reload
- [Phase 04-inspector-monaco]: [Plan 04-03]: reqwest was ABSENT from Cargo.toml at plan start (plan assumed it present at line 37) — followed plan's Step 1 fallback: added canonical form. No duplicate-key risk; cargo check clean. Plan-assumption drift, not a deviation rule
- [Phase 07-drift-detection-watcher-path]: [Plan 07-01]: tokio added as explicit direct dep (features = ["sync"]) — was transitive only via tauri/reqwest/sqlx; tokio::sync::Mutex requires explicit dep for feature flag activation
- [Phase 07-drift-detection-watcher-path]: [Plan 07-01]: Pure-logic should_skip_write test chosen over in-memory sqlite integration test — extracted helper makes NOT NULL constraint explicit; ON CONFLICT semantics validated by db module tests; keeps task budget tight
- [Phase 07-drift-detection-watcher-path]: [Plan 07-01]: #[allow(unreachable_patterns)] on DbPool wildcard arm in engine.rs — matches established pattern in db/scanner.rs and commands/nodes.rs; only sqlite feature compiled
- [Phase 07-drift-detection-watcher-path]: [Plan 07-02]: refresh_nodes drops DbInstances read lock in inner block before calling refresh_source_watcher_from_db — both functions acquire the same RwLock; scoping prevents deadlock; clippy -D warnings requires std::sync::MutexGuard not held across .await
- [Phase 07-drift-detection-watcher-path]: [Plan 07-02]: Option A for refresh_nodes repo path — fetch RepoState via app.state::<RepoState>() inline with std::sync::MutexGuard scoped before .await; no signature change needed
- [Phase 07-drift-detection-watcher-path]: [Plan 07-02]: Delete-event TODO punted to 07-04 UAT — DRIFT-01 core case (edit file → drift pulse) doesn't depend on delete handling; distinguishing sidecar-delete vs source-delete requires more surface
- [Phase 07-drift-detection-watcher-path]: [Plan 07-02]: acknowledge_drift payload { uuid, drifted: false, currentCodeHash: null, baselineCodeHash: null } matches DriftChanged camelCase shape — React uses one drift:changed handler for watcher and acknowledge events
- [Phase 07-drift-detection-watcher-path]: [Plan 07-03]: buildFlowNodes signature changed to accept driftedUuids: Set<string> as second param — keeps function pure and useMemo deps explicit; no stale closure
- [Phase 07-drift-detection-watcher-path]: [Plan 07-03]: drift overrides ALL health states — driftedUuids.has(uuid) ? 'drifted' : 'healthy'; Phase 3 had 'healthy' as const hardcoded with TODO; no untested state to preserve
- [Phase 07-drift-detection-watcher-path]: [Plan 07-03]: useDriftStore.getState().reset() wired in BOTH pickAndOpenRepo AND openRepo — missing either leaves stale red pulse from previous repo
- [Phase 07-drift-detection-watcher-path]: [Plan 07-04]: LOAD-BEARING — per-range scoping is correct, not a watcher bug. Drift hash is computed over the node's code_ranges slice ONLY. Edits outside the watched range produce NO drift (hash-of-slice behavior confirmed via SC1 UAT: EOF append to write_derived_contract.ts outside range 31-98 produced no drift; in-range edit at line 50 produced drift_state row within ~3s). Any future "drift didn't fire" report must FIRST check whether the edit falls inside the watched range before diagnosing a watcher issue.
- [Phase 07-drift-detection-watcher-path]: [Plan 07-04]: Phase 4 DriftBadge showHint placeholder ("Reconcile flow ships in Phase 7") fully removed — replaced by live onReconcile callback; Inspector holds reconcileOpen state + auto-closes on selectedNode change
- [Phase 07-drift-detection-watcher-path]: [Plan 07-04]: ReconcilePanel Rewrite prompt includes post-rewrite write_derived_contract call (RESEARCH Open Question 1 single-combined-prompt) — prevents immediate re-drift on post-rewrite watcher tick (Pitfall 6 mitigation)
- [Phase 08-01]: collect_h2_bounds single-pass: captures (name, heading_line_start, body_start) in one offset_iter walk; body_end = heading_line_start of next section
- [Phase 08-01]: section-parser-cli is a standalone Cargo project (not path dep on main crate) because main crate has cdylib/staticlib types incompatible with bin path deps; binary at binaries/section-parser-cli-aarch64-apple-darwin; MCP sidecar spawns via SECTION_PARSER_CLI_PATH env var + execFileSync
- [Phase 08-01]: receipts table already existed from v1 migration (CREATE_core_tables); v3 uses ALTER TABLE ADD COLUMN only; receipt_nodes join table and idx_receipts_node_uuid not touched by v3
- [Phase 08-05]: Targeted ring glow hue: ring-teal-400/70 with 2s slow pulse — not orange (Phase 13), not amber (08-02 rollup), reads as focus indicator
- [Phase 08-05]: apply_cherrypick_inner: testable inner fn pattern used — same approach as other audit-friendly Rust commands; DriftLocks arc binding fix (arc = locks.for_uuid(); guard = arc.lock().await) required for E0716
- [Phase 08]: CLI flag set: claude -p --output-format stream-json --verbose (not --include-partial-messages); session_id exposed at .session_id in stream events; snapshot-diff fallback also implemented
- [Phase 08]: encode_cwd: /Users/yang/lahacks/contract-ide maps to -Users-yang-lahacks-contract-ide; all / replaced with -
- [Phase 08]: Cost rates 2026-04: opus-4-7 (15/75 per 1M), sonnet-4-5 (3/15), haiku-4 (1/5); default to opus-4-7 if model not matched
- [Phase 08-02]: compute_rollup_and_emit is a SIBLING of compute_and_emit — acquires same DriftLocks per-UUID Tokio mutex (body writes and rollup writes serialize per-UUID); Phase 7 code untouched (PROPAGATION.md no-retroactive-changes)
- [Phase 08-02]: SourceWatcher.refresh() takes parent_map: HashMap<String,String> as 4th arg; uuid_to_parent snapshot in closure enables ancestor walk without per-event DB queries
- [Phase 08-02]: stored_rollup_hash on first-pass: rollup_derived checked first (prior run), fallback to nodes.rollup_hash (reconcile-committed value), NULL → stale immediately
- [Phase 08-02]: CVA amber: ring-2 ring-amber-400 (non-pulsing); gray: ring-2 ring-slate-400 opacity-80 (non-pulsing); compoundVariants suppress targeted teal when rollupState stale/untracked; drift red pulse visually dominates amber for combined-state nodes
- [Phase 08-02]: SECTION_PARSER_CLI_PATH injected in launch_mcp_sidecar for MCP TypeScript sidecar execFileSync
- [Phase 08-03]: Hook subshell FD redirection `() </dev/null >/dev/null 2>&1 & disown` is load-bearing — without full subshell-level redirection, backgrounded `claude -p` subprocesses inherit the parent's stdout/stderr pipes and Rust callers using `wait_with_output()` block until the children exit (caught via timing test running mock-claude with sleep 5). Just `claude -p > /dev/null` is NOT sufficient.
- [Phase 08-03]: Mock-claude shim must live in a caller-owned tempdir held alive past `run_hook` return, otherwise the disowned subshell's exec races against TempDir::drop deleting the shim. Lifecycle parallel to Phase 7 race-safe async store actions.
- [Phase 08-03]: `tac` (Linux) vs `tail -r` (macOS) detected at runtime in the hook; macOS dev box ships `tail -r` by default, `tac` only via brew. Both reverse line order for tail-of-transcript intent extraction (avoids O(N) full-file reads at multi-MB session sizes).
- [Phase 08-03]: DB path discovery is `CONTRACT_IDE_DB_PATH` env override → `$HOME/Library/Application Support/com.contract-ide.app/contract-ide.db` fallback. Hook tolerates missing DB (returns affected_uuids: []) so first-launch agent runs don't crash before app boots.
- [Phase 08-03]: Hook is fire-and-forget at agent-flow level — `set -u` + `trap 'exit 0' ERR` envelope guarantees exit 0 on every path so a hook bug NEVER breaks the user's agent flow mid-demo. Risky calls wrapped with `|| true`.
- [Phase 08-03]: Pitfall 2 reframed — "single-writer" means mutex coordination via DriftLocks, NOT exclusive writer existence. Hook is a *client* of the existing update_contract MCP tool which is itself a client of write_contract; both writers (hook→claude-p AND active session direct) coexist. No new write paths added.
- [Phase 08-04b]: Receipt snake_case confirmed: list_receipts_for_node + parse_and_persist use explicit snake_case keys in serde_json::json! macros; no rename_all; TS Receipt interface uses matching snake_case — zero alias mapping needed
- [Phase 08-04b]: W4 closed: ChatPanel uses useGraphStore(s => s.selectedNodeUuid) — no currentZoomedNodeUuid field exists; selectedNodeUuid IS the zoomed node set by graph node click and Cmd+K
- [Phase 08-04b]: AppShell subscription pattern: agent+receipt subscriptions block mounts ONCE at boot (useEffect [], sibling to drift+rollup blocks); unlistenStream/Complete/Receipt cleaned up on unmount; survives tab switches
- [Phase 08-04b]: DeltaBanner three-row Phase 13 reservation: rulesHonored='N/A' in Phase 8; row renders at 28px+ with muted explanatory text; reserved height prevents layout shift when Phase 13 substrate verifier wires real value
- [Phase 08-06]: accept_rollup_as_is uses three independent multiline regexes for in-place YAML edit (rollup_hash, rollup_generation, rollup_state). Never calls serde_yaml_ng round-trip (YAML round-trip not byte-preserving — engineering red team flag). Body bytes confirmed byte-identical pre/post by test.
- [Phase 08-06]: rollup_generation added to ContractNode (Rust + TS) with COALESCE(rollup_generation, 0) in all SELECT queries. Enables ReconcilePanel to pass expected_generation without extra IPC round-trip.
- [Phase 08-06]: Staleness annotation v1 phrasing "cited children may have diverged" (W7) — not "N dependent children changed" — because no per-generation snapshot table exists yet. Phase 9 mass-edit ranking keys on this header format.
- [Phase 08-06]: Gate 5 cascade mechanic: cascade requires Edit manually at each level (not Accept as-is). Accept as-is only updates rollup_hash/generation/state — does NOT recompute section_hashes from body edit — so upstream rollup_hash doesn't change and the parent never sees a stale child.
- [Phase 08-06]: Phase 7 ReconcilePanel drift branch byte-identical to Phase 7 shipped version. Rollup-stale amber paths are sibling renders (PROPAGATION.md no-retroactive-changes mandate).
- [Phase 08 UAT close-out]: upsert_node_pub MUST round-trip section_hashes_json + rollup_inputs_json + rollup_hash + rollup_state + rollup_generation. The pre-Phase-8 helper only knew about v2 columns; migration v3 added the columns but no upsert path populated them. Net effect: every cascade probe loaded rollup_inputs as NULL and the engine marked nodes "untracked" forever. Fix is in db/scanner.rs upsert_node_pub.
- [Phase 08 UAT close-out]: write_contract MUST do a server-side merge-read of Phase 8 fields (rollup_inputs / rollup_hash / rollup_state / rollup_generation / parent) from the existing sidecar before upsert. The frontend's saveContract path constructs ContractFrontmatter from the React ContractNode without those fields (engine-owned, not user-editable), so a naive write would clobber engine state on every autosave. Section_hashes ARE recomputed each save (correct — they reflect body content).
- [Phase 08 UAT close-out]: write_contract MUST compute section_hashes from the new body via section_parser before serialization. Without this the .md file goes back to disk with empty section_hashes despite format_version: 3, breaking the cascade reader's `child.section_hashes_json[section_name]` lookup.
- [Phase 08 UAT close-out]: write_contract MUST cascade compute_rollup_and_emit up the parent chain after upsert (SourceWatcher only fires the cascade on SOURCE-file edits, not .contracts/*.md edits). Walk parent_uuid in SQLite, spawn one async task per ancestor (skip L0).
- [Phase 08 UAT close-out]: Tauri command-level args (top-level invoke params) auto-rename camelCase → snake_case ONLY with `#[tauri::command(rename_all = "camelCase")]`. Nested struct fields (e.g. FilePatch.new_content) need their own `#[serde(rename_all = "camelCase")]`. Caught at UAT — cherrypick Approve threw "missing field new_content" because FilePatch struct was bare. Same class fixed in apply_cherrypick + accept_rollup_as_is + draft_propagation_diff + read_children_section_diffs.
- [Phase 08 UAT close-out]: SQLite cache MUST be wiped on open_repo before re-populating (DELETE FROM nodes/edges/node_flows/drift_state/rollup_derived/receipt_nodes). Pre-fix, opening a different repo layered the new repo's contracts on top of the previous one's because the cache was ambient (not repo-scoped). Receipts intentionally preserved (session-scoped, not repo-scoped). Caught at UAT: opening /tmp/uat-repo showed CheckoutButton/PrimaryButton/SpinnerIcon nodes from a prior repo.
- [Phase 08 UAT close-out]: run_agent MUST set `.current_dir(repo_path)` on the spawned `claude` CLI; without this the agent inherits the Tauri host's cwd (contract-ide source root in dev), which (a) makes Read/Write tool paths land in the wrong tree and (b) caused successful agent edits to trigger Tauri's own auto-rebuild because they were in src-tauri/src/ — visible as "the app crashed and relaunched mid-stream" during Gate 2.
- [Phase 08 UAT close-out]: claude CLI invocations from agent paths (run_agent + PostToolUse hook Pass-2 spawn) MUST include `--dangerously-skip-permissions`. Non-interactive subprocesses have no stdin to answer permission prompts so each Read/Write tool call would hang the stream. The chat panel IS the consent surface (CLAUDE.md demo bar).
- [Phase 08 UAT close-out]: AppShell event subscription effects MUST guard against StrictMode mount → cleanup → mount race with a `cancelled` flag — bare `then(u => unsub = u)` leaks the first listener when cleanup runs before the await resolves, producing duplicate event handling. Fixed for drift, rollup, agent stream, agent complete, receipt subscriptions.
- [Phase 08 UAT close-out]: editor.saveContract MUST refresh useGraphStore.nodes after writeContract returns. Without it ContractTab reads contract_body from a stale ContractNode snapshot on the next mount and the just-saved body appears reverted.
- [Phase 08 UAT close-out]: Zustand selectors MUST return stable references — `s.current?.streamBuffer ?? []` returns a fresh `[]` every render and trips useSyncExternalStore's "getSnapshot should be cached" warning into Maximum update depth. Use a module-level constant for the fallback. CherrypickModal's object-literal selector had the same bug — split into individual selectors.
- [Phase 08 UAT close-out]: Monaco DiffEditor needs an explicit pixel `height` prop in @monaco-editor/react 4.x — `flex-1` inside a flex column inside a scrollable parent collapses to ~0px. Cherrypick modal showed a thin diff strip pre-fix.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-01]: Migration v4 phase10_sessions_and_episodes appended after Phase 8 v3; pre-flight grep confirmed v1+v2+v3 contiguous before append. v4 was already applied to dev DB at execution time (sqlx_migrations row 4 present, sessions/episodes tables match spec) — no re-launch needed. Immutability rule extended to v4.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-01]: SessionLocks structurally identical to DriftLocks (DashMap<String, Arc<tokio::sync::Mutex<()>>>); only the key changes (session_id vs uuid). Both registered in lib.rs .manage() chain and key into distinct hash spaces — Phase 8 hook (UUID-keyed) and Phase 10 ingest (session-id-keyed) co-exist without collision. Tokio mutex (NOT std) is mandatory: guard held across .await DB queries.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-01]: derive_cwd_key extracted as pure helper (slash→hyphen rule) before any caller exists — single source of truth for the four 10-03 call sites (watcher, list_ingested_sessions, get_backfill_preview, execute_backfill). 5-test suite locks the rule against the four research-validated path shapes including trailing-slash edge case.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-01]: ingestor.rs + watcher.rs land as documented stubs in 10-01 so 10-02 + 10-03 can be authored in parallel without re-touching mod.rs. Stub-then-fill pattern keeps file-modified surface strict per plan. #[allow(dead_code)] annotations are per-symbol with explicit TODO(Plan 10-02) / TODO(Plan 10-03) markers — `rg "TODO\\(Plan 10-0[23]\\)" contract-ide/src-tauri/src/session/` enumerates the cleanup queue.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-01]: Zero new Cargo.toml deps for the entire scaffold — dashmap v6, tokio (sync feature), sha2, notify v8 all already present from Phases 6-8. Verified via cargo tree. Zero Claude API surface (grep -rEn 'reqwest|Anthropic|anthropic|claude\.com' over session/ + db/migrations.rs returns nothing) — Phase 10 makes ZERO LLM calls; Phase 11 distiller is the consumer.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-02]: sha2 0.11 LowerHex incompatibility — `format!("{:x}", hasher.finalize())` does NOT compile (returns Array<u8, _> which doesn't impl LowerHex). Project convention from Phase 6 + 4 other call sites: `hex::encode(hasher.finalize())`. Hex crate already in Cargo.toml. Auto-fixed Rule 3 (blocking) inline, documented in source comment. Pattern locked: any new sha256 hash call site MUST use hex::encode, never format!("{:x}").
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-02]: sqlx::SqlitePool clone-and-drop-RwLock pattern: clone the SqlitePool out of the DbInstances RwLock guard BEFORE dropping the guard (`let pool = match db { DbPool::Sqlite(p) => p.clone(), ... }; drop(map);`). Pool is internally Arc so clone is cheap. Required when async DB work follows the clone — borrow from RwLock guard becomes invalid when the guard drops. Critical for notify-storm scenarios (Pitfall 2).
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-02]: Filter regression strategy = content-preservation assertion (NOT snapshot-from-fixture). Re-derive expected user/assistant text from raw JSONL using same filter rules; assert source ⊆ filter_session_lines output. Tests skip gracefully if user-local fixtures absent. Observed: 5f44 627KB → 9.6KB (94%); efadfcc4 1.3MB → 24KB (98%). Both well under 50KB SC2 target. Strategy can't catch a wrong rule but WILL catch a regression — Phase 11 distiller is the consumer-level validation surface.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-02]: 5f44 user-turn count observed = 2 (not 4 as kernel-experiment notes suggested). Filter correctly excluded 2 isMeta:true caveats or `<` preamble strings. This is the filter doing its job exactly per spec, not a regression. The content-preservation test re-derives the expected set from the same rules so the count automatically matches.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-02]: idempotency primitives proven by tests. `INSERT OR IGNORE INTO episodes` on episode_id PK skips duplicates (rows_affected=0 on every re-ingest insert). `ON CONFLICT(session_id) DO UPDATE` on sessions preserves started_at + takes `MAX(sessions.last_line_index, excluded.last_line_index)` for last_line_index. Schema-level guarantees verified directly via in-memory sqlite (tests/session_idempotency_tests.rs) — no Tauri AppHandle required.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-02]: Parallel-execution coordination event — 10-03's `git add` swept up my staged Task 2 test files into commit 7c78ec0. Files correct + tests passing on trunk; commit attribution caveat documented in 10-02-SUMMARY.md Issues Encountered. Future-proofing: per-task commits should run `git add <specific-files>` IMMEDIATELY before `git commit`, not interleave any other operations between stage + commit.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-03]: SessionWatcher mirrors Phase 7 SourceWatcher structurally — same notify::RecommendedWatcher / std::sync::Mutex / managed-state-registration shape. Key difference: RecursiveMode::NonRecursive directory-level watch (not individual files) — Claude Code creates new session UUIDs dynamically per /clear and per `claude` invocation, so directory-level watching is required. Pitfall 4 graceful deferral: missing ~/.claude/projects/<cwd-key>/ silently no-ops with eprintln warning rather than failing open_repo (Phase 7-style degraded posture).
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-03]: MCP tool uses bun:sqlite NOT better-sqlite3. Plan template referenced better-sqlite3 but actual sidecar (Phase 5 Plan 05-01) uses bun:sqlite via the Bun runtime (built-in, no native addon). Adapted as Rule 3 blocking-issue deviation — silent dependency mismatch would have failed compilation. Confirmed via tools/list JSON-RPC probe that all 8 tools register cleanly. Mirror of tools/list_drifted_nodes.ts import shape.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-03]: session:status null-placeholder pattern after batch backfill. execute_backfill emits {watchingSessions: null, episodesIngested: null} after batch completes, signaling UI to refetch via get_session_status. This avoids racing the per-ingest emits the watcher already sent during the batch (each ingest_session_file call in the batch causes the watcher's spawn closure to fire its own emit_session_status from the file-modify event; the batch emit lets the UI consolidate).
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-03]: Backfill preview is char/4 × Sonnet rate arithmetic. Constants ESTIMATED_INPUT_RATE_PER_MTOK_USD=3.0 + ESTIMATED_TOKENS_PER_CHAR=0.25 front-and-center in commands/session.rs. 4 unit tests assert the math. ZERO Claude API calls in Phase 10 (verified by grep over session/ + commands/session.rs). Phase 11 distiller will report actual tokens on its receipt.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-03]: SessionRow decode via query()→SqliteRow→try_get pattern (NOT query_as with #[derive(sqlx::FromRow)]) — sqlx 0.8 in this crate is configured WITHOUT the `derive` feature (verified: Cargo.toml shows only `sqlite, runtime-tokio-native-tls`). Mirror of commands::nodes::hydrate_node_rows. Pattern locked: any new SqliteRow→struct decode in this crate must use try_get, never query_as<_, MyStruct> with derive.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-03]: MCP tool defensive sqlite_master probe pattern: SELECT name FROM sqlite_master WHERE type='table' AND name='X' before SELECT FROM X — handles partial-migration / wrong-DB-path gracefully with a helpful message rather than throwing SqliteError. Established here for new-table tools; existing 4 tools assume tables present (Phase 1 v1 migration baseline).
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-04]: Gap-closure FK constraint fix (commit d6f3444) — `ingest_session_file` for non-empty turns inserted episodes BEFORE the sessions row, tripping SQLITE_CONSTRAINT_FOREIGNKEY (787) on `episodes.session_id REFERENCES sessions(session_id)`. Fix: promoted `ensure_session_row` (INSERT OR IGNORE — idempotent) to BEFORE the episode INSERT loop. Bug invisible to integration tests because `session_idempotency_tests` pre-seed the sessions row in `setup_pool`. Caught only during live UAT Step 1 with a brand-new JSONL. Pattern locked: any future ingestion path with FK references must INSERT OR IGNORE the parent BEFORE the child loop, even when "tests pass".
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-04]: SC4 opt-in is structurally enforced, not procedural. `executeBackfill` is wrapped in `confirmExecute()` whose ONLY call site is the "Confirm & Ingest" button onClick. No other call site exists. A regression would require adding a new call site, which is grep-discoverable (`grep -rn 'executeBackfill' contract-ide/src/`). Pattern locked: any future opt-in IPC (e.g., Phase 11 `execute_distiller`) MUST follow the same structural pattern — wrap the call in a single-source-of-truth handler, not allow ambient call sites.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-04]: Native HTML checkboxes used in BackfillModal selection table — kept new-component-install surface to zero. Pattern matches existing project conventions (Inspector tab strip uses native buttons, not shadcn Tabs). shadcn Dialog was already installed for Phase 7 ReconcilePanel; reused without new deps.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-04]: SessionStatusIndicator race-resistance pattern (seed-from-IPC + subscribe-to-event) is structural — both paths required. Removing either leaves a race window. Same lesson Plan 05-01 documented for McpStatusIndicator. Phase 11 distiller status indicator MUST follow this pattern.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-04]: Phase 10 confirmed ZERO Claude API calls TWO ways. (1) Static: grep -rEn over `session/` + `commands/session.rs` returns only doc-comment SAFETY assertions. (2) Live: user `lsof` against app + MCP sidecar processes returned EMPTY for Anthropic API endpoints during UAT Step 4. Phase 11 distiller is the LLM consumer — it WILL show up in lsof.
- [Phase 10-session-watcher-filter-pipeline]: [Plan 10-04]: Filter actuals confirmed against kernel-experiment fixtures. 5f44f5af: 642KB → 9824 chars (94% reduction); efadfcc4: 1332KB → 24038 chars (98% reduction). Both well under 50KB SC2 ceiling. RESEARCH.md projected ~12KB / ~27KB; actuals are smaller.
- [Phase 09]: shadcn Textarea + Label installed in 09-03 (npx shadcn@latest add textarea label) — first time these components added to the project
- [Phase 09]: DISPLAY-ONLY TS section parsers in contract-sections.ts carry mandatory header comment forbidding section_hashes computation; Rust section_parser.rs is sole authority for section_hashes
- [Phase 09]: DelegateToAgentButton Phase 11 stub prop seam: onDelegate?: (body, uuid) => void; Phase 11 wires handler without modifying Phase 9 component
- [Phase 09]: [Plan 09-04b]: BABEL-01 spike PASSED with Route A (custom webpack loader, NOT babel-loader, NOT SWC plugin). Plugin in contract-ide-demo/contract-uuid-plugin/index.js uses @babel/parser to inject data-contract-uuid on JSX elements identified by L4 UI atom code_ranges. Phase 13 CHIP-01 dependency confirmed: DOM attribute path; bounding-rect chip-overlay fallback shelved.
- [Phase 09]: [Plan 09-04b]: Next.js 16 defaults to Turbopack which does not support custom module.rules — pnpm scripts use --webpack flag. Pattern locked for the demo repo: any future custom webpack loader needs the --webpack flag in package.json.
- [Phase 09]: [Plan 09-04b]: JSX-01 validator uses structural bracket-counting (NOT swc_ecma_parser AST) to avoid ~5min compile-time regression in the Tauri build graph. Sufficient for the single-element invariant; full AST upgrade only if production edge cases require it.
- [Phase 09]: [Plan 09-04b]: BACKEND-FM-01 validator reuses Phase 8 PROP-01's section_parser::parse_sections — single source of truth. flow-kind contracts EXEMPT alongside UI; backend kinds = API / lib / data / external / job / cron / event.
- [Phase 09]: [Plan 09-04b]: Validator wiring deviation — spec asked for "non-toast persistent banner"; reused existing GraphPlaceholder error display by piggybacking on ScanResult.errors with [JSX-01] / [BACKEND-FM-01] / [VALIDATORS] prefixes. src/lib/repo-load.ts categorizeRepoLoadErrors() helper is the seam if a dedicated banner component is later added (Phase 9 polish backlog).
- [Phase 09]: [Plan 09-04b]: Two stream-idle timeouts on the executor agent — orchestrator finished Tasks 2-4 (mod registration, backend validator, repo wiring, TS lib, SUMMARY, state updates) directly. All success criteria met; commit history clean atomic per-task.
- [Phase 09]: GivenWhenThenEditor dep array excludes contractBody to prevent setContractText loop; eslint-disable-next-line documents intentional omission
- [Phase 09]: Routing: Option A Rust IPC find_by_intent_mass Tauri command — MCP sidecar is stdio-only, not reachable from React
- [Phase 09]: section-parser-cli emits only section_hashes (no text) — simpleH2Split always used for text in TS re-ranker
- [Phase 09]: bun:test chosen for mcp-sidecar tests (vitest not installed; Bun runtime throughout)
- [Phase 09-mass-edit-non-coder-mode-demo-repo-seeding]: Prisma 7.8.0 datasource URL moved to prisma.config.ts; adapter uses PrismaBetterSqlite3({url}) not pre-created Database instance
- [Phase 09]: Option A bare-Claude protection: demo repo NEVER contains CLAUDE.md at any commit; setup notes in DEMO-SETUP.md
- [Phase 09]: applyMassEdit routes through write_contract (single-writer Rust IPC) with client-side SKIPPED-PINNED detection from frontmatter.human_pinned before write_contract call
- [Phase 09]: MassEditModal serial apply loop (for...of + await per node) avoids FSEvents debounce race and correctly accumulates skipped_pinned/applied/errors counters
- [Phase 11]: Phase 11-01: anchored_uuids ships as TEXT NOT NULL DEFAULT '[]' on substrate_nodes for Phase 11-03 cousin-exclusion JOIN at candidate-selection time (CONTEXT lock)
- [Phase 11]: Phase 11-01: intent_drift_* columns NOT pre-added on substrate_nodes — SQLite lacks ADD COLUMN IF NOT EXISTS, pre-adding breaks Phase 12-01 migration
- [Phase 11]: Phase 11-01: source_session_id and episode_id on substrate_nodes are TEXT only (no FK) to tolerate Phase 10 not having shipped when Phase 11 first runs
- [Phase 09]: FLOW-01: kind:flow + members[] is a permissible contract shape; migration v5 persists members_json; validate_flow_members checks non-empty + all UUIDs exist; demo-base tag advanced to 9f5029b
- [Phase 11]: Cousins excluded at SQL time via json_each(anchored_uuids) JOIN filter — not post-filter — per CONTEXT lock
- [Phase 11]: candidate_selection() exported as pub fn for Phase 9 mass-edit reuse (Open Question 4)
- [Phase 11]: contract_body truncated to 800 chars in LLM rerank prompt (Pitfall 8) to prevent input window blow-up
- [Phase 11]: delegate_execute reuses Phase 8 run_agent (no re-implementation of spawn/streaming/receipt logic)
- [Phase 11]: bare: Option<bool> param on run_agent defaults false; Phase 11 delegate_execute passes Some(true)
- [Phase 11]: SimplifiedInspector DelegateToAgentButton stub replaced with real DelegateButton (store-driven)
- [Phase 09-05]: Token count interpretation: input_tokens=25-32 (fresh prompt), cache_read=518k-863k (full codebase); pre-estimate of ~7,200 was pre-repo-build; delta claim holds at tool_calls level
- [Phase 09-05]: Baseline JSONL field: .message.usage.input_tokens on type=assistant rows; tool_calls counted from .message.content[].type==tool_use
- [Phase 12-01]: Migration v7 (not v5) — v5/v6 already taken by Phase 9/Phase 11; v7 is the next free version per plugin immutability rule
- [Phase 12-01]: ALTER TABLE ADD COLUMN strategy for intent_drift_* fields — Phase 11 v6 already shipped substrate_nodes WITHOUT them; CREATE TABLE IF NOT EXISTS would silently no-op
- [Phase 12-01]: Verdict enum SCREAMING_SNAKE_CASE serde rename matches the SQLite CHECK constraint string verbatim — single canonical string at every boundary
- [Phase 12-01]: Composite edge index suffixed _lookup to avoid collision with Phase 11 v6's idx_substrate_edges_type
- [Phase 12-01]: REQUIREMENTS.md count stays at 70 — SUB-06/07 stubs were already counted; collapsing into canonical entries is net-zero
- [Phase 11]: Side panel uses separate list_substrate_for_atom (FTS5-only, no LLM rerank) vs find_substrate_for_atom (LLM rerank)
- [Phase 12-conflict-supersession-engine]: Plan 12-02: SKIP Migration v8 — Phase 11 v6 already ships substrate_nodes_fts virtual table + AI/AU/AD triggers with exact (uuid UNINDEXED, text, applies_when, scope) shape. Adding redundant migration would risk trigger-name collision (Phase 11 names differ from plan-proposed names). Cleaner to read directly from Phase 11's table.
- [Phase 12-conflict-supersession-engine]: Plan 12-02: Lock ordering for fact_engine — new_uuid first (held for full call) + each stale_uuid in turn during invalidation loop. Cross-engine deadlock-safe because intent_engine (12-03) holds at most one lock at a time. DriftLocks (Phase 7) reused — no parallel mutex map.
- [Phase 12-conflict-supersession-engine]: Plan 12-02: claude -p uses --output-format text (not --json-schema) for invalidation verdict — Graphiti dedupe-edges prompt instructs LLM to output JSON object literal in text response; defensive parser strips markdown fences and treats parse failure as fail-safe (Ok(vec![]) = no contradictions found). Differs from distiller pipeline which uses --json-schema.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: Reverse rollup walker is two-phase — Phase 1 BFS over contract DAG via parent_uuid + rollup_inputs JSON LIKE scan (frontier-bounded by max_depth ≤ 5); Phase 2 substrate-anchor join via derived-from-contract edges with current-truth filter (invalid_at IS NULL) and decision/constraint node_type filter. JSON LIKE pattern `%"child_uuid":"<uuid>"%` matches Phase 8 08-01 emitted shape. De-dup substrate node anchored to multiple contracts.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: Confidence calibration thresholds HARDCODED in intent_engine: ≥0.85+DRIFTED → auto_applied=1; 0.50–0.85 → surfaced for review; <0.50 → filtered noise (substrate_nodes.intent_drift_state stays NULL but intent_drift_verdicts row IS written for full audit). Per evaluation.md tier-3 noise filter.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: REJECT-overlapping-shifts implemented in record_priority_shift_internal — pre-check `SELECT id FROM priority_shifts WHERE applied_at IS NULL LIMIT 1` returns Err if any pending shift exists. RESEARCH.md Q2 decision (REJECT, not queue). Atomic to v1 by definition since one shift fully cascades before another can be recorded.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: Per-decision DriftLocks held one at a time in propagate_intent_drift — no cross-pair locking. Eliminates the deadlock-pair concern fact_engine has to manage (intent_engine never holds two locks simultaneously, so no (a,b)/(b,a) cycle can form across engines).
- [Phase 12-conflict-supersession-engine]: Plan 12-03: Verdict-id placeholder mapping uses chunk-local d{idx+1} positional pairing with substrate_nodes.uuid via the chunk_nodes vec ordering. Missing verdicts (LLM truncation) synthesize NEEDS_HUMAN_REVIEW + 0.0 confidence — row writes to intent_drift_verdicts (audit) but does NOT update substrate_nodes (since 0.0 < 0.50 noise floor). No silent loss; user sees in audit but not in UI.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: substrate:intent_drift_changed event payload {uuid, verdict, confidence, auto_applied, priority_shift_id} established for Phase 13 UI to subscribe and re-render the substrate-state overlay. Complementary to 12-02's substrate:invalidated event.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: uuid crate features in Cargo.toml unchanged — Phase 7 already shipped uuid = { version = "1", features = ["v4"] }. Plan suggested adding "serde" feature; not needed (intent_engine never serializes Uuid directly, only .to_string() at DB boundary). Avoided unnecessary feature bloat.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: Smoke verification via SQL-level test (sqlite3 INSERT + SELECT WHERE applied_at IS NULL) instead of full app launch — gave deterministic guarantee that priority_shifts schema accepts our write shape AND that the RESEARCH.md Q2 reject path triggers correctly. Full IPC smoke deferred to 12-04.
- [Phase 12-conflict-supersession-engine]: Plan 12-03: run_claude_judge duplicated (~10 lines) from fact_engine instead of imported — keeps the two engines decoupled per plan guidance; if claude -p invocation patterns diverge later (timeout, args), each engine evolves independently.
- [Phase 13]: useSubstrateStore extended additively (Phase 11 footer counter slice + Phase 13 nodeStates Map slice coexist)
- [Phase 13]: resolveNodeState exported from contractNodeStyles.ts as load-bearing import for Wave 2 plans 13-04/05/06/07/09 — single precedence definition site (drifted > intent_drifted > rollup_stale > superseded > rollup_untracked > healthy)
- [Phase 13]: Defensive PRAGMA table_info column check in get_substrate_states_for_canvas — app boots even when Phase 12 v7 migration hasn't run; falls back to invalid_at-only state derivation
- [Phase 13]: focusedAtomUuid slice on graphStore (NOT substrateStore) — UI-interaction marker, not substrate semantic. Canonical setter API: useGraphStore.getState().selectNode(uuid) NOT setSelectedNode
- [Phase 12-conflict-supersession-engine]: 12-04: Adversarial harness (5 fact + 1 intent baseline fixtures) gated by CI_LLM_LIVE=1 — first-run numbers become regression baseline — Per RESEARCH.md Pattern 5: recall ≥ 0.80, precision ≥ 0.85 thresholds; ≥ 8/10 baseline match for intent + d8 NEEDS_HUMAN_REVIEW special case
- [Phase 12-conflict-supersession-engine]: 12-04: demo_force_intent_drift uses stub-fallback pattern (real impl + stub on opposite cfg arms) — generate_handler! always references same symbol; function body is the gate — Tauri's macro doesn't support cfg-block insertion cleanly; stub-pattern means JS callers always have a stable command name regardless of build flavor
- [Phase 12-conflict-supersession-engine]: 12-04: Beat 3 backstop layered safety — compile-time cfg(feature=demo-fixture) + runtime path heuristic (.contains('contract-ide-demo')) — both required to mutate state — Production binaries cannot reach impl regardless of caller; runtime gate is belt-and-braces against accidental triggering in feature-enabled builds opened against non-demo repos
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-04]: ServiceCard is one component, kind-switched header, uniform body — 7 backend kinds (api/lib/data/external/job/cron/event) share the body rendering. Adding a new kind is one header case, not a new file.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-04]: Test infrastructure parity — vitest .test.ts (NOT .test.tsx), no @testing-library/react, environment:'node'. String-shape / pure-logic checks per DeltaBanner.test.ts precedent. Plan asked for testing-library; project owner can opt-in to that in a future test-infrastructure phase.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-04]: Line-based markdown section parser instead of regex with multiline anchors. JS regex \\z is not supported and ^/$ multiline + lookahead requires careful escape handling. Line-based scan is more readable, debuggable, and correct on a controlled format.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-04]: Multi-output via ### status subheadings. pendingStatus tracker attaches the most recent ### label to the next fenced block; resets after attachment. Backwards-compatible — single fenced block under ## Outputs renders without status label.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-04]: data-atom-uuid + data-state DOM attributes on chips — stable selectors for plan 13-07's citation halo without coupling to React component identity. document.querySelector by uuid bypasses store and memo churn.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-04]: Empty-schema fallback copy 'No schema declared (BACKEND-FM-01 not populated)' — informative + traceable to Phase 9 contract gap rather than alarming. Phase 13 plan 11 rehearsal should treat this as a Phase 9 gap signal, not a 13-04 bug.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: nodes.file_path is intentionally NULL post-Phase 2 DATA-01 pivot (scanner.rs:213 comment "file_path kept NULL"). Sidebar IPC walks .contracts/ on disk in spawn_blocking instead of grouping by file_path column. Future repo-tree IPCs (PR review file tree, etc.) should follow the disk-walk pattern.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: ContractFrontmatter has NO `name` field — name is derived at scan time from code_ranges[0].file basename (scanner.rs:177-187), stored in `nodes.name`. Any IPC needing a contract's display name must cross-reference against `nodes` table, not parse the sidecar.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: ROOT_AREA = '_root' sentinel exported from ipc/sidebar.ts (NOT hardcoded as a magic literal in JSX). Frontend special-cases italic "Root" rendering. Importable for plan 13-10a reset script + plan 13-11 rehearsal indicators.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: SIDEBAR_REFRESH_EVENT = 'sidebar:refresh' CustomEvent contract — plan 13-10a's reset script dispatches this after re-seeding fixture data; SidebarTree listens and re-fetches via getSidebarTree(). Decouples 13-10a from graph store update cadence.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: Lens switcher DELETED (not feature-flagged). currentLens slice on graphStore retained as unused (Phase 14 cleanup) — removing it would cascade into Breadcrumb, GraphCanvasInner, Phase 3 SUMMARY contract. Cleaner to delete the affordance and defer the slice removal.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: Reducer-style Zustand selectors for badge counts — `area.member_uuids.reduce((acc, u) => acc + (set.has(u) ? 1 : 0), 0)` returns a primitive number so Object.is short-circuits on unchanged counts. Filter-and-length would create a new array per evaluation and re-render the row even when count is identical.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: Flow click writes to BOTH stores in one handler — useSidebarStore.setSelectedFlow(uuid) AND useGraphStore.getState().pushParent(uuid). Plan 13-06 will refine the canvas response, but the navigation handle is wired today so the demo arc works incrementally.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-02]: Inline Badge pill component (16px-min, 10px text, ring-1 + bg-color/15 + text-color/40) — shadcn Badge would add dep weight without giving us the tight sizing needed at sidebar scale. Reusable for plan 13-08 PR review badges + plan 13-11 rehearsal indicators.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: shouldFilter={false} on cmdk Command.Dialog when an external IPC ranker is authoritative. cmdk's default command-score against Command.Item value=uuid drops every row (uuids share no chars with intent queries). The IPC's BM25 ranking is the source of truth; cmdk should render our pre-ranked list without re-sorting. Reusable for any future palette where BM25 / embedding / LLM rerank is authoritative — set shouldFilter={false} + key items by uuid.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: BM25 inversion + score normalisation for contracts-above-substrate ranking. nodes_fts.bm25() returns lower-is-better; negate for higher-is-better, then normalise so contract scores stay above substrate's flat 0.5 baseline. Demo Beat 1 wants AccountSettings.DangerZone (a contract) at top-1, not a substrate decision that mentions both words.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: OR-tokenizer with English stopword stripping for FTS5 (build_fts_or_query in commands/substrate.rs, mirrors Phase 9 mass_edit.rs). Query "the workspace is deleted" → "workspace OR deleted" not "the AND workspace AND is AND deleted" — broader recall than AND-tokenized phrases. Stopwords: the/of/a/an/and/or/in/to/at/etc.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: Per-kind navigation handler in IntentPalette is the single source of truth for canvas drill-in from intent search: flow → setSelectedFlow + pushParent (sidebar + canvas in lockstep); L4 atom → pushParent(parent) + setFocusedAtomUuid (chip halo for plan 13-05); L0–L3 contract → pushParent(uuid); substrate → selectNode(parent_uuid). Plan 13-07 will swap the substrate branch for a dedicated archaeology modal trigger.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: preventDefault BEFORE setOpen on Cmd+P keydown listener — macOS default Cmd+P opens system Print dialog; without preventDefault, the dialog races React state flush and the keystroke is unrecoverable. Pitfall 2 in 13-RESEARCH.md.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: Precision test fixture gated on VITEST_INTEGRATION=1 — default unit-test environment is `node` with no Tauri runtime; invoke('find_substrate_by_intent') would throw 'invoke is not a function'. Three structural tests (length=10, all entries non-empty, expected_kind in valid 7-kind enum) run unconditionally; integration assertion runs in plan 13-10b UAT against live Tauri dev server with seeded substrate.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-03]: Task 3 checkpoint:human-verify is the textbook case for why Wave 2 plans use human-verify rather than auto. Tasks 1+2 shipped correct IPC + navigation + fixture; cargo + tsc + vitest all green; but a single missing prop (shouldFilter={false}) on Command.Dialog made the entire pipeline invisible at the UI layer because cmdk's filter dropped IPC-ranked hits. User-verify caught it in 60 seconds; programmatic verification would have missed it (TS unit tests don't render cmdk through real DOM, cargo tests stop at IPC return).
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: 13-05 visual verification deferred to plan 13-06 per user direction — natural test surface via FlowChainLayout. Plan 13-06's FlowChainLayout depends on 13-05 ScreenCard and will mount it inside a real flow chain (ScreenCard at top + ServiceCards below in flow.members order). Setting up a custom annotated localhost page just for 13-05 isolation is wasted scope; the seeded contract-ide-demo repo's app/account/settings/page.tsx is the production test surface. Unit tests (5/5 iframeChipPositioning + 8/8 ScreenCard) + tsc clean + vite build success cover plan-level isolation; visual end-to-end gates on 13-06.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: Wave 2 COMPLETE — 4 plans landed (13-04 ServiceCard, 13-02 Sidebar, 13-03 Cmd+P + cmdk fix, 13-05 ScreenCard). Wave 3 next: plan 13-06 FlowChainLayout — assembles vertical chain with ScreenCard at top + ServiceCards below; renders call-shape edge labels from each participant's ## Outputs → next participant's ## Inputs (BACKEND-FM-01).
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: Parent-layer atom-chip overlay (NOT inside iframe) — three reasons codified: (1) cross-origin defence — even if same-origin today, no script injection into user dev server; (2) pan/zoom integrity — iframe content scrolls/scales independently of canvas, parent-layer chips stay positioned correctly under react-flow zoom; (3) click intercept — pointer-events-none container + pointer-events-auto chips lets Inspect mode work. Reusable pattern for any future card variant where body content is third-party (remote preview targets, sandboxed live demos, etc.).
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: requestChipRects same-origin direct + postMessage fallback — preferred path is iframe.contentDocument.querySelectorAll('[data-contract-uuid]') (works under Phase 4 Plan 04-03 frame-src CSP for http://localhost:* — same-origin Tauri WebView), graceful postMessage fallback with 250ms timeout for cross-origin scenarios. SecurityError swallowed silently. Future cross-origin scenarios (remote preview targets, prod deploy URLs) gracefully degrade to postMessage; today the seeded fixture's iframe content does NOT register a postMessage responder so fallback returns [].
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: Inspect (default) / Interact mode toggle — Inspect: iframe pointer-events: none + chips pointer-events: auto → chips intercept clicks (Beat 1 visceral click-to-resolve foundation). Interact: iframe pointer-events: auto + chips fade to 0.4 opacity but still clickable (power-user affordance). Toggle ships even though demo doesn't require Interact mode — Inspect mode requires the iframe to ignore pointer events by default for chip-click intercept to work.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: nodeTypes.ts append-only Wave 2 serialization compliance — `screenCard: ScreenCard` placed AFTER plan 13-04's `serviceCard: ServiceCard` entry; both registrations preserved. lib.rs UNTOUCHED (no Rust IPC commands added — probeRoute already shipped in Phase 4 Plan 04-03). tauri.conf.json frame-src verified READ-ONLY (Phase 4 Plan 04-03 ships both standard ports `http://localhost:* http://127.0.0.1:*`).
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: Empty-element fallback (no JSX has matching `data-contract-uuid`) renders nothing in 13-05 — section-bottom placement (look up `code_ranges.section_uuid` → find parent section's `data-contract-section-uuid` element → place chip at section's bottom region) deferred to plan 13-10b once seeded fixture exists. TODO comment in AtomChipOverlay.tsx references plan 13-10b. Phase 9 BABEL-01 dependency surfaces as Phase 9 contract gap to plan 13-11 rehearsal if iframe content lacks `data-contract-uuid` annotations on the Beat 1 surface.
- [Phase 13-substrate-ui-demo-polish]: [Plan 13-05]: ScreenCardData wire shape — `{uuid, name, route, devServerUrl?}` is the contract for plan 13-06's flow-chain assembler. Assembler emits `{type: 'screenCard', data: ScreenCardData, position}` for L3 trigger nodes when `kind: 'screen'`; `route` extracted from contract frontmatter (Phase 9 FLOW-01 UI flow trigger contracts carry this); default `devServerUrl` to `http://localhost:3000` unless user has configured otherwise. Leading-slash defence on route composition (8 vitest cases verify URL composition).
- [Phase 13-substrate-ui-demo-polish]: Plan 13-07 complete: useCitationStore + SourceArchaeologyModal + citation halo wiring
- [Phase 13]: Plan 13-08 complete: SUB-09 PR-review intent-drift mode shipped — parseDiffHunks + analyze_pr_diff IPC + PRReviewPanel + Cmd+Shift+P toggle; transient overlay via snapshot-and-restore; defensive on missing substrate schema
- [Phase 13-substrate-ui-demo-polish]: 13-09 visual verification deferred to plan 13-10b — natural test surface via DemoOrchestrationPanel + fixture-loaded uuids
- [Phase 13-substrate-ui-demo-polish]: Wave 4 complete: 3 plans landed (13-07 SourceArchaeologyModal, 13-08 PRReviewPanel, 13-09 Sync/Verifier/Harvest panels)
- [Phase 13-substrate-ui-demo-polish]: Wave 5 next: 13-10a (data layer — SQL seed + JSON fixtures + reset script) and 13-10b (UI orchestration — IPCs + DemoOrchestrationPanel) — zero file overlap, full parallel
- [Phase 14]: 14-01b: bundled-deps strategy ratified — skill commits node_modules (~8.5MB) per RESEARCH Pattern 5; root .gitignore negation override applied
- [Phase 13]: Plan 13-10a: PLACEHOLDER-* uuid prefix for Phase 9 substitution; defensive CREATE TABLE IF NOT EXISTS for seed
- [Phase 14]: Bootstrap demo target = Marginalia (Yang-built micro-SaaS, option-a). 51 src files / 16 commits / Prisma 7 + argon2 sessions in-house (lucia v3 EOL).
- [Phase 15]: Option A for substrate_rules_json threading: frontend JSON-stringifies hit UUIDs and passes to delegate_execute, avoids wasteful re-run of FTS+rerank
- [Phase 15]: FTS tombstone fix: v8 replacement substrate_nodes_au trigger with WHERE new.invalid_at IS NULL guard — tombstoned rows removed from FTS, not re-indexed

### Pending Todos

- [ ] Phase 7 carry-over: refresh_nodes delete-event handling (punted from 07-02) — distinguishing sidecar-delete vs source-delete requires more surface area; watcher currently skips non-existent paths with inline TODO(Phase 7). Assess in Phase 8 if PostToolUse hook deletes files.
- [ ] Cosmetic: rename Tauri bundle identifier away from `.app` suffix (e.g., `com.contract-ide.ide`) — non-blocking, surfaced by Plan 01-01 build warning
- [ ] Hygiene: human-verify checkpoints that run `npm run tauri build` should quit the resulting `.app` bundle before the next plan starts (Plan 01-02 tripped on a stale Plan 01-01 production process)
- [ ] UX copy review: GraphPlaceholder empty-state reads "No contracts yet — open a repo" — if Phase 2 descopes the folder picker (SHELL-02), copy must change to avoid a dead-reference affordance
- [ ] Dev-hygiene: add an explicit "verify major-version dep API shape against node_modules" step to upgrade checklist (Phase 1 has been bitten twice: shadcn v4 `--preset`, react-resizable-panels v4 percentage-string API)
- [ ] Phase 4 Monaco integration: replace blur-triggered autosave with debounced typing-while-saving cadence (SHELL-05's full spec; Phase 1 ships blur-only per plan)
- [x] Phase 2: remove `?force-error` URL-param override from GraphPlaceholder once real IPC failure modes exist — DONE in Plan 02-02
- [ ] Phase 9 polish: remove `Day1Validation` panel + floating pill + `commands::validation` Rust module before any user-facing ship (tracked from Plan 01-04)
- [ ] Phase 9 distribution: fix `bundle_dmg.sh` failure under `tauri build -- --debug` (non-blocking until DMG artifacts are actually needed)
- [ ] Phase 13 polish: CodeTab does not auto-refresh after the agent edits a source file mid-stream. Beat 2 lands harder if the inspector visibly catches up — subscribe CodeTab to drift:changed for the currently-selected node and re-invoke read_file_content on fire. Caught at Phase 8 UAT Gate 2.
- [ ] Phase 13 polish: Address Phase 8 review blocker remnants — accept_rollup_as_is + apply_cherrypick still rely on UI-layer pin gating to drive the user paths; the writers re-check pin under DriftLocks but never surface SKIPPED-PINNED back to the user as a toast. Wire a non-fatal toast for the rare race-loss case.
- [ ] Phase 13 polish: Cherrypick DiffPane shipped with a fixed 320px height per Monaco's @monaco-editor/react 4.x layout collapse. Consider responsive sizing once a global Dialog-content height system exists.
- [ ] Phase 13 polish: AppShell + CherrypickModal still emit the React `Internal React error: Expected static flag was missing` warning during HMR cycles. Production build is unaffected. Track if it appears outside dev.
- [ ] Phase 13 polish: claude CLI emits `Warning: no stdin data received in 3s` on every run_agent spawn — cosmetic noise, requires forking tauri-plugin-shell to detach stdin. Acceptable for demo (warning is suppressed via stream-json output).
- [ ] Phase 8 deferred: `recompute_all_rollups` Rust IPC exists but is never called at boot. The cascade now fires reactively on save. Wire it into open_repo's post-scan path (or invoke from AppShell on hydrate) for a startup full-scan if cascade state ever needs cold-start guarantees.
- [ ] Phase 8 deferred: `accept_rollup_as_is` synthesizes `session_id = "ide-direct-<ts>"` for journal entries from direct IDE actions; not the actual Claude session ID. v2 should fold this into the real session journal once the substrate distiller (Phase 11) consumes journal entries.

### Blockers/Concerns

- JSONL schema field names VALIDATED (08-04a): camelCase sessionId, .message.usage.input_tokens, .message.content[i].type=="tool_use" — all confirmed against real session files.
- PostToolUse hook output schema has a documented inconsistency (GitHub #19115); confirmed approach: exit 0, no stdout — validate before Phase 7
- MCP SDK v2 anticipated Q2 2026; assess migration cost if it ships during build week (Phase 5)
- Non-coder copy-edit UX (Phase 9) has LOW confidence per research; test with one non-technical person before scripting the demo beat

## Session Continuity

Last session: 2026-04-25
Stopped at: Completed 13-10b-PLAN.md — Phase 13 DEMO-04 UI orchestration shipped (sync.rs reads blast-radius.json + new demo_orchestration.rs IPCs + DemoOrchestrationPanel mounted dev-only); Task 3 verification deferred to plan 13-11 by user direction (natural test surface for real Phase 9 uuid substitution + 4-beat end-to-end + cmdp-precision ≥8/10 gate). Wave 5 complete; Wave 6 next: plan 13-11 (final plan). Commits 4d648fd, f6f6f8d.

Previous stopped at: Completed 13-05-PLAN.md — Phase 13 ScreenCard + atom-chip overlay (CARD-01 + CHIP-01) shipped. UI-mode L3 trigger card with iframe at the screen contract's `route` (Phase 4 PreviewTab probe + retry pattern reused via probeRoute IPC); AtomChipOverlay layered in PARENT layer (NOT inside iframe — sidesteps cross-origin script injection AND pan/zoom interference); requestChipRects helper prefers same-origin iframe.contentDocument.querySelectorAll('[data-contract-uuid]') (works under Phase 4 frame-src CSP for http://localhost:*), graceful postMessage fallback with 250ms timeout for cross-origin scenarios. AtomChip uses canonical selectNode + setFocusedAtomUuid setters (checker N7) + data-atom-uuid/data-state attributes for plan 13-07 citation halo. Inspect (default, iframe pointer-events: none → chips intercept clicks) / Interact mode toggle (iframe pointer-events: auto + chips fade to 0.4 opacity). nodeTypes.ts: append-only `screenCard: ScreenCard` AFTER plan 13-04's `serviceCard` entry per Wave 2 serialization_hint; both registrations preserved. lib.rs UNTOUCHED. 13 vitest cases (5 iframeChipPositioning + 8 ScreenCard); tsc + vite build all clean. Commits d66cbc9 (Task 1 helper + chips + overlay + styles), 4089fda (Task 2 ScreenCard + nodeTypes registration). Task 3 checkpoint:human-verify DEFERRED to plan 13-06 by user direction — natural test surface via FlowChainLayout (ScreenCard at top + ServiceCards below); setting up custom annotated localhost page for 13-05 isolation alone is wasted scope. Empty-element + section-bottom fallback deferred to plan 13-10b. Wave 2 COMPLETE (4 plans landed: 13-04, 13-02, 13-03, 13-05).
Resume file: 13-06-PLAN.md (Wave 3 FlowChainLayout — CHAIN-01 + CHAIN-02). `/gsd:execute-phase 13` resumes the next incomplete plan. 13-06 assembles vertical L2 chain with ScreenCard at top + ServiceCards below in flow.members order; renders call-shape edge labels from each participant's ## Outputs → next participant's ## Inputs (BACKEND-FM-01). Visual end-to-end verification of 13-05 ScreenCard isolation lands inside 13-06's checkpoint — flow-chain integration is the natural test surface. Concurrent outstanding: Phase 9 UAT (09-UAT.md), Phase 12 phase-verifier (orchestrator's job — gsd-verifier closes Phase 12), Plan 11-05 (Substrate panel + UAT), ≥80% top-1 precision SC validation (gates on plan 13-10b after 13-10a populates substrate), and 13-10b's section-bottom chip fallback wiring (TODO in AtomChipOverlay.tsx).
