# Live Scenario — Beat 4 Workspace Delete

**Status:** SUPERSEDED 2026-04-25 by `.planning/demo/presentation-script.md` § Beat 4. The "live moment" of the demo is now Beat 4 of the locked 4-beat structure (workspace-delete prompt on T's laptop with substrate compounding visible), NOT a separate single-prompt artifact.

This file is preserved as a redirect for any external reference; the canonical content lives in the script.

**See:** `.planning/demo/presentation-script.md` § Beat 4 — Closed loop (3:15–3:50)

---

## Why replaced

The original `live-scenario.md` was placeholder content (button-color brand-voice scenario) carried over from the v1 demo plan, when the live moment was conceived as a single-prompt 20–30s beat after a 3-minute recorded video. The committed delete-account → workspace-delete scenario locked 2026-04-24 absorbed the live moment into Beat 4 of the 4-beat structure.

Per `.planning/ROADMAP.md` planning note: *"Phase 13 absorbs ... the live-scenario.md replacement (the live moment is now Beat 4 of presentation-script, not a separate single-prompt artifact)."*

## What's specific to the live-recording moment (Beat 4)

These details belong here because they're filming-specific, not performance-canonical:

**Camera:** T laptop primary; recorded inset (8s) plays in adjacent window during the bare-Claude comparison.

**Key lines (full prose in script § Beat 4):**
- T types `add a delete-workspace button to the team settings page` in Claude Code terminal
- Substrate query surfaces all 5 morning rules
- Agent writes 5-file change first try
- Three new substrate atoms animate onto specific participants in the new chain (NOT into a generic harvest panel)
- One of the three carries `[⌃ promoted from implicit]` badge

**Reset state for the live moment:**
- T's laptop substrate must contain the 5 morning rules from Beat 2 (already loaded)
- The Beat 4 prompt must produce the workspace-delete flow chain (Phase 9 FLOW-01 dependency)
- DemoOrchestrationPanel "Beat 4: Harvest panel" button armed
- `flow-delete-account` already rendered on canvas from Beat 3 (so the new `flow-delete-workspace` enters as a sibling, not a replacement)

**Timing budget:** ~35s per script § Beat 4. Anything longer pulls budget from the Close.

**Glitches → see `runbook-v2.md` § Beat 4 glitch playbook** (workspace-delete flow doesn't render, HarvestPanel doesn't appear, promoted badge missing, ghost-ref styling issues).

---

*This file is now a redirect, not the canonical source. Beat 4 of `presentation-script.md` is what gets filmed; this file points there.*
