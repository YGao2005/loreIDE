---
phase: 04-inspector-monaco
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - .planning/phases/04-inspector-monaco/04-04-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - INSP-01
  - INSP-02
  - INSP-03
  - INSP-04
  - INSP-05

must_haves:
  truths:
    - "Clicking any graph node opens the inspector with Contract / Code / Preview / Receipts tabs"
    - "Monaco Code tab renders range-scoped view with workers verified in Tauri dev console"
    - "Inspector shows a live localhost preview iframe when vercel/commerce is running"
    - "Preview tab shows 'Start dev server' prompt when vercel/commerce is not running"
    - "Contract edits autosave with human_pinned:true written to sidecar .md"
    - "Drift indicator reads Synced vs Drifted correctly based on contract_hash vs code_hash"
    - "Cmd+R reveals the correct file in Finder for a multi-file code_ranges node; Cmd+O opens in $EDITOR"
    - "vercel/commerce clones and npm run dev boots on localhost:3000 with committed Shopify credentials"
  artifacts:
    - path: ".planning/phases/04-inspector-monaco/04-04-SUMMARY.md"
      provides: "End-to-end Phase 4 UAT results across all six success criteria"
      min_lines: 40
  key_links:
    - from: "Graph canvas node click"
      to: "Inspector with correct tab content"
      via: "selectedNodeUuid Zustand subscription"
      pattern: "selectedNodeUuid"
    - from: "Contract edit"
      to: "sidecar .md with human_pinned:true"
      via: "saveContract → write_contract IPC"
      pattern: "human_pinned:\\s*true"
    - from: "vercel/commerce dev server"
      to: "Preview iframe"
      via: "probe_route + frame-src CSP"
      pattern: "frame-src.*localhost"
---

<objective>
Phase 4 end-to-end UAT. Validate every Phase 4 success criterion against a running app + a live vercel/commerce dev server, using real (committed) Shopify Partner test-store credentials. This plan ships no code — it exercises the code from plans 04-01, 04-02, and 04-03 as a single integrated system and surfaces any integration gap as a blocker for this phase before Phase 5/6/7 start consuming it.

Purpose: the three preceding plans each tested their own piece in isolation. SC 6 in particular ("vercel/commerce clones and `npm run dev` boots the preview target on localhost") can only be verified end-to-end. This plan also catches any integration surprise — e.g., vercel/commerce routes not matching the seeded contract routes, Monaco + iframe context contention, Shopify credential flakiness under real load. Discovering these now (not during demo filming) is the entire point.

Output: a filled-out UAT report in `04-04-SUMMARY.md` with pass/fail on each of the six Phase 4 success criteria, committed Shopify credentials in `.env.local` of the local vercel/commerce checkout (NOT in the Contract IDE repo), and an explicit sign-off marking the phase complete (or CONDITIONALLY complete — see PATH C handling in Task 2 and the success-criteria section below).

Wave ordering: runs in Wave 4 — strictly after Plan 04-03 (Wave 3) so the reqwest probe and frame-src CSP are in place before the UAT exercises the live preview iframe.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-inspector-monaco/04-RESEARCH.md
@.planning/phases/04-inspector-monaco/04-01-PLAN.md
@.planning/phases/04-inspector-monaco/04-02-PLAN.md
@.planning/phases/04-inspector-monaco/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone vercel/commerce and attempt dev-server boot</name>
  <files>
    (external checkout at /Users/yang/vercel-commerce or equivalent — no Contract IDE repo files modified)
  </files>
  <action>
Prepare the live preview target. This prepares Task 2's human checkpoint.

1. Check whether `vercel/commerce` is already cloned locally. If yes, record the path and skip to step 4. If no, proceed.

2. Clone vercel/commerce to a sibling directory of the Contract IDE checkout:
   ```bash
   cd /Users/yang
   git clone https://github.com/vercel/commerce.git vercel-commerce
   cd vercel-commerce
   npm install
   ```
   Record the exact commit SHA of vercel/commerce HEAD in the SUMMARY so the UAT is reproducible.

3. Attempt `npm run dev` against vercel/commerce. Expected outcome: without Shopify credentials, it will fail with a missing-env-var error. Capture the exact error message for the SUMMARY.

4. Inspect whether Shopify credentials are already available somewhere:
   - `/Users/yang/vercel-commerce/.env.local`
   - Any other vercel/commerce checkout on the machine
   - Any prior project `.env*` Yang has referenced for Shopify

   If credentials exist, copy them to the active checkout's `.env.local` (verify the file is in `.gitignore` — it is in upstream vercel/commerce), run `npm run dev`, and jump to step 6.

5. If no credentials exist ANYWHERE on the machine, stop and surface Task 2's human checkpoint. Do NOT attempt to create a Shopify account via automation — Shopify actively blocks that pathway.

6. Once `.env.local` is populated with `SHOPIFY_STORE_DOMAIN`, `SHOPIFY_STOREFRONT_ACCESS_TOKEN`, and `SHOPIFY_REVALIDATION_SECRET`, run `npm run dev` and confirm the server boots on `http://localhost:3000` without errors. `curl -I http://localhost:3000/` should return 200 or 307. `curl http://localhost:3000/ | head -100` should include product/collection names from the connected Shopify store (proves credentials are actually wired, not just syntactically present).

7. Leave the dev server running for Task 3's verification checkpoint. Record the `.env.local` location in the SUMMARY so Plan 09-xx (demo seeding) can reuse it.

Avoid:
- Do NOT commit `.env.local` to the Contract IDE repo (`/Users/yang/lahacks/`) — Shopify credentials stay with the vercel/commerce checkout.
- Do NOT attempt to create a Shopify account via puppeteer/selenium/CLI — Shopify actively blocks automated account creation; a human checkpoint is appropriate.
- Do NOT fake the credentials with placeholder values and skip the real boot — SC 6 is explicit that the preview must actually BOOT against a live storefront.
  </action>
  <verify>
    `curl -I http://localhost:3000/` returns 200 or 307. Response body contains real product data from the connected Shopify test store. `.env.local` exists at the vercel-commerce checkout root and contains three non-empty Shopify env vars.
  </verify>
  <done>
    Either: vercel/commerce dev server running on localhost:3000 with live Shopify data, OR Task 2 checkpoint raised because no credentials were available. `.env.local` location documented.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Shopify credentials checkpoint (skip if Task 1 succeeded)</name>
  <action>
This checkpoint fires only if Task 1 Step 5 discovered no Shopify credentials. If Task 1 Step 6 succeeded, SKIP this task and proceed to Task 3.

SC 6 of Phase 4 requires vercel/commerce to actually BOOT. That requires three environment variables that only come from a Shopify Partner account + development storefront. Shopify requires web-based account creation with email verification — no CLI, no API. This is the single legitimate human-action checkpoint in Phase 4.

The user must choose ONE path and follow the instructions below:

PATH A — Create a new Shopify Partner test store (recommended per RESEARCH §vercel/commerce):
1. Visit https://partners.shopify.com and sign up with gaoyang1000@gmail.com.
2. After verifying email, create a Development store (Stores → Add store → Create development store).
3. In the dev store admin: Apps → App and sales channel settings → Develop apps → Create an app → name it "Contract IDE Demo".
4. Under Configuration for that app, enable Storefront API access with scopes: unauthenticated_read_product_listings, unauthenticated_read_collection_listings, unauthenticated_read_checkouts.
5. Copy the Storefront access token.
6. In the vercel-commerce checkout: `cp .env.example .env.local` and populate SHOPIFY_STORE_DOMAIN (e.g. `your-dev-store.myshopify.com`), SHOPIFY_STOREFRONT_ACCESS_TOKEN (paste), SHOPIFY_REVALIDATION_SECRET (any random string).
7. Seed a few products via the dev store admin so the preview has real content.
8. Run `npm run dev` and verify localhost:3000 boots with a real storefront.
9. Resume with "credentials ready".

PATH B — Use existing Shopify store credentials (if the user already has them):
1. Verify the credentials have Storefront API scopes listed above.
2. Populate `.env.local` in the vercel-commerce checkout.
3. Confirm `.gitignore` excludes `.env.local` (it does in upstream vercel/commerce — verify in your checkout).
4. Run `npm run dev` and verify.
5. Resume with "credentials ready".

PATH C — Defer SC 6 (CONDITIONAL phase completion; DOES NOT satisfy the phase goal):
Choosing PATH C means Phase 4 is marked CONDITIONALLY COMPLETE, not UNCONDITIONALLY COMPLETE. The phase goal per ROADMAP requires Shopify credentials committed (to the vercel/commerce `.env.local`) and the dev server boot verified before the phase passes. Deferral punts that verification — it does not waive it. The user decides whether the tradeoff is acceptable.

If PATH C is chosen, Task 3 MUST perform all of the following (not merely some):
1. Write `04-04-SUMMARY.md` recording SC 6 as **DEFERRED** (not FAILED, not PASSED) with an explicit justification for why deferral is acceptable right now.
2. In `04-04-SUMMARY.md`, include a top-level "Phase 4 Status: CONDITIONALLY COMPLETE (SC 6 deferred)" line so downstream readers cannot miss it.
3. Update `.planning/STATE.md` to record SC 6 as **BLOCKING for Phase 9** — specifically, add it under an explicit Blockers section (not merely as a pending todo). Phase 9 execution MUST NOT start until SC 6 is resolved. The STATE.md entry should state this in one line, verbatim: "Phase 9 BLOCKED: SC 6 of Phase 4 (vercel/commerce boot with Shopify credentials) must be resolved before Phase 9 starts."
4. Update `.planning/ROADMAP.md` Phase 4 row to "Complete (conditional — SC 6 deferred, see STATE.md)" rather than plain "Complete".
5. Update `.planning/REQUIREMENTS.md` traceability for INSP-02 to "Conditional" rather than "Complete" (INSP-01, -03, -04, -05 can still flip to Complete if their SCs passed).

Then resume with "defer sc6".

RESEARCH §vercel/commerce Preview Target flags this as "catch demo blocker here, not the night before filming" — PATH C trades that protection for time. PATH A or B remain strongly preferred.
  </action>
  <verify>
    User has typed "credentials ready" or "defer sc6". If "credentials ready": `curl http://localhost:3000/` returns real Shopify storefront HTML. If "defer sc6": Task 3 sign-off checklist below enforces the CONDITIONAL completion paperwork.
  </verify>
  <done>
    Either live vercel/commerce is running OR SC 6 is explicitly deferred with documented reason AND the conditional-completion paperwork obligations are surfaced for Task 3 to execute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end Phase 4 UAT — all six success criteria</name>
  <action>
Run the full Phase 4 UAT against the live running app. Pre-flight state:
- Contract IDE running via `npm run tauri dev` with a contract-seeded repo loaded.
- vercel/commerce dev server running on http://localhost:3000 with real Shopify data (from Task 1), OR SC 6 explicitly deferred (from Task 2 PATH C).
- Tauri dev console open (View → Toggle Developer Tools).

Execute each SC check below, record PASS / FAIL / DEFERRED with brief notes, write results into `.planning/phases/04-inspector-monaco/04-04-SUMMARY.md`. Any FAIL triggers gap-closure flow (`/gsd:plan-phase 4 --gaps`).

**SC 1 — Node click → Inspector + tabs + Monaco workers:**
1. Click any node in the graph → inspector updates with that node's data.
2. Header shows node name, level, kind.
3. Four tabs visible: Contract, Code, Preview, Receipts.
4. Switch to Code tab → Monaco loads.
5. Check Tauri dev console → ZERO instances of "Could not create web worker" or any worker-related error.
6. Code tab shows actual line numbers from the file (not starting at 1); ~3 lines of dimmed context above/below the range.
7. Click an expand handle → hidden lines reveal.

**SC 2 — Live localhost preview + Start-dev-server prompt:**
1. Click a node whose `route` points to a valid vercel/commerce path (e.g. `/`, `/products`, `/cart`). If no seeded contract has a matching route, manually edit one sidecar's `route` field to `/` and reload the repo.
2. Switch to Preview tab → iframe renders the live storefront from localhost:3000.
3. Stop the vercel/commerce dev server (Ctrl+C in its terminal).
4. Click "Retry probe" in the Preview tab → state flips to "No dev server reachable" prompt.
5. Restart `npm run dev` → click Retry → iframe re-renders.

**SC 3 — Contract edit + human_pinned preserved:**
1. Click a node. Switch to Contract tab.
2. Type new text in the contract body. Wait 500ms.
3. "Editing…" indicator flips to "Saved".
4. In a terminal: `cat <repo>/.contracts/<uuid>.md` → verify YAML frontmatter contains `human_pinned: true` AND `contract_hash: <hash>` (non-null, non-placeholder).
5. Restart the app, re-open the repo, click same node → edit persisted.

**SC 4 — Drift indicator:**
1. Find a node whose `contract_hash` matches its `code_hash` → inspector header shows green "Synced" pill.
2. Artificially introduce drift: edit the sidecar .md and change `code_hash` to a junk value like `000000...`, save, wait for file watcher to refresh.
3. Click the node → inspector header shows red "Drifted" pill with pulsing dot + "Reconcile" button.
4. Click Reconcile → console shows placeholder log (full reconcile lands in Phase 7, not a blocker).

**SC 5 — Reveal in Finder + Open in External Editor for multi-file node:**
1. Find (or construct) a node whose `code_ranges` has entries in TWO different files.
2. Click the node. Switch to Code tab. Two stacked Monaco instances visible, one per file.
3. Click `⌘R Reveal` on the SECOND file's toolbar → Finder opens with that file highlighted (not the first file).
4. Click `⌘O Open` on the second file → `$EDITOR` opens the file at approximately the correct line.
5. Keyboard shortcut check: with Code tab focused, Cmd+R reveals the FIRST range (documented behavior per Plan 04-01). Cmd+O opens first range.

**SC 6 — vercel/commerce boots (skip if deferred via PATH C):**
1. Already verified in Task 1 step 6. Spot-check: visit http://localhost:3000 in a browser (outside Tauri) → real storefront loads with products.
2. Iframe in Contract IDE's Preview tab renders the same storefront.
3. If PATH C was taken in Task 2: do not claim SC 6 as PASSED. Mark DEFERRED and execute the PATH C sign-off checklist below.

After all checks, execute the appropriate sign-off path:

**Sign-off path UNCONDITIONAL (PATH A or PATH B — SC 6 passed live):**
1. Write `04-04-SUMMARY.md` with a section per SC including PASS/FAIL, evidence (command output excerpt, textual confirmation, or terse bug report). Include a top-line "Phase 4 Status: COMPLETE" banner.
2. `.planning/ROADMAP.md` — Phase 4 row → "Complete" with date.
3. `.planning/REQUIREMENTS.md` — INSP-01..05 → Complete.
4. `.planning/STATE.md` — advance current position past Phase 4; record decisions surfaced during UAT; add any new pending todos (Phase 7 reconcile, Phase 9 visual polish).

**Sign-off path CONDITIONAL (PATH C — SC 6 deferred):**
1. Write `04-04-SUMMARY.md` with a section per SC. SC 6 marked **DEFERRED** (not PASSED, not FAILED). Top-line banner reads: "Phase 4 Status: CONDITIONALLY COMPLETE (SC 6 deferred — blocks Phase 9)". Include the deferral justification captured at PATH C acceptance time.
2. `.planning/ROADMAP.md` — Phase 4 row → "Complete (conditional — SC 6 deferred, see STATE.md)". Do NOT write plain "Complete".
3. `.planning/REQUIREMENTS.md` — INSP-01, INSP-03, INSP-04, INSP-05 → Complete (only if their SCs passed). INSP-02 → "Conditional" (live-preview surface exists but has not been verified against the required target).
4. `.planning/STATE.md` — advance current position past Phase 4 AND add an explicit Blockers section entry, verbatim: "Phase 9 BLOCKED: SC 6 of Phase 4 (vercel/commerce boot with Shopify credentials) must be resolved before Phase 9 starts." This is NOT just a pending todo — it must live under a Blockers heading so future `/gsd:execute-phase 9` reads it as a hard gate.
5. Add a pending todo under Phase 9 prep: "Resolve Phase 4 SC 6 (vercel/commerce + Shopify credentials) before Phase 9 starts."

Commit via: `node /Users/yang/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(04): Phase 4 UAT complete" --files .planning/phases/04-inspector-monaco/04-04-SUMMARY.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`. Use commit message `"docs(04): Phase 4 UAT complete (conditional — SC 6 deferred)"` on PATH C.
  </action>
  <verify>
    `.planning/phases/04-inspector-monaco/04-04-SUMMARY.md` exists with a section per SC (PASS/FAIL/DEFERRED) AND a top-line Phase 4 Status banner reflecting COMPLETE or CONDITIONALLY COMPLETE. ROADMAP.md Phase 4 row flipped to Complete (plain or conditional per branch). REQUIREMENTS.md traceability table flipped for INSP-01..05 (INSP-02 marked Conditional on PATH C). On PATH C: STATE.md Blockers section contains the verbatim Phase-9-blocked line. User has typed "phase 4 approved" OR described a specific failure.
  </verify>
  <done>
    All six Phase 4 Success Criteria exercised against a live running app. Results recorded with evidence. Either: (a) Phase 4 marked UNCONDITIONALLY complete in ROADMAP + REQUIREMENTS with STATE.md advanced; or (b) Phase 4 marked CONDITIONALLY complete with SC 6 deferred AND STATE.md Blockers section contains the Phase-9-blocked entry AND ROADMAP/REQUIREMENTS flag the conditional state; or (c) specific blockers documented as gap-closure inputs for `/gsd:plan-phase 4 --gaps`.
  </done>
</task>

</tasks>

<verification>
- vercel/commerce dev server running on localhost:3000 with real Shopify data (PATH A/B) OR SC 6 explicitly deferred via PATH C with the full conditional-completion paperwork.
- `.planning/phases/04-inspector-monaco/04-04-SUMMARY.md` exists, includes a pass/fail/defer line per SC, and carries a top-line Phase 4 Status banner (COMPLETE or CONDITIONALLY COMPLETE).
- On PATH C: `.planning/STATE.md` contains a Blockers section entry flagging SC 6 as a hard gate on Phase 9 (not merely a pending todo).
- On PATH C: `.planning/ROADMAP.md` Phase 4 row reads "Complete (conditional — SC 6 deferred, see STATE.md)" and `.planning/REQUIREMENTS.md` marks INSP-02 as Conditional.
- User has typed "phase 4 approved" OR documented specific failures.
- No Monaco worker errors in Tauri dev console (Pitfall 1 regression check).
- No CSP violations in Tauri dev console (Pitfall 4 regression check).
- No `human_pinned: false` written to any sidecar .md during manual editing (Pitfall 3 regression check).
</verification>

<success_criteria>
- All six Phase 4 Success Criteria exercised end-to-end against a live running app + live vercel/commerce (or documented deferral on PATH C).
- Each SC has a recorded pass/fail/defer decision with evidence in the SUMMARY.
- One of the following outcomes holds:
  - UNCONDITIONAL: SC 6 passed live; Phase 4 marked Complete in ROADMAP + REQUIREMENTS; STATE.md advanced.
  - CONDITIONAL (PATH C only): SC 6 DEFERRED; Phase 4 marked "Complete (conditional — SC 6 deferred)"; INSP-02 flagged Conditional in REQUIREMENTS; STATE.md Blockers section records SC 6 as a hard gate on Phase 9 (Phase 9 execution MUST NOT start until SC 6 is resolved); SUMMARY carries the "CONDITIONALLY COMPLETE" banner.
  - GAP CLOSURE: specific blockers documented as gap-closure inputs for `/gsd:plan-phase 4 --gaps`.
- On CONDITIONAL outcome, phase completion is explicitly marked conditional everywhere a downstream reader would check (ROADMAP row, REQUIREMENTS traceability, STATE.md Blockers, SUMMARY banner) — silent acceptance of PATH C is NOT acceptable completion.
</success_criteria>

<output>
After completion, `.planning/phases/04-inspector-monaco/04-04-SUMMARY.md` captures:
- Top-line Phase 4 Status banner (COMPLETE or CONDITIONALLY COMPLETE).
- vercel/commerce commit SHA used for verification (or "N/A — PATH C deferred" if not cloned).
- `.env.local` location (NOT contents); or "N/A — PATH C deferred".
- Pass/fail/defer result per SC with brief evidence.
- Any integration surprises discovered (unexpected CSP interaction, route mismatches, credential flakiness, Monaco + iframe rendering conflicts, etc.).
- On PATH C: the deferral justification AND an explicit pointer to the STATE.md Blockers entry flagging SC 6 as a Phase 9 gate.
- Sign-off timestamp and user approval signal.
</output>
