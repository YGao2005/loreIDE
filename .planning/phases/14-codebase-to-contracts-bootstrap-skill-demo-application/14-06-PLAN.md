---
phase: 14-codebase-to-contracts-bootstrap-skill-demo-application
plan: 06
type: execute
wave: 5
depends_on:
  - 14-05
files_modified:
  - .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md
  - .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md
autonomous: false
requirements:
  - BOOTSTRAP-05  # [proposed] Demo target bootstrapped end-to-end live or recorded as a Phase 14 SC

must_haves:
  truths:
    - "User can run /codebase-to-contracts /Users/yang/lahacks/bootstrap-demo-target inside a Claude Code session and observe the 5-stage pipeline emit a complete .contracts/ tree in <5min for <$2"
    - "Bootstrapped bootstrap-demo-target loads in Contract IDE without manual cleanup — sidebar populates with areas, Cmd+P returns semantic hits, atom chips render on the rendered iframe (BABEL-01 from Phase 9 replicates on the new target)"
    - "Re-running the skill on the same repo with no source changes performs ZERO LLM calls (full hash-skip) — verifies BOOTSTRAP-* idempotency claim"
    - "Yang reviews 5–6 derived contracts post-cold-run for prose quality (decision specificity, cross-reference density, prose density) — the live-pipeline analogue of Plan 14-04 Task 3's sample gate. Generic output triggers Plan 14-04 prompt re-iteration before recording."
    - "A Q&A-only pre-recorded inset (75–105 seconds) exists showing the bootstrap operation END-TO-END including 10–15s of actual contract .md scrolling (one backend + one UI L3 with ## Intent / ## Side effects visible) — the shot that sells 'what came out is coherent prose, not metadata noise'"
    - "The locked 4-beat presentation script (.planning/demo/presentation-script.md) is NOT MODIFIED — Phase 14's demo posture is Q&A-only inset, not a 5th beat"
    - "14-UAT.md runbook exists with reproducibility check: bootstrap runs 3 times in a row, all 3 succeed, total time per run within ±30s of mean"
  artifacts:
    - path: ".planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md"
      provides: "75–105s Q&A inset script — what to record (including contract-scrolling tail), what to say, fallback if live demo glitches"
    - path: ".planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md"
      provides: "End-to-end UAT: 3-run reproducibility + IDE-load smoke + cost / time tracking"
  key_links:
    - from: "/Users/yang/lahacks/bootstrap-demo-target/.contracts/"
      to: "Contract IDE (sidebar tree + Cmd+P + L3 trigger view)"
      via: "User opens bootstrap-demo-target in IDE; sidebar populates from .contracts/; chips render on iframe"
      pattern: "data-contract-uuid"
    - from: "qa-inset-runbook.md"
      to: ".planning/demo/presentation-script.md"
      via: "Q&A inset is OUT-OF-BAND from the 4-beat script — locked script untouched"
      pattern: "Q&A-only"
---

<objective>
End-to-end execution + verification + recording of the bootstrap demo. Run the full 5-stage pipeline against bootstrap-demo-target three times in a row to verify reproducibility, capture cost / time metrics, **gate on prose quality (5–6 contract review)** before recording, confirm the bootstrapped repo loads in the actual IDE without manual cleanup, and record a 75–105s Q&A-only inset (extended from previous 60–90s to include 10–15s of contract-file scrolling) that's playable if judges ask "does this work on any repo?" — without modifying the locked 4-beat presentation script.

Purpose: BOOTSTRAP-05 — the demo posture per RESEARCH § "Demo target codebase selection" + "Validation of the orchestrator's hypothesis". Phase 14 is the operationalization of three pitch claims (Day-0 bootstrap, ingestion-period promise, code-based seeding for cold-start). Without a recorded demo, those claims remain narrative.

**Post-revision-2 additions:**
1. **Prose Quality Gate (new Task 2.5)** between cold-run and recording: Yang reads 5–6 derived contracts (mix of UI L4 + backend L3 + flow), scores against Phase 9 exemplars on (a) decision specificity, (b) cross-reference density, (c) prose density. If any score is "generic," loop back to Plan 14-04 prompt iteration before recording. This is the gate that prevents the "show me the .contracts/ directory" embarrassment in Q&A AND seals the answer to "what did contracts look like before review?" — Yang's Q&A answer becomes "I review a sample, iterate the prompt if needed, then re-run. Three iterations of prompt tuning got us to where it produces prose I'd accept in a code review."
2. **Inset extension (Task 4)**: add 10–15s at the end of the recording showing actual contract .md files. Scroll through one backend contract (`api/account/route.ts` analogue from bootstrap-demo-target) and one UI L3 contract in a text editor or the IDE inspector; `## Intent` and `## Side effects` sections must be visible. This is the shot that sells coherent prose vs metadata noise.

Output: Q&A inset runbook (extended to 75–105s with contract-scrolling tail), Phase 14 UAT runbook (3-run reproducibility + IDE smoke + cost/time), prose-quality gate evidence in SUMMARY.md, three checkpoints for Yang.

This plan deliberately includes 3 checkpoints — (a) live end-to-end smoke with Yang witnessing, (b) prose-quality gate, (c) Q&A inset recording approval.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-RESEARCH.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-01-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-02-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-03-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-04-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-05-SUMMARY.md

# Phase 9 prose-density exemplars — the bar Yang scores against in Task 2.5
@contract-ide-demo/.contracts/ambient/api-account-delete-001.md
@contract-ide-demo/.contracts/ambient/lib-begin-account-deletion-001.md
@contract-ide-demo/.contracts/a0000000-0000-4000-8000-000000000000.md
@contract-ide-demo/.contracts/a1000000-0000-4000-8000-000000000000.md

# The locked 4-beat script — NOT modified by Phase 14
@.planning/demo/presentation-script.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author qa-inset-runbook.md (75–105s with contract-scrolling tail) + 14-UAT.md (3-run reproducibility + IDE smoke)</name>
  <files>
    .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md
    .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md
  </files>
  <action>
    **qa-inset-runbook.md** (~180 lines, revised UP from 150 for contract-scrolling tail) — Q&A inset script + recording instructions:

    Sections:
    - **Purpose:** when/whether to play this inset. Triggers: judge asks "can this work on any repo?", "what about other codebases?", "is this just for the demo repo?", "what did the contracts look like before review?". Hold the inset ready in the demo machine's mpv / QuickTime, queued. If unprompted, do NOT play.
    - **Runtime target: 75–105 seconds** (revised from 60–90s to accommodate the contract-scrolling tail). Cap firmly at 105s — anything longer dilutes Q&A flow.
    - **Recording setup:**
      - Demo machine: contract-ide running on macOS, screen-recorded via QuickTime
      - Pre-step: ensure /Users/yang/lahacks/bootstrap-demo-target/.contracts/ does NOT exist (`rm -rf` if present from prior runs). Same for contract-uuid-plugin/. Same for next.config.ts BOOTSTRAP-INSERT block.
      - In iTerm: navigate to bootstrap-demo-target, open Claude Code session
    - **Script (verbatim talking points):**

      [0:00–0:60 — main pipeline run, ~60s with jump-cuts]
      - "Most teams have repos already. Here's what 'point at any repo' looks like."
      - "I open Claude Code in an existing Next.js notes app. No contracts. No Contract IDE setup."
      - "I type: `/codebase-to-contracts`"
      - "Skill kicks off. Stage 1: enumerates the source files, classifies them. Stage 2 derives frontmatter — parallel, ~30 seconds. Stage 3 derives bodies — Intent, Role, Inputs, Outputs, Side effects. Stage 4 aligns each L4 atom to its JSX element. Stage 5 synthesizes flows, installs the Babel plugin, validates everything. Atomic — if anything fails, no `.contracts/` gets written."
      - "Total: 4 minutes. About a dollar fifty."

      [0:60–0:75 — IDE confirmation, ~15s]
      - "Open the IDE, point it at this repo. Sidebar populates. Cmd+P searches by intent. Click a screen — the iframe renders, atom chips overlay the components."

      [0:75–1:30 — NEW: contract scrolling tail, ~10–15s]
      - "And here's what came out — these are the actual `.md` files."
      - [On screen: open `bootstrap-demo-target/.contracts/<uuid-for-account-route>.md` in VS Code or the IDE inspector. Scroll slowly through `## Intent` and `## Side effects` for 4–5 seconds — these sections must be fully visible. Then jump-cut to a UI L3 contract `.md` for the account/settings page; scroll through `## Intent` and `## Role` for 4–5 seconds. The viewer should be able to read complete sentences, not just see headers.]
      - "Same operation, different repo — and what came out is prose I'd accept in a code review."

    - **Recording checklist:**
      1. Reset state (rm -rf .contracts, contract-uuid-plugin, restore next.config.ts)
      2. Time budget: 75–105s. Cuts:
         - Skip 30s of Stage 2 LLM streaming (jump-cut to "30s later")
         - Skip 60s of Stage 3 (jump-cut to "1min later")
         - Skip Stage 4-5 progress (jump-cut to validator-pass)
         - **DO NOT cut the contract-scrolling tail** — that's the load-bearing 10–15s
      3. Final 3 shots in order:
         - IDE opening the bootstrapped repo, sidebar populated, one Cmd+P search, one click on a screen card with chips visible
         - Open one backend `.md` contract in editor; scroll through `## Intent` + `## Side effects` slowly enough to read
         - Open one UI L3 `.md` contract; scroll through `## Intent` + `## Role` slowly enough to read
      4. Render at 1080p, h.264, no audio compression — the recording will play through the room PA

    - **Fallback plan if live demo glitches at recording time:**
      - Keep a previously-recorded "good" inset committed to .planning/phases/14-.../assets/ or external (Drive)
      - Worst case: skip the inset; verbal answer "yes, the skill we ship handles it; happy to demonstrate offline + show you the .contracts/ tree right now"

    - **Anti-patterns:**
      - DO NOT play the inset unprompted. The 4-beat script is the headline; this is a side-pocket answer.
      - DO NOT extend past 105s. Q&A is a clock burn; respect the room.
      - DO NOT skip the contract-scrolling tail to fit in 60s. The tail is the answer to "what did the contracts look like" — without it, the inset is "spinner → jump-cut → IDE" and the prose claim is unsupported.
      - DO NOT modify the locked 4-beat script to add a Phase 14 segment

    **14-UAT.md** (~200 lines) — End-to-end UAT runbook:

    [Sections unchanged from previous revision: Pre-UAT setup, Run 1 (cold cache + IDE smoke), Run 2 (warm cache hash-skip), Run 3 (selective re-derivation), edge cases, cost/time aggregate, acceptance gate. See previous revision for full content.]

    **Single addition to 14-UAT.md** — add to the "Acceptance gate for phase close" section:
    - **Prose Quality Gate (Task 2.5) approved** — Yang reviewed 5–6 derived contracts post-cold-run and scored them passing on decision specificity, cross-reference density, prose density. If failed, loop back to Plan 14-04 prompt iteration BEFORE recording (Task 4).
  </action>
  <verify>
    `wc -l .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md` returns ≥150
    `wc -l .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md` returns ≥150
    `grep -i "75.*105\|contract.*scroll\|## Intent.*visible\|## Side effects.*visible" .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md` matches (extended runtime + scrolling tail documented)
    `grep -i "Prose Quality Gate" .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md` matches (gate added to acceptance criteria)
    Both files reference the bootstrap-demo-target absolute path, the locked 4-beat script presentation-script.md (as do-not-modify), and a clear acceptance gate
  </verify>
  <done>
    Q&A inset runbook authored with verbatim talking points (75–105s including contract-scrolling tail), recording checklist, fallback plan, anti-patterns. 14-UAT.md authored with 3-run reproducibility checklist + edge cases + cost/time targets + acceptance gate INCLUDING the new Prose Quality Gate. Both docs ready for Task 2's live execution.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Yang executes 3-run UAT and verifies IDE-load smoke checklist</name>
  <action>Pause for Yang to run the full 3-run UAT per 14-UAT.md (cold cache, warm cache, selective re-derivation). After each run, capture cost / wall time / node count / validator source. After Run 1, open Contract IDE on bootstrap-demo-target and walk the 6-item IDE smoke checklist. Approve only if all 6 smoke items pass + hash-skip on Run 2 = $0 + selective re-derivation on Run 3 <$0.20.</action>
  <what-built>
    Full Phase 14 pipeline executed end-to-end against bootstrap-demo-target. Yang witnesses or executes the live 3-run UAT per .planning/phases/14-.../14-UAT.md.
  </what-built>
  <how-to-verify>
    [Unchanged from previous revision — execute 3 runs per 14-UAT.md, walk IDE smoke checklist on Run 1, verify hash-skip = $0 on Run 2, verify selective re-derivation <$0.20 on Run 3.]

    Approve only if:
    - All 3 runs succeed (no crash, no validator failure)
    - IDE smoke 100% green on Run 1
    - Run 2 cost = $0 (hash-skip working)
    - Run 3 cost <$0.20 (selective re-derivation working)
    - 3-run mean wall time <7 min
    - 3-run total cost <$3
  </how-to-verify>
  <resume-signal>Type "approved" + paste the 3-run cost / time table from 14-UAT.md, OR describe the failure</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.5: CHECKPOINT — Prose Quality Gate (Yang reviews 5–6 derived contracts post-cold-run)</name>
  <action>Pause for Yang to read 5–6 contracts from bootstrap-demo-target/.contracts/ post-cold-run (Run 1 output) and score each against Phase 9 exemplars (contract-ide-demo/.contracts/ambient/api-account-delete-001.md, lib-begin-account-deletion-001.md, a0000000.md, a1000000.md). Sample selection: at minimum 2 backend L3 (one API route, one lib function), 2 UI L3 (one page, one layout if present), 1 UI L4 (an atom inside a page), 1 flow contract. Score each on three axes; if any contract scores "generic" on any axis, loop back to Plan 14-04 to iterate the derive-body.txt prompt and re-run Stage 3, then re-review. Recording (Task 4) does NOT happen until this gate passes.</action>
  <what-built>
    Stage 3 + Stage 4 pipeline output on bootstrap-demo-target produced 20+ contracts (sidecars + flows). Plan 14-04 Task 3 already validated 3 sample derivations during the build of Stage 3. Now: validate the FULL pipeline output at scale — does prose quality hold across 20+ derivations the way it held on the 3 samples?

    This is the live-pipeline analogue of the Plan 14-04 sample gate — Stage 3's prompt was tuned on 3 samples; this gate confirms it generalized.
  </what-built>
  <how-to-verify>
    1. Pre-req: Task 2 (3-run UAT) Run 1 complete; bootstrap-demo-target/.contracts/ populated.

    2. Sample 5–6 contracts spanning kinds + levels:
       - 2 backend L3 (e.g., `api/account/route.ts` analogue, `lib/notes.ts` searchNotes function)
       - 2 UI L3 (e.g., `app/account/settings/page.tsx`, `app/notes/page.tsx`)
       - 1 UI L4 (an atom inside a page — e.g., the Danger Zone stub from bootstrap-demo-target/src/app/account/settings/page.tsx)
       - 1 flow contract (e.g., the auth-login flow if Stage 5a synthesized one)

    3. For each sample, open it side-by-side with the most-analogous Phase 9 exemplar:
       - backend API → `contract-ide-demo/.contracts/ambient/api-account-delete-001.md`
       - backend lib → `contract-ide-demo/.contracts/ambient/lib-begin-account-deletion-001.md`
       - UI L3 → `contract-ide-demo/.contracts/a0000000-0000-4000-8000-000000000000.md`
       - UI L4 → `contract-ide-demo/.contracts/a1000000-0000-4000-8000-000000000000.md`
       - flow → `contract-ide-demo/.contracts/flow-delete-account.md`

    4. Score each sample on three axes (PASS / GENERIC):

       **(a) Decision specificity** — does the body call out WHICH specific behavior was chosen vs alternatives?
       - PASS: "Soft-deletes by setting deletedAt rather than removing the row, preserving FK references in Invoice / AuditLog."
       - GENERIC: "Deletes the user from the database."

       **(b) Cross-reference density** — does the body link to other contracts or substrate by name?
       - PASS: "Calls beginAccountDeletion(userId) which: ..."
       - GENERIC: "Performs the deletion logic." (no name referenced)

       **(c) Prose density** — does it read like a human wrote it?
       - PASS: paragraph rhythm, varied sentence length, specific identifier names, parenthetical asides
       - GENERIC: bullet-list-only, repetitive sentence structure ("This function ... This function ...")

    5. Decision tree:
       - **All samples PASS on all 3 axes** → approve; proceed to Task 4 (recording).
       - **1–2 samples fail on 1 axis each** → minor edit to derive-body.txt (e.g., strengthen one rule), re-run Stage 3 only on the failing nodes (`derive-body.mjs <repo> --uuids=<failing-uuid>,...`), re-score those samples. Loop until pass.
       - **3+ samples fail OR any sample fails on 2+ axes** → larger prompt revision required. Loop back to Plan 14-04 for full prompt rework + add an additional exemplar; re-run full Stage 3; re-do this gate from scratch.
       - Hard ceiling: 3 iterations at this gate. If still failing after 3 loops, surface to planning — may indicate the codebase is genuinely too thin for rich derivation (in which case: enrich bootstrap-demo-target with more business logic in lib/).

    6. Document in SUMMARY.md:
       - Which 5–6 contracts were sampled (paths)
       - Score per axis per sample
       - Iteration count + edits made
       - Final decision: approved / loop / escalate

    Approve only if all samples pass all axes. Recording in Task 4 inherits this output; once recorded, prose-quality regressions can't be fixed without re-recording.

    Reject + iterate if any sample reads generic on any axis. The contract-scrolling tail in Task 4's recording (10–15s of `.md` files visible on screen) shows EXACTLY these contracts — generic prose here = generic prose on the projector.
  </how-to-verify>
  <resume-signal>Type "approved" + paste the per-sample scoring table + iteration count, OR describe which samples failed which axes and which prompt edit you're making</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Yang approves Q&A inset recording (75–105s including contract-scrolling tail)</name>
  <action>Pause for Yang to record the 75–105s Q&A inset per qa-inset-runbook.md and verify the recording end-to-end. Confirm length 75–105s; visible jump-cuts at slow LLM stages so the inset doesnt drag; final 3 shots in order (IDE with chips → backend contract .md scrolling → UI L3 contract .md scrolling); ## Intent and ## Side effects sections fully visible (readable, not just header glimpses) on the contract-scrolling tail; no on-camera errors; audio matches verbatim talking points; file is 1080p+ h.264 ready for projector playback. Verify the recording is queued in the demo machines mpv / QuickTime, NOT buried in Downloads/.</action>
  <what-built>
    Q&A inset recording per qa-inset-runbook.md — a 75–105s screen capture showing /codebase-to-contracts running on bootstrap-demo-target, the IDE displaying the bootstrapped repo with chips on a rendered iframe, AND a 10–15s tail scrolling through 2 actual contract .md files with ## Intent / ## Side effects / ## Role sections clearly visible.

    Recorded with QuickTime; held ready in the demo machine's mpv queue; ready to play if judges ask "does this work on any repo?" OR "what did the contracts actually look like?" during Q&A.
  </what-built>
  <how-to-verify>
    1. Watch the Q&A inset recording end-to-end (75–105s)
    2. Confirm:
       - [ ] Total length 75–105s (not 60s, not 2min)
       - [ ] Visible jump-cuts at slow LLM stages so the inset doesn't drag
       - [ ] Final 3 shots in order:
         - [ ] IDE displaying bootstrap-demo-target with sidebar populated + atoms visible on iframe
         - [ ] Backend contract `.md` opened in editor; ## Intent + ## Side effects sections fully visible (scrolled slowly enough to read, not just glimpsed)
         - [ ] UI L3 contract `.md` opened; ## Intent + ## Role sections fully visible
       - [ ] No on-camera errors / dialogs
       - [ ] Audio (if any) is clean / matches the verbatim talking points in qa-inset-runbook.md
       - [ ] File is 1080p+ h.264, ready for projector playback

    3. Verify it's discoverable on the demo machine:
       - File path noted in qa-inset-runbook.md
       - Pre-loaded in mpv / QuickTime queue (not buried in Downloads/)

    Approve if all items check; reject + re-record if not.
  </how-to-verify>
  <resume-signal>Type "approved" + recording file absolute path + duration in seconds + which 2 contract .md files are shown in the tail, OR describe what to re-record</resume-signal>
</task>

</tasks>

<verification>
**Plan-level checks:**

1. `cat .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md | grep -i "Q&A-only"` matches
2. `cat .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md | grep -iE "75.*105|contract.*scroll|## Intent.*visible"` matches (extended runtime + tail documented)
3. `cat .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md | grep -iE "3 times|Prose Quality Gate"` matches twice
4. `git diff .planning/demo/presentation-script.md` returns empty (locked 4-beat script unmodified)
5. After Task 2 + Task 2.5 + Task 4 checkpoints approved: cost / time / IDE-smoke metrics + prose-quality scoring table + recording metadata captured in SUMMARY.md
6. Recording duration is 75–105s (Task 4 confirms)
</verification>

<success_criteria>
1. Q&A inset runbook (qa-inset-runbook.md) authored with verbatim talking points, recording checklist (including 10–15s contract-scrolling tail), fallback plan, anti-patterns
2. 14-UAT.md authored with 3-run reproducibility checklist + edge cases + cost / time targets + acceptance gate INCLUDING Prose Quality Gate
3. Live 3-run UAT executed: all 3 runs succeed; IDE smoke 100% green on Run 1; hash-skip = $0 on Run 2; selective re-derivation <$0.20 on Run 3
4. 3-run mean wall time <7 min; 3-run total cost <$3
5. **Prose Quality Gate (Task 2.5) passed** — Yang scored 5–6 derived contracts as PASS on decision specificity, cross-reference density, prose density (with iteration loop back to Plan 14-04 if any failed)
6. Q&A inset recording produced (75–105s, 1080p, h.264) and Yang-approved; queued on demo machine; contract-scrolling tail readable
7. .planning/demo/presentation-script.md NOT modified — Phase 14 demo posture is Q&A-only inset (RESEARCH Open Question 4 ratified)
8. BOOTSTRAP-05 [proposed] satisfied — demo target bootstrapped end-to-end, prose-quality gated, recorded for Q&A
</success_criteria>

<output>
After completion, create `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-06-SUMMARY.md`. Document:
- 3-run UAT results table (cost / time / node count per run)
- IDE smoke checklist outcome on Run 1 (6 items × pass/fail)
- Hash-skip verification on Run 2 (saved cost: 100% target)
- Selective re-derivation on Run 3 (cost / time delta from cold)
- **Prose Quality Gate (Task 2.5) results:**
  - Which 5–6 contracts sampled (absolute paths)
  - Per-axis scoring table (sample × {decision specificity, cross-ref density, prose density})
  - Iteration count + summary of prompt edits made (if any)
  - Final decision (approved / how many loops)
- Validator source decision: did `contract-ide validate-repo` work, or did we use JS fallback? (Closes Open Question 7)
- Q&A inset recording: file path, duration, recording tool, which 2 contract .md files shown in the tail
- Confirm presentation-script.md is unchanged (close Open Question 4)
- Closeout note: with 14-06 done, Phase 14 closes. Update STATE.md + ROADMAP.md to mark Phase 14 Complete.

Then create `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-SUMMARY.md` (the phase-level summary). Document:
- BOOTSTRAP-01..05 status (proposed → ratified → satisfied)
- Skill artifacts (file list)
- Demo target artifact (path + commit hash + product name)
- Phase 14 demo affordance status: Q&A-only inset ready
- Carry-forward debt: any pitfalls hit during UAT
</output>
