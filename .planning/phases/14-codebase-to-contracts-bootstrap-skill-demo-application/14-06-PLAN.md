---
phase: 14-codebase-to-contracts-bootstrap-skill-demo-application
plan: 06
type: execute
wave: 5
depends_on:
  - 14-05
files_modified:
  - .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md
  - .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md
autonomous: false
requirements:
  - BOOTSTRAP-05  # [proposed] Demo target bootstrapped end-to-end live or recorded as a Phase 14 SC

must_haves:
  truths:
    - "User can run /codebase-to-contracts /Users/yang/lahacks/bootstrap-demo-target inside a Claude Code session and observe the 5-stage pipeline emit a complete .contracts/ tree in <5min for <$2"
    - "Bootstrapped bootstrap-demo-target loads in Contract IDE without manual cleanup — sidebar populates with areas, Cmd+P returns semantic hits, atom chips render on the rendered iframe (BABEL-01 from Phase 9 replicates on the new target)"
    - "Re-running the skill on the same repo with no source changes performs ZERO LLM calls (full hash-skip) — verifies BOOTSTRAP-* idempotency claim"
    - "A Q&A-only pre-recorded inset (60–90 seconds) exists showing the bootstrap operation, ready to play if judges ask 'does this work on any repo?'"
    - "The locked 4-beat presentation script (.planning/demo/presentation-script.md) is NOT MODIFIED — Phase 14's demo posture is Q&A-only inset, not a 5th beat"
    - "14-UAT.md runbook exists with reproducibility check: bootstrap runs 3 times in a row, all 3 succeed, total time per run within ±30s of mean"
  artifacts:
    - path: ".planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md"
      provides: "60–90s Q&A inset script — what to record, what to say, fallback if live demo glitches"
    - path: ".planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md"
      provides: "End-to-end UAT: 3-run reproducibility + IDE-load smoke + cost / time tracking"
  key_links:
    - from: "/Users/yang/lahacks/bootstrap-demo-target/.contracts/"
      to: "Contract IDE (sidebar tree + Cmd+P + L3 trigger view)"
      via: "User opens bootstrap-demo-target in IDE; sidebar populates from .contracts/; chips render on iframe"
      pattern: "data-contract-uuid"
    - from: "qa-inset-runbook.md"
      to: ".planning/demo/presentation-script.md"
      via: "Q&A inset is OUT-OF-BAND from the 4-beat script — locked script untouched"
      pattern: "Q&A-only"
---

<objective>
End-to-end execution + verification + recording of the bootstrap demo. Run the full 5-stage pipeline against bootstrap-demo-target three times in a row to verify reproducibility, capture cost / time metrics, confirm the bootstrapped repo loads in the actual IDE without manual cleanup, and record a 60–90s Q&A-only inset that's playable if judges ask "does this work on any repo?" — without modifying the locked 4-beat presentation script.

Purpose: BOOTSTRAP-05 — the demo posture per RESEARCH § "Demo target codebase selection" + "Validation of the orchestrator's hypothesis". Phase 14 is the operationalization of three pitch claims (Day-0 bootstrap, ingestion-period promise, code-based seeding for cold-start). Without a recorded demo, those claims remain narrative. With this plan, the skill IS the demo affordance.

Output: Q&A inset runbook (what to record, talking points, fallback plan), Phase 14 UAT runbook (3-run reproducibility checklist + IDE smoke + cost / time), and three checkpoints for Yang to verify the live + recorded artifacts before phase close.

This plan deliberately includes 2 checkpoints — (a) live end-to-end smoke with Yang witnessing, (b) Q&A inset recording approval. RESEARCH explicitly recommended Q&A-only posture; Yang ratifies.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-RESEARCH.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-01-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-02-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-03-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-04-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-05-SUMMARY.md

# The locked 4-beat script — NOT modified by Phase 14
@.planning/demo/presentation-script.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author qa-inset-runbook.md + 14-UAT.md (3-run reproducibility + IDE smoke)</name>
  <files>
    .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md
    .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md
  </files>
  <action>
    **qa-inset-runbook.md** (~150 lines) — Q&A inset script + recording instructions:

    Sections:
    - **Purpose:** when/whether to play this inset. Triggers: judge asks "can this work on any repo?", "what about other codebases?", "is this just for the demo repo?". Hold the inset ready in the demo machine's mpv / QuickTime, queued. If unprompted, do NOT play.
    - **Runtime target:** 60–90 seconds. Anything longer dilutes Q&A flow.
    - **Recording setup:**
      - Demo machine: contract-ide running on macOS, screen-recorded via QuickTime
      - Pre-step: ensure /Users/yang/lahacks/bootstrap-demo-target/.contracts/ does NOT exist (`rm -rf` if present from prior runs). Same for contract-uuid-plugin/. Same for next.config.ts BOOTSTRAP-INSERT block.
      - In iTerm: navigate to bootstrap-demo-target, open Claude Code session
    - **Script (verbatim talking points):**
      - "Most teams have repos already. Here's what 'point at any repo' looks like."
      - "I open Claude Code in an existing Next.js notes app. No contracts. No Contract IDE setup."
      - "I type: `/codebase-to-contracts`"
      - "Skill kicks off. Stage 1: enumerates 25 source files, classifies them. Stage 2 derives frontmatter — 25 calls in parallel, ~30 seconds. Stage 3 derives bodies. Stage 4 aligns each L4 atom to its JSX element. Stage 5 synthesizes flows, installs the Babel plugin, validates everything. Atomic — if anything fails, no `.contracts/` gets written."
      - "Total: 4 minutes. $1.40."
      - "Open the IDE, point it at this repo. Sidebar populates. Cmd+P searches by intent. Click a screen — the iframe renders, atom chips overlay the components."
      - "Same operation, different repo."
    - **Recording checklist:**
      1. Reset state (rm -rf .contracts, contract-uuid-plugin, restore next.config.ts)
      2. Time budget: 60–90s. Cuts: skip the 30s of Stage 2 LLM streaming (jump-cut to "30s later"); skip 60s of Stage 3 (jump-cut to "1min later"); skip Stage 4-5 progress (jump-cut to validator-pass).
      3. Final shot: IDE opening the bootstrapped repo, sidebar populated, one Cmd+P search, one click on a screen card with chips visible
      4. Render at 1080p, h.264, no audio compression — the recording will play through the room PA
    - **Fallback plan if live demo glitches at recording time:**
      - Keep a previously-recorded "good" inset committed to .planning/phases/14-.../assets/ or external (Drive)
      - Worst case: skip the inset; verbal answer "yes, the skill we ship handles it; happy to demonstrate offline"
    - **Anti-patterns:**
      - DO NOT play the inset unprompted. The 4-beat script is the headline; this is a side-pocket answer.
      - DO NOT extend to 2+ minutes. Q&A is a clock burn; respect the room.
      - DO NOT modify the locked 4-beat script to add a Phase 14 segment (RESEARCH Open Question 4 — Yang already opted Q&A-only at the planner-level scope decision; this plan codifies it)

    **14-UAT.md** (~200 lines) — End-to-end UAT runbook:

    Sections:
    - **Pre-UAT setup:**
      - Verify all 14-01..14-05 plans have SUMMARY.md files committed (gate)
      - Verify .agents/skills/codebase-to-contracts/ tree complete + tests pass (full project test suite)
      - Verify bootstrap-demo-target builds (`cd bootstrap-demo-target && pnpm build` exits 0) — known pre-state
      - Verify Claude Code is logged in + has `claude` CLI on PATH + has the skill discoverable

    - **Run 1 — cold cache, full pipeline:**
      1. Reset: `rm -rf bootstrap-demo-target/.contracts bootstrap-demo-target/contract-uuid-plugin && git -C bootstrap-demo-target checkout next.config.ts`
      2. Open Claude Code in `/Users/yang/lahacks/bootstrap-demo-target/`
      3. Type: `/codebase-to-contracts` (no arg — defaults to cwd)
      4. Observe pipeline. Stop the clock when summary table prints.
      5. Record:
         - Total wall time (target: <5 min)
         - LLM cost from claude session footer (target: <$2)
         - Stage breakdown (Stage 1 / 2 / 3 / 4 / 5a / 5b times)
         - Final node count (target: ≥20 sidecars + ≥3 flow contracts)
         - Validator outcome (`source: ide-binary` if `contract-ide` CLI exists; else `js-fallback` with warning)
      6. Open Contract IDE → File → Open Repo → bootstrap-demo-target
      7. Verify:
         - [ ] Sidebar populates with areas (Account / Notes / Auth / Checkout)
         - [ ] Cmd+P returns at least 3 semantic hits for the query "delete account"
         - [ ] Click a UI L3 atom in sidebar → L3 trigger view renders the iframe at the screen's route
         - [ ] Atom chips overlay the iframe components (BABEL-01 replication on new target)
         - [ ] Click an atom chip → inspector slides in
         - [ ] No startup errors in Tauri console (especially no jsx_align_validator.rs / backend_section_validator.rs failures)

    - **Run 2 — warm cache, hash-skip verification:**
      1. Do NOT reset — leave .contracts/ and plugin in place
      2. Type `/codebase-to-contracts` again
      3. Verify:
         - [ ] Pre-flight detects existing .contracts/, prompts user (resume / restart / abort) — choose RESUME
         - [ ] Stage 2: hash-skip count is N (= total nodes); derived count is 0
         - [ ] Stage 3: same, 0 derivations
         - [ ] Stage 4: all atoms still aligned (no JSX-search reruns since frontmatter didn't change)
         - [ ] Stage 5b: validator passes; no .md sidecar rewritten
         - [ ] Total cost: $0.00 (the hash-skip claim from RESEARCH Pitfall 1 is the load-bearing test)
         - [ ] Total time: <30s

    - **Run 3 — selective re-derivation:**
      1. Modify ONE source file: `cd bootstrap-demo-target && echo '// trivial change' >> src/app/account/settings/page.tsx`
      2. Type `/codebase-to-contracts` (resume mode)
      3. Verify:
         - [ ] Stage 2: hash-skip = N-1; derived = 1 (only the modified file's frontmatter)
         - [ ] Stage 3: derived = 1
         - [ ] Stage 4: realigns the modified file's atoms (assuming candidate ranges shift)
         - [ ] Stage 5: emits 1 new .md sidecar (overwriting); plugin install no-op (BOOTSTRAP-INSERT block already present, idempotent replace)
         - [ ] IDE re-loads the repo → modified atom's contract reflects the changed source
         - [ ] Total cost: <$0.20 (1 file × 2 stages × ~$0.10/stage)
         - [ ] Total time: <60s

    - **Edge case checklist:**
      - [ ] Run with `BOOTSTRAP_TEST_MODE=1` env var — confirm no real claude calls fire
      - [ ] Delete a source file between runs — confirm Stage 1 prunes its node from nodes.json AND the corresponding .md sidecar is removed from .contracts/ on emit (or stays there with a "stale" flag — TBD: surface to Yang if not implemented)
      - [ ] Bootstrap-demo-target's `pnpm build` still works after plugin install (BABEL-01 replication)

    - **Cost / time aggregate (target):**
      - 3-run mean cost: <$3 total ($1.40 cold + $0 warm + $0.10 selective ≈ $1.50; budget for double per Pitfall 5)
      - 3-run mean wall time: <7 min total (4 + 0.5 + 1 ≈ 5.5; budget for double)

    - **Acceptance gate for phase close:**
      - All 3 runs succeed
      - IDE smoke checklist 100% green on Run 1
      - Hash-skip on Run 2 saves >95% of LLM calls
      - Selective re-derivation on Run 3 only re-derives the touched file
      - Q&A inset recording is approved by Yang (Task 3 checkpoint)
      - Phase 14 SUMMARY.md committed
  </action>
  <verify>
    `wc -l .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md` returns ≥120
    `wc -l .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md` returns ≥150
    Both files reference the bootstrap-demo-target absolute path, the locked 4-beat script presentation-script.md (as do-not-modify), and a clear acceptance gate
  </verify>
  <done>
    Q&A inset runbook authored with verbatim talking points, recording checklist, fallback plan, anti-patterns. 14-UAT.md authored with 3-run reproducibility checklist (cold cache + warm cache + selective re-derivation), edge cases, cost / time targets, and acceptance gate. Both docs ready for Task 2's live execution.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Yang executes 3-run UAT and verifies IDE-load smoke checklist</name>
  <action>Pause for Yang to run the full 3-run UAT per 14-UAT.md (cold cache, warm cache, selective re-derivation). After each run, capture cost / wall time / node count / validator source. After Run 1, open Contract IDE on bootstrap-demo-target and walk the 6-item IDE smoke checklist (sidebar populated, Cmd+P semantic hits, L3 trigger view + iframe + atom chips, atom chip click → inspector, no startup errors). Approve only if all 6 smoke items pass + hash-skip on Run 2 = $0 + selective re-derivation on Run 3 <$0.20.</action>
  <what-built>
    Full Phase 14 pipeline executed end-to-end against bootstrap-demo-target:
    - Skill scaffold (Plan 14-01)
    - Demo target codebase (Plan 14-02)
    - Stages 1-5 pipeline (Plans 14-03/04/05)
    - End-to-end UAT runbook (Task 1 of this plan)

    Now: Yang witnesses or executes the live 3-run UAT per .planning/phases/14-.../14-UAT.md. Captures cost / time per run, IDE smoke verification, hash-skip verification, selective-re-derivation verification.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md` — execute the 3 runs in order
    2. Run 1 (cold cache):
       - `rm -rf /Users/yang/lahacks/bootstrap-demo-target/.contracts /Users/yang/lahacks/bootstrap-demo-target/contract-uuid-plugin && git -C /Users/yang/lahacks/bootstrap-demo-target checkout next.config.ts`
       - Open Claude Code in `/Users/yang/lahacks/bootstrap-demo-target/`
       - Type `/codebase-to-contracts` and let it run to completion
       - Record: total wall time, LLM cost, node count, validator source
       - Open Contract IDE → File → Open Repo → bootstrap-demo-target
       - Walk the 6-item IDE smoke checklist in 14-UAT.md
    3. Run 2 (warm cache): re-type `/codebase-to-contracts`. Verify hash-skip = N, derived = 0, cost = $0
    4. Run 3 (selective): modify one .tsx, re-run. Verify exactly 1 file re-derived
    5. Capture: open issues if ANY of the 6 IDE smoke items fail; surface validator-source choice (ide-binary vs js-fallback) for Open Question 7 ratification

    Approve only if:
    - All 3 runs succeed (no crash, no validator failure)
    - IDE smoke 100% green on Run 1
    - Run 2 cost = $0 (hash-skip working)
    - Run 3 cost <$0.20 (selective re-derivation working)
    - 3-run mean wall time <7 min
    - 3-run total cost <$3

    Reject if:
    - Any run produces a partial .contracts/ tree (.staging/ remains alongside .contracts/)
    - IDE shows startup errors
    - Hash-skip writes new sidecars when source unchanged
    - Cost overruns by >2× the target
  </how-to-verify>
  <resume-signal>Type "approved" + paste the 3-run cost / time table from 14-UAT.md, OR describe the failure (which run, which check, which surface) + decide rerun vs gap-closure plan</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — Yang approves Q&A inset recording (60–90s)</name>
  <action>Pause for Yang to record the 60–90s Q&A inset per qa-inset-runbook.md and verify the recording end-to-end. Confirm length 60–90s (not 30s, not 2min); visible jump-cuts at slow LLM stages so the inset doesnt drag; final shot is IDE displaying bootstrap-demo-target with chips on iframe; no on-camera errors; audio matches verbatim talking points; file is 1080p+ h.264 ready for projector playback. Verify the recording is queued in the demo machines mpv / QuickTime, NOT buried in Downloads/.</action>
  <what-built>
    Q&A inset recording per qa-inset-runbook.md — a 60–90s screen capture showing /codebase-to-contracts running on bootstrap-demo-target, ending on the IDE displaying the bootstrapped repo with chips on a rendered iframe.

    Recorded with QuickTime (or chosen tool); held ready in the demo machine's mpv queue; ready to play if judges ask "does this work on any repo?" during Q&A.
  </what-built>
  <how-to-verify>
    1. Watch the Q&A inset recording end-to-end (60–90s)
    2. Confirm:
       - [ ] Total length 60–90s (not 30s, not 2min)
       - [ ] Visible jump-cuts at slow LLM stages (Stage 2 / Stage 3) so the inset doesn't drag
       - [ ] Final shot: IDE displaying bootstrap-demo-target, sidebar populated, atoms visible on iframe
       - [ ] No on-camera errors / dialogs
       - [ ] Audio (if any) is clean / matches the verbatim talking points in qa-inset-runbook.md
       - [ ] File is 1080p+ h.264, ready for projector playback

    3. Verify it's discoverable on the demo machine:
       - File path noted in qa-inset-runbook.md
       - Pre-loaded in mpv / QuickTime queue (not buried in Downloads/)

    Approve if all 6 items check; reject + re-record if not.
  </how-to-verify>
  <resume-signal>Type "approved" + recording file absolute path + duration in seconds, OR describe what to re-record</resume-signal>
</task>

</tasks>

<verification>
**Plan-level checks:**

1. `cat .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/qa-inset-runbook.md | grep -i "Q&A-only"` matches (posture explicitly stated)
2. `cat .planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-UAT.md | grep -i "3 times"` matches (reproducibility check stated)
3. `git diff .planning/demo/presentation-script.md` returns empty (locked 4-beat script unmodified)
4. After Task 2 + Task 3 checkpoints approved: cost / time / IDE-smoke metrics captured in SUMMARY.md
5. Recording file path noted in qa-inset-runbook.md after Task 3
</verification>

<success_criteria>
1. Q&A inset runbook (qa-inset-runbook.md) authored with verbatim talking points, recording checklist, fallback plan, anti-patterns
2. 14-UAT.md authored with 3-run reproducibility checklist + edge cases + cost / time targets + acceptance gate
3. Live 3-run UAT executed: all 3 runs succeed; IDE smoke 100% green on Run 1; hash-skip = $0 on Run 2; selective re-derivation <$0.20 on Run 3
4. 3-run mean wall time <7 min; 3-run total cost <$3
5. Q&A inset recording produced (60–90s, 1080p, h.264) and Yang-approved; queued on demo machine
6. .planning/demo/presentation-script.md NOT modified — Phase 14 demo posture is Q&A-only inset (RESEARCH Open Question 4 ratified)
7. BOOTSTRAP-05 [proposed] satisfied — demo target bootstrapped end-to-end, recorded for Q&A
</success_criteria>

<output>
After completion, create `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-06-SUMMARY.md`. Document:
- 3-run UAT results table (cost / time / node count per run)
- IDE smoke checklist outcome on Run 1 (6 items × pass/fail)
- Hash-skip verification on Run 2 (saved cost: 100% target)
- Selective re-derivation on Run 3 (cost / time delta from cold)
- Validator source decision: did `contract-ide validate-repo` work, or did we use JS fallback? (Closes Open Question 7 — log Yang's call on whether to add CLI subcommand to IDE binary as v2 work.)
- Q&A inset recording: file path, duration, recording tool used
- Confirm presentation-script.md is unchanged (close Open Question 4)
- Closeout note: with 14-06 done, Phase 14 closes. Update STATE.md + ROADMAP.md to mark Phase 14 Complete.

Then create `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-SUMMARY.md` (the phase-level summary aggregating all plans). Document:
- BOOTSTRAP-01..05 status (proposed → ratified → satisfied)
- Skill artifacts (file list)
- Demo target artifact (path + commit hash)
- Phase 14 demo affordance status: Q&A-only inset ready
- Carry-forward debt: any pitfalls hit during UAT (Pitfall 4 idempotency on partial failures? Pitfall 5 cost overruns? Pitfall 7 validator parity?) — surface for v2 follow-up if any
</output>
