---
phase: 14-codebase-to-contracts-bootstrap-skill-demo-application
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bootstrap-demo-target/package.json
  - bootstrap-demo-target/tsconfig.json
  - bootstrap-demo-target/next.config.ts
  - bootstrap-demo-target/prisma/schema.prisma
  - bootstrap-demo-target/prisma/seed.ts
  - bootstrap-demo-target/src/app/layout.tsx
  - bootstrap-demo-target/src/app/page.tsx
  - bootstrap-demo-target/src/app/notes/page.tsx
  - bootstrap-demo-target/src/app/notes/[id]/page.tsx
  - bootstrap-demo-target/src/app/notes/new/page.tsx
  - bootstrap-demo-target/src/app/login/page.tsx
  - bootstrap-demo-target/src/app/account/settings/page.tsx
  - bootstrap-demo-target/src/app/checkout/page.tsx
  - bootstrap-demo-target/src/app/api/auth/login/route.ts
  - bootstrap-demo-target/src/app/api/auth/logout/route.ts
  - bootstrap-demo-target/src/app/api/notes/route.ts
  - bootstrap-demo-target/src/app/api/notes/[id]/route.ts
  - bootstrap-demo-target/src/app/api/checkout/route.ts
  - bootstrap-demo-target/src/app/api/webhooks/stripe/route.ts
  - bootstrap-demo-target/src/app/api/account/route.ts
  - bootstrap-demo-target/src/lib/db.ts
  - bootstrap-demo-target/src/lib/auth.ts
  - bootstrap-demo-target/src/lib/stripe.ts
  - bootstrap-demo-target/src/lib/email.ts
  - bootstrap-demo-target/src/lib/notes.ts
  - bootstrap-demo-target/README.md
  - bootstrap-demo-target/.env.example
autonomous: false
requirements:
  - BOOTSTRAP-05  # [proposed] Demo target scaffold exists; bootstrap pipeline can run end-to-end against it

must_haves:
  truths:
    - "bootstrap-demo-target/ directory contains a runnable Next.js + Prisma + TypeScript app distinct from contract-ide-demo"
    - "App has 50–150 .tsx files, 10–20 API routes, 3–8 Prisma models, ≥1 external integration (Stripe + email)"
    - "App contains NO .contracts/ directory at start of Phase 14 demo (scaffold is the input; bootstrap is the operation)"
    - "App is a recognizable product surface (notes app with auth + checkout for Pro tier) — not a hello-world or a low-code generator"
    - "App matches contract-ide-demo's stack so Phase 9 plumbing (validators, Babel plugin format) reuses 1:1"
  artifacts:
    - path: "bootstrap-demo-target/package.json"
      provides: "Next.js + Prisma + Stripe + auth dependency manifest"
      contains: "next"
    - path: "bootstrap-demo-target/prisma/schema.prisma"
      provides: "3-5 Prisma models for the notes-app domain"
      contains: "model User"
    - path: "bootstrap-demo-target/next.config.ts"
      provides: "Vanilla Next.js config — NO contract-uuid-plugin yet (Plan 14-05 installs it)"
    - path: "bootstrap-demo-target/src/app/account/settings/page.tsx"
      provides: "Account settings surface — non-trivial flow for the bootstrap demo's hero shot"
  key_links:
    - from: "bootstrap-demo-target/src/app/api/checkout/route.ts"
      to: "bootstrap-demo-target/src/lib/stripe.ts"
      via: "import + call site"
      pattern: "import.*from.*lib/stripe"
    - from: "bootstrap-demo-target/src/app/api/auth/login/route.ts"
      to: "bootstrap-demo-target/src/lib/auth.ts + src/lib/db.ts"
      via: "import + call site"
      pattern: "import.*from.*lib/(auth|db)"
    - from: "bootstrap-demo-target/prisma/schema.prisma"
      to: "bootstrap-demo-target/src/lib/db.ts (via Prisma Client)"
      via: "Prisma generate"
      pattern: "PrismaClient"
---

<objective>
Scaffold the second of Phase 14's two coupled deliverables: a small, real, runnable Next.js + Prisma + TypeScript notes app that is the BOOTSTRAP TARGET — a separate codebase the skill is run against in front of judges to demonstrate "point this at any repo." This is the input; Plans 14-03/04/05 produce the `.contracts/` tree from it; Plan 14-06 records the end-to-end bootstrap demo.

Purpose: BOOTSTRAP-05 — the demo posture (per RESEARCH § "Demo target codebase selection") strongly recommends a Yang-built micro-SaaS over an open-source candidate. Yang controls the codebase, can guarantee no surprises during demo prep, can plant 1–2 deliberate "interesting" patterns the skill will surface (e.g., a `lib/stripe.ts` adapter that the skill must classify as `kind: external` not `kind: lib`). Open-source candidates add a vetting research step that hasn't happened.

Output: A `bootstrap-demo-target/` directory at the repo root (sibling to `contract-ide-demo/` and `contract-ide/`), containing a self-contained Next.js 15+ App Router + Prisma + Stripe-checkout + minimal-auth notes app. ~10 page.tsx + 8 route.ts + 4 prisma models + 4 lib files. App MUST run (`pnpm dev`) and have a README documenting the dev setup. App MUST NOT have a `.contracts/` directory (that's the bootstrap output, produced by Plans 14-03/04/05).

Scope decision (Yang ratification at Task 1 checkpoint): the demo target codebase choice is a load-bearing call. RESEARCH recommends Yang-built micro-SaaS (notes app suggested). Alternatives: (a) existing Yang repo from `/Users/yang/lahacks/`, (b) small open-source SaaS starter, (c) second copy of contract-ide-demo without seeded contracts. Task 1 surfaces these for Yang to pick before any scaffolding work happens.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/CANVAS-PURPOSE.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-RESEARCH.md
@.planning/demo/presentation-script.md

# Reference stack — bootstrap-demo-target should mirror this stack 1:1
@contract-ide-demo/package.json
@contract-ide-demo/next.config.ts
@contract-ide-demo/prisma/schema.prisma

# DO NOT model after the contracts — those are seeded; bootstrap target starts WITHOUT contracts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: CHECKPOINT — Yang ratifies demo target codebase choice</name>
  <action>Pause for Yang to select option-a (Yang-built micro-SaaS), option-b (existing repo + path), option-c (open-source candidate + URL), or option-d (second contract-ide-demo copy). The decision determines the scaffold scope in Task 2 — option-a builds a fresh notes app; option-b/c/d skip scaffolding and pivot subsequent plans (14-03/04/05) to the chosen path. Surface the four trade-offs verbatim from the options block below; do not summarize.</action>
  <decision>Demo target codebase choice — Yang-built micro-SaaS vs alternative</decision>
  <context>
    RESEARCH § "Demo target codebase selection" recommends a Yang-built notes/time-tracker/expense-splitter app (~50–150 .tsx, ~10 API routes, 3–8 Prisma models, ≥1 external integration like Stripe). Trade-off: 1–2h of upstream scaffold vs control over surprises. Open-source alternatives (vercel/commerce too large; small starters require vetting) introduce demo-day risk.

    The Beat 1 demo on `contract-ide-demo` shows the substrate value (delete-account flow). Phase 14's demo on a SECOND repo shows the bootstrap value (any repo → contracts in <5min). The two demos MUST be on different repos so generalization is visibly demonstrated.

    Yang's call. The plan continues only after Yang picks; subsequent tasks scaffold accordingly.
  </context>
  <options>
    <option id="option-a">
      <name>Yang-built notes-app micro-SaaS (RESEARCH primary recommendation)</name>
      <pros>
        - Yang controls the codebase end-to-end; can plant deliberate patterns (e.g., lib/stripe.ts as external classifier test)
        - Stack matches contract-ide-demo 1:1 (Phase 9 plumbing reuses)
        - Size is dialable (~10 page.tsx + 8 route.ts + 4 prisma models)
        - 1–2h scaffold time; total Phase 14 plan execution time goes up by ~2h but no other-repo vetting risk
        - Pre-recordable on a fixed commit hash; demo reproducibility is guaranteed
      </pros>
      <cons>
        - 1–2h of upstream work in addition to the 5-stage skill build
        - "We built our own demo" angle is less credible than "any random SaaS works" — but the bootstrap demo is fundamentally a demo of an OPERATION, not of a codebase, so the codebase identity matters less than the operation's success
      </cons>
    </option>
    <option id="option-b">
      <name>Existing Yang repo (e.g., one already in /Users/yang/lahacks/)</name>
      <pros>
        - Zero scaffold time
        - "Real codebase" angle is intrinsic
      </pros>
      <cons>
        - Requires upstream validation: is it Next.js + Prisma + TS? Right size? At least one external integration?
        - May contain contract-ide-demo or contract-ide itself (which already have .contracts/ — the bootstrap is a no-op or a re-derivation, NOT a generalization demo)
        - Yang names the candidate at this checkpoint; planner adds a small validation step in Task 2 if so
      </cons>
    </option>
    <option id="option-c">
      <name>Open-source small SaaS starter (e.g., Documenso clone, micro-fork of cal.com)</name>
      <pros>
        - Genuine "any random open-source repo" provenance
        - Zero scaffold time
      </pros>
      <cons>
        - Requires research-and-vet pass (~1–2h) — license fit, size, integrations, bootstrap behavior
        - Risk: the repo turns out unsuitable mid-Phase-14 (>150 files, missing integrations, wrong stack); Plan 14-02 has to start over
        - License clarity for demo recording
      </cons>
    </option>
    <option id="option-d">
      <name>Second copy of contract-ide-demo WITHOUT seeded .contracts/</name>
      <pros>
        - Zero scaffold time (clone + delete .contracts/)
        - A/B comparison of skill output vs the seeded hand-crafted contracts is internally informative as a QA gate
      </pros>
      <cons>
        - Loses the "any repo" narrative — same product, same surface, no generalization shown
        - Useful as an internal QA harness, but the demo target should be visibly different from contract-ide-demo
      </cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b (and name the existing repo path), option-c (and name the candidate repo URL), or option-d. Task 2 onward scaffolds based on this.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold the chosen demo target — Next.js + Prisma + auth + Stripe checkout + email + notes-app domain</name>
  <files>
    bootstrap-demo-target/package.json
    bootstrap-demo-target/tsconfig.json
    bootstrap-demo-target/next.config.ts
    bootstrap-demo-target/prisma/schema.prisma
    bootstrap-demo-target/prisma/seed.ts
    bootstrap-demo-target/src/app/layout.tsx
    bootstrap-demo-target/src/app/page.tsx
    bootstrap-demo-target/src/app/notes/page.tsx
    bootstrap-demo-target/src/app/notes/[id]/page.tsx
    bootstrap-demo-target/src/app/notes/new/page.tsx
    bootstrap-demo-target/src/app/login/page.tsx
    bootstrap-demo-target/src/app/account/settings/page.tsx
    bootstrap-demo-target/src/app/checkout/page.tsx
    bootstrap-demo-target/src/app/api/auth/login/route.ts
    bootstrap-demo-target/src/app/api/auth/logout/route.ts
    bootstrap-demo-target/src/app/api/notes/route.ts
    bootstrap-demo-target/src/app/api/notes/[id]/route.ts
    bootstrap-demo-target/src/app/api/checkout/route.ts
    bootstrap-demo-target/src/app/api/webhooks/stripe/route.ts
    bootstrap-demo-target/src/app/api/account/route.ts
    bootstrap-demo-target/src/lib/db.ts
    bootstrap-demo-target/src/lib/auth.ts
    bootstrap-demo-target/src/lib/stripe.ts
    bootstrap-demo-target/src/lib/email.ts
    bootstrap-demo-target/src/lib/notes.ts
    bootstrap-demo-target/README.md
    bootstrap-demo-target/.env.example
  </files>
  <action>
    **IF option-a (Yang-built micro-SaaS, expected default):**

    Scaffold a notes-app micro-SaaS in `bootstrap-demo-target/` (sibling to `contract-ide/` and `contract-ide-demo/`). First confirm `ls /Users/yang/lahacks/` to verify the parent directory; create `bootstrap-demo-target/` with `mkdir -p`.

    Initialize via `cd /Users/yang/lahacks && pnpm create next-app@latest bootstrap-demo-target --typescript --tailwind --app --src-dir --no-eslint --no-import-alias` (interactive — supply: TypeScript yes, App Router yes, src/ yes, Tailwind yes, ESLint no for hackathon speed, default import alias). If `pnpm create next-app` is non-interactive in CI, fall back to `pnpm create next-app@latest bootstrap-demo-target --typescript --tailwind --app --src-dir --no-eslint --use-pnpm --yes`.

    Add deps: `cd bootstrap-demo-target && pnpm add prisma @prisma/client stripe lucia @lucia-auth/adapter-prisma resend zod`. (Lucia: lightweight auth library, mirrors contract-ide-demo's auth pattern. Resend: email sending — counts as an external integration alongside Stripe.)

    `pnpm prisma init --datasource-provider sqlite` then write `prisma/schema.prisma` with these 4 models:
    - `User` (id, email unique, hashedPassword, createdAt, deletedAt nullable, stripeCustomerId nullable)
    - `Session` (id, userId FK, expiresAt) — Lucia session
    - `Note` (id, userId FK, title, body, createdAt, updatedAt, archivedAt nullable)
    - `Subscription` (id, userId FK unique, stripeSubscriptionId, status, currentPeriodEnd)

    Run `pnpm prisma db push` to materialize the SQLite db, then `pnpm prisma generate`. Write `prisma/seed.ts` to seed 1 demo user with 3 notes.

    **Pages (10 .tsx — keeps the bootstrap demo timing under 5min on stage per Pitfall 8):**

    - `src/app/layout.tsx` — root layout, sidebar nav (Notes / Account / Logout)
    - `src/app/page.tsx` — landing page redirect (logged-in → /notes; logged-out → /login)
    - `src/app/notes/page.tsx` — list of user's notes (server component reading db)
    - `src/app/notes/[id]/page.tsx` — single note view + edit form
    - `src/app/notes/new/page.tsx` — new-note form
    - `src/app/login/page.tsx` — email + password form
    - `src/app/account/settings/page.tsx` — account settings (matches `contract-ide-demo/src/app/account/settings/page.tsx` shape so Yang-familiarity carries over). Sections: Profile (email, name), Subscription (current plan + Manage button), Danger Zone (placeholder — empty so the bootstrap demo's last move can be "watch the skill emit a sectioned L4 atom for an empty section")
    - `src/app/checkout/page.tsx` — Stripe Checkout entry, Pro plan upsell

    **API routes (8 route.ts):**

    - `src/app/api/auth/login/route.ts` — POST email+password, sets session cookie
    - `src/app/api/auth/logout/route.ts` — POST clears session
    - `src/app/api/notes/route.ts` — GET list / POST create
    - `src/app/api/notes/[id]/route.ts` — GET / PATCH update / DELETE
    - `src/app/api/checkout/route.ts` — POST creates Stripe Checkout session
    - `src/app/api/webhooks/stripe/route.ts` — POST handles `checkout.session.completed` + `customer.subscription.deleted`
    - `src/app/api/account/route.ts` — DELETE soft-deletes account (mirrors contract-ide-demo's pattern; sets User.deletedAt; cancels subscription via Stripe)

    **Lib (5 files):**

    - `src/lib/db.ts` — singleton PrismaClient export
    - `src/lib/auth.ts` — Lucia setup + getSession() helper
    - `src/lib/stripe.ts` — Stripe client init, helpers (createCheckoutSession, archiveCustomer)
    - `src/lib/email.ts` — Resend client + sendDeletionConfirmationEmail() helper (mirrors contract-ide-demo's lib pattern)
    - `src/lib/notes.ts` — Note CRUD logic (encapsulates Prisma calls)

    **Files-count target: ~25 source files** (10 pages + 8 routes + 5 lib + layout + 1-2 utils). This is BELOW the lower bound of the RESEARCH spec (50–150 .tsx) — INTENTIONAL. RESEARCH numbers were for the worst case; for a 4-min demo, smaller is better (Pitfall 8 — bootstrap timing). 25 source files keeps the bootstrap pipeline under 4 min on stage even with cold cache.

    **Critical constraints:**
    - DO NOT create `.contracts/` directory anywhere under `bootstrap-demo-target/`. The bootstrap is the operation that creates it.
    - DO NOT install `@babel/parser` / `@babel/traverse` / `js-yaml` in dependencies — those are runtime deps the contract-uuid-plugin needs, but the plugin isn't installed yet (Plan 14-05 does that). They'll appear in the lockfile transitively if needed.
    - DO NOT import or reference any `data-contract-uuid` in the source. The Babel plugin (Plan 14-05) injects them at build time.
    - DO put non-trivial business logic in lib/ (e.g., lib/notes.ts has a `searchNotes(userId, query)` function with a real Prisma query). The skill needs SOMETHING to derive interesting `## Intent` from; a hello-world `lib/notes.ts` defeats the demo.
    - Stripe and email integrations: use `process.env.STRIPE_SECRET_KEY` / `process.env.RESEND_API_KEY` per `.env.example`; runtime checks gracefully degrade if env vars absent (return mock objects in dev).

    **README.md:** ~50 lines documenting:
    - What this app is (notes app for the bootstrap demo)
    - Stack: Next.js 15 + Prisma + Stripe + Lucia + Resend
    - Setup: `pnpm install && pnpm prisma db push && pnpm prisma db seed && pnpm dev`
    - Demo user: `demo@example.com` / `password123`
    - "This repo is the INPUT to the codebase-to-contracts skill. After running `/codebase-to-contracts /path/to/bootstrap-demo-target`, a `.contracts/` tree appears."

    **.env.example:** placeholders for `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `RESEND_API_KEY`. Document defaults: `DATABASE_URL="file:./dev.db"`.

    Run `pnpm install && pnpm prisma db push && pnpm prisma generate` to verify the scaffold compiles. Then `pnpm tsc --noEmit` clean.

    **IF option-b (existing Yang repo):**

    Yang named the path at the checkpoint. Validate: `ls <path> | grep -E 'next.config|package.json'` and confirm `package.json` has `"next"` in deps. Confirm absence of `.contracts/` (`ls <path>/.contracts/ 2>&1 | grep "No such"` should match). Write a SHORTENED README.md in the chosen repo (or a sibling NOTE.md if writing to a third-party repo isn't appropriate) documenting "this repo is the bootstrap demo target." Skip the scaffold work entirely. Surface a NOTE in the SUMMARY.md that the chosen repo bypassed scaffold; subsequent plans (14-03+) operate on `<repo-path>` instead of `bootstrap-demo-target/`.

    **IF option-c (open-source SaaS starter):**

    Yang named the candidate URL at checkpoint. Clone via `git clone <url> bootstrap-demo-target` then validate the same way as option-b. Write README.md noting the upstream + commit hash. Same pivot for subsequent plans.

    **IF option-d (second contract-ide-demo copy):**

    `cp -r contract-ide-demo bootstrap-demo-target && rm -rf bootstrap-demo-target/.contracts && rm -rf bootstrap-demo-target/contract-uuid-plugin && cd bootstrap-demo-target && rm next.config.ts.bak 2>/dev/null` — strip the seeded artifacts. Edit next.config.ts to remove the contract-uuid-plugin webpack hook. Edit package.json to remove `contract-uuid-plugin` workspace dep. Write README.md noting "this is contract-ide-demo without its seeded artifacts; bootstrap demo will re-derive what was hand-crafted in Phase 9."
  </action>
  <verify>
    `ls bootstrap-demo-target/` shows a Next.js project layout (package.json + next.config.ts + src/ + prisma/).
    `cd bootstrap-demo-target && pnpm tsc --noEmit` exits 0.
    `cd bootstrap-demo-target && pnpm prisma generate` exits 0.
    `find bootstrap-demo-target/src -name '*.tsx' -o -name '*.ts' | wc -l` returns 15–60 (target ~25 source files).
    `ls bootstrap-demo-target/.contracts/ 2>&1 | grep "No such file"` matches (no .contracts/ directory exists).
    `find bootstrap-demo-target -name 'next.config.*' | head -1 | xargs grep -L "contract-uuid-plugin"` matches (no Babel plugin wired yet).
    `cat bootstrap-demo-target/package.json | grep -E '"next":|"prisma":|"stripe":'` matches three deps.
    `cd bootstrap-demo-target && pnpm prisma db push` succeeds (sqlite dev.db created).
  </verify>
  <done>
    bootstrap-demo-target/ exists at /Users/yang/lahacks/bootstrap-demo-target/ as a runnable Next.js + Prisma + TS + Stripe + Lucia + Resend project. Has 4 Prisma models (User, Session, Note, Subscription), ~10 page.tsx, ~8 route.ts, ~5 lib files. NO `.contracts/` directory. NO `contract-uuid-plugin` installed. `pnpm tsc --noEmit` clean. `pnpm prisma db push && pnpm prisma generate` complete. README.md documents stack and bootstrap-demo-target's role as the bootstrap input. Subsequent Plans 14-03/04/05 operate on this directory.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks:**

1. `ls /Users/yang/lahacks/bootstrap-demo-target/` shows Next.js project structure
2. `cd bootstrap-demo-target && pnpm tsc --noEmit` exits 0
3. `find bootstrap-demo-target/src -name '*.tsx' -o -name '*.ts' | wc -l` returns 15–60
4. `ls bootstrap-demo-target/.contracts/ 2>&1 | grep -q "No such"` matches (cardinal check — bootstrap input must lack contracts)
5. `cat bootstrap-demo-target/package.json | jq '.dependencies | has("next") and has("@prisma/client") and has("stripe")'` returns true (or grep equivalents if jq unavailable)
6. `cd bootstrap-demo-target && pnpm prisma db push && pnpm prisma generate` succeeds
7. `cat bootstrap-demo-target/README.md | grep -i bootstrap` matches (README documents purpose)

**No skill execution this plan** — Plan 14-02 builds the demo input. Plans 14-03/04/05 are the operations against it.
</verification>

<success_criteria>
1. bootstrap-demo-target/ exists at the repo root, a sibling of contract-ide/ and contract-ide-demo/
2. Stack matches RESEARCH spec: Next.js 15+ App Router + TypeScript + Tailwind + Prisma + Stripe + at least one additional external integration (Resend email)
3. ~25 source files (10 page.tsx + 8 route.ts + 5 lib + layout + utils) — INTENTIONALLY below the 50-150 RESEARCH range to keep on-stage bootstrap under 4 min (Pitfall 8 mitigation)
4. 4 Prisma models (User / Session / Note / Subscription)
5. App runs: `pnpm install && pnpm prisma db push && pnpm prisma generate && pnpm dev` succeeds
6. NO `.contracts/` directory exists (this is the bootstrap input; output comes from Plans 14-03/04/05)
7. README.md documents stack + bootstrap-demo-target's role as the bootstrap input + demo user credentials
8. BOOTSTRAP-05 [proposed] satisfied — demo target codebase exists; the bootstrap pipeline can run against it
9. Yang's option choice (a/b/c/d) recorded in SUMMARY.md so subsequent plans operate on the right path
</success_criteria>

<output>
After completion, create `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-02-SUMMARY.md`. Document:
- Yang's option choice (a/b/c/d) from the checkpoint
- The chosen target repo's absolute path (this is what subsequent plans use as `<repo-path>`)
- File count breakdown (page.tsx / route.ts / lib / prisma)
- Any deviations from the spec (e.g., scaffold tool versions, dep versions)
- Setup commands run + outcome (pnpm install, prisma db push)
- The repo's git status (initialized? committed?)
- A list of "interesting" patterns deliberately planted for the skill to surface (e.g., "lib/stripe.ts is a clear external classifier test", "lib/notes.ts is a clear lib classifier test", "Account settings has empty Danger Zone — skill should emit an empty-section L4 atom for it")
</output>
