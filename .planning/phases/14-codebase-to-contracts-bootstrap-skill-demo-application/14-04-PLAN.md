---
phase: 14-codebase-to-contracts-bootstrap-skill-demo-application
plan: 04
type: execute
wave: 3
depends_on:
  - 14-03
files_modified:
  - .agents/skills/codebase-to-contracts/scripts/derive-body.mjs
  - .agents/skills/codebase-to-contracts/scripts/align-jsx.mjs
  - .agents/skills/codebase-to-contracts/prompts/derive-body.txt
  - .agents/skills/codebase-to-contracts/prompts/exemplars/api-account-delete.md
  - .agents/skills/codebase-to-contracts/prompts/exemplars/lib-begin-account-deletion.md
  - .agents/skills/codebase-to-contracts/prompts/exemplars/ui-l3-account-settings.md
  - .agents/skills/codebase-to-contracts/prompts/exemplars/ui-l4-danger-zone.md
  - .agents/skills/codebase-to-contracts/scripts/__tests__/derive-body.test.mjs
  - .agents/skills/codebase-to-contracts/scripts/__tests__/align-jsx.test.mjs
  - .agents/skills/codebase-to-contracts/scripts/__tests__/fixtures/page-with-jsx.tsx
  - .agents/skills/codebase-to-contracts/scripts/__tests__/fixtures/page-multi-element-bad.tsx
autonomous: false
requirements:
  - BOOTSTRAP-02  # [proposed] Continued — Stages 3-4 fill body content + JSX-align L4 atom code_ranges; Plan 14-05 closes BOOTSTRAP-02 with the validator gate

must_haves:
  truths:
    - "Stage 3 derives complete contract body sections per node: ## Intent + ## Role for all kinds; ## Examples for UI L4; ## Inputs / ## Outputs / ## Side effects for backend kinds (BACKEND-FM-01)"
    - "Stage 3's derive-body.txt prompt embeds 4 verbatim few-shot exemplars (UI L3, UI L4, backend API, backend lib) extracted from contract-ide-demo/.contracts/ — NOT just an instruction to 'match Phase 9 prose density'"
    - "Stage 3 batches body derivations by L3 surface — page.tsx + its child L4 atoms processed together for context coherence"
    - "Stage 3 prose density is gated — Yang reviews 3 sample derivations before full pipeline runs and iterates the prompt if prose reads generic (cardinal believability check)"
    - "Stage 4 aligns every L4 UI atom to an outermost JSX element via @babel/parser AST walk; refuses to emit any L4 UI atom whose AST search returns zero matches"
    - "Stage 4 multi-match disambiguates via single LLM tiebreak call (passing intent + candidate JSX snippets)"
    - "Stage 4 uses the SAME @babel/parser config as Phase 9's webpack loader (sourceType: module, plugins: [jsx, typescript]) — config drift breaks validator parity"
    - "Stage 4 backend kinds (API / lib / data / external / job / cron / event) skip JSX alignment (JSX-01 exempt per Phase 9)"
    - "All Stage 3+4 output writes to .staging/ — Plan 14-05 atomically promotes to .contracts/"
  artifacts:
    - path: ".agents/skills/codebase-to-contracts/scripts/derive-body.mjs"
      provides: "Stage 3: per-node body derivation via claude -p --json-schema (contract-body.json)"
    - path: ".agents/skills/codebase-to-contracts/scripts/align-jsx.mjs"
      provides: "Stage 4: AST-walk every .tsx, set L4 UI atom code_ranges to outermost JSX element"
    - path: ".agents/skills/codebase-to-contracts/prompts/derive-body.txt"
      provides: "System prompt for Stage 3 — embeds 4 verbatim exemplars + instructs LLM to match prose density specifically (not generically)"
    - path: ".agents/skills/codebase-to-contracts/prompts/exemplars/"
      provides: "4 exemplar contract bodies extracted from contract-ide-demo/.contracts/ — load-bearing prose-density anchors for the prompt"
  key_links:
    - from: ".agents/skills/codebase-to-contracts/scripts/derive-body.mjs"
      to: ".agents/skills/codebase-to-contracts/schemas/contract-body.json"
      via: "schemaPath argument to claude -p --json-schema"
      pattern: "schemas/contract-body.json"
    - from: ".agents/skills/codebase-to-contracts/prompts/derive-body.txt"
      to: ".agents/skills/codebase-to-contracts/prompts/exemplars/*.md"
      via: "verbatim inline embedding (read at script startup, interpolated into system prompt)"
      pattern: "exemplars/.*\\.md"
    - from: ".agents/skills/codebase-to-contracts/scripts/align-jsx.mjs"
      to: ".agents/skills/codebase-to-contracts/scripts/helpers/babel-parser-bridge.mjs"
      via: "loadBabel(targetRoot) — resolves @babel/parser via pnpm-store fallback"
      pattern: "loadBabel\\("
    - from: ".agents/skills/codebase-to-contracts/scripts/align-jsx.mjs"
      to: "contract-ide/src-tauri/src/sidecar/jsx_align_validator.rs"
      via: "MUST emit code_ranges that pass jsx_align_validator.rs (load-bearing parity)"
      pattern: "outermost JSX element"
---

<objective>
Build Stages 3 (Derive Body) and 4 (Align JSX code_ranges) — the body-content + UI-anchor half of the pipeline. Stage 3 fills `## Intent` / `## Role` for all nodes plus `## Examples` (UI L4), `## Inputs` / `## Outputs` / `## Side effects` (backend kinds). Stage 4 walks every .tsx with @babel/parser, finds the outermost JSX element matching each L4 UI atom's candidate range, and updates its `code_ranges` accordingly — refusing to emit any L4 UI atom that can't align.

**Post-revision-2 addition: Prose Quality Gate.** Stage 3's `derive-body.txt` prompt MUST embed 4 verbatim few-shot exemplars (extracted from contract-ide-demo/.contracts/) — NOT just an instruction to "match Phase 9 prose density." Plus a new Task 3 prompt-iteration checkpoint: Yang reviews 3 sample derivations against the bootstrap-demo-target before Stage 3 runs the full pipeline. If prose reads generic, iterate the prompt. This is the gate that prevents the "show me the .contracts/ directory" embarrassment in Q&A.

Purpose: BOOTSTRAP-02 — body content makes contracts useful (Stage 2's frontmatter is structurally valid but conceptually empty); JSX alignment makes BABEL-01 chip-overlay work. Together with Stage 5 (Plan 14-05's validator gate), this satisfies the BOOTSTRAP-02 promise that the IDE loads the bootstrapped repo without manual cleanup.

Output: Two pipeline scripts, one LLM prompt, **4 exemplar files**, two unit-test suites with `.tsx` fixtures, and a Yang-approved prompt that produces prose at Phase 9 density (verified on 3 sample derivations before full run).

This is the WAVE 3 plan — depends on Plan 14-03 (Stages 1-2) only. Plan 14-05 (Wave 4) is the next link.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-RESEARCH.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-01-SUMMARY.md
@.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-03-SUMMARY.md  # Stage 1+2 output shape

# Phase 9 validator — source of truth for JSX alignment correctness
@contract-ide/src-tauri/src/sidecar/jsx_align_validator.rs
@contract-ide/src-tauri/src/sidecar/backend_section_validator.rs

# Phase 9 webpack loader — same @babel/parser config Stage 4 must use
@contract-ide-demo/contract-uuid-plugin/index.js

# Body prose-density exemplars — these are EXTRACTED VERBATIM into prompts/exemplars/ during Task 1
@contract-ide-demo/.contracts/ambient/api-account-delete-001.md
@contract-ide-demo/.contracts/ambient/lib-begin-account-deletion-001.md
@contract-ide-demo/.contracts/ambient/external-stripe-001.md
@contract-ide-demo/.contracts/a0000000-0000-4000-8000-000000000000.md
@contract-ide-demo/.contracts/a1000000-0000-4000-8000-000000000000.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: derive-body.mjs (Stage 3) + prompt with embedded exemplars (UI L3, UI L4, backend API, backend lib)</name>
  <files>
    .agents/skills/codebase-to-contracts/scripts/derive-body.mjs
    .agents/skills/codebase-to-contracts/prompts/derive-body.txt
    .agents/skills/codebase-to-contracts/prompts/exemplars/api-account-delete.md
    .agents/skills/codebase-to-contracts/prompts/exemplars/lib-begin-account-deletion.md
    .agents/skills/codebase-to-contracts/prompts/exemplars/ui-l3-account-settings.md
    .agents/skills/codebase-to-contracts/prompts/exemplars/ui-l4-danger-zone.md
    .agents/skills/codebase-to-contracts/scripts/__tests__/derive-body.test.mjs
  </files>
  <action>
    ### Step 1 — Extract exemplars from contract-ide-demo

    Copy the body sections (everything after the frontmatter `---`) from these 4 contracts into the prompts/exemplars/ directory verbatim:

    - `contract-ide-demo/.contracts/ambient/api-account-delete-001.md` → `prompts/exemplars/api-account-delete.md` (backend API exemplar — has Intent / Role / Inputs / Outputs / Side effects)
    - `contract-ide-demo/.contracts/ambient/lib-begin-account-deletion-001.md` → `prompts/exemplars/lib-begin-account-deletion.md` (backend lib exemplar — pure-function shape with Inputs / Outputs / Side effects)
    - `contract-ide-demo/.contracts/a0000000-0000-4000-8000-000000000000.md` → `prompts/exemplars/ui-l3-account-settings.md` (UI L3 exemplar — has Intent / Role / Notes; NO Inputs/Outputs/Side effects)
    - `contract-ide-demo/.contracts/a1000000-0000-4000-8000-000000000000.md` → `prompts/exemplars/ui-l4-danger-zone.md` (UI L4 exemplar — has Intent / Role / Examples placeholder)

    Each exemplar file is just the markdown body (sections starting from `## Intent`). NO frontmatter. These are read by derive-body.mjs at startup and interpolated into the system prompt.

    ### Step 2 — Author prompts/derive-body.txt (~180 lines, exemplars-embedded)

    The prompt has three sections:

    **Section A — Mission (~20 lines):** "You are deriving the body content of a Contract IDE format-v3 contract for a single node in a target codebase. The frontmatter has already been written; you write only the markdown sections that follow it. Match the prose density, sentence rhythm, and specificity of the exemplars below — they are NOT illustrative; they are the literal target."

    **Section B — Per-kind branch instructions (~40 lines):**

    - UI L3 (page.tsx, layout.tsx): produce `## Intent` (50–200 words: user-facing capability) + `## Role` (30–100 words: positional/functional) + optional `## Notes` (≤50 words). DO NOT produce Inputs/Outputs/Side effects.
    - UI L4 (atoms inside page.tsx): produce `## Intent` + `## Role` + `## Examples` (placeholder `(none yet — to be authored when this atom gets its first action)` if no obvious examples; brief Given/When/Then if the atom is a clear action surface).
    - Backend (API / lib / data / external / job / cron / event): produce `## Intent` + `## Role` + `## Inputs` (bullet list, ≥1) + `## Outputs` (bullet list, ≥1) + `## Side effects` (bullet list, may be 0 for pure functions but the section header MUST be present per BACKEND-FM-01).

    Explicit content rules:
    - Bullets in Inputs / Outputs use inline-code spans for code-y identifiers (`Authorization: Bearer <token>`)
    - Outputs section uses HTTP status codes when kind is API; field-level outputs when kind is lib/data/external
    - Side effects names other lib functions called (resolve via imports); never invents names not in the source
    - Cross-references: when a backend node calls another known node (visible in imports), name it: "Calls beginAccountDeletion(userId) which: ..." — this enables Stage 5a flow synthesis
    - Intent must call out WHICH SPECIFIC behavior was chosen vs alternatives (e.g., "Soft-deletes by setting deletedAt rather than removing the row" — NOT "Deletes the user").

    **Section C — Embedded exemplars (~120 lines, verbatim interpolation):**

    The script reads each `prompts/exemplars/*.md` file at startup and substitutes `{{EXEMPLAR_API}}`, `{{EXEMPLAR_LIB}}`, `{{EXEMPLAR_UI_L3}}`, `{{EXEMPLAR_UI_L4}}` into the prompt template. The template structure:

    ```
    --- EXEMPLAR 1 of 4: backend API endpoint (DELETE /api/account) ---
    File: src/app/api/account/route.ts
    Kind: API · Level: L3
    {{EXEMPLAR_API}}
    --- END EXEMPLAR 1 ---

    --- EXEMPLAR 2 of 4: backend lib function (beginAccountDeletion) ---
    File: src/lib/account/beginAccountDeletion.ts
    Kind: lib · Level: L3
    {{EXEMPLAR_LIB}}
    --- END EXEMPLAR 2 ---

    --- EXEMPLAR 3 of 4: UI L3 page surface (Account Settings) ---
    File: src/app/account/settings/page.tsx
    Kind: UI · Level: L3
    {{EXEMPLAR_UI_L3}}
    --- END EXEMPLAR 3 ---

    --- EXEMPLAR 4 of 4: UI L4 atom (Danger Zone container) ---
    File: src/app/account/settings/page.tsx
    Kind: UI · Level: L4
    {{EXEMPLAR_UI_L4}}
    --- END EXEMPLAR 4 ---

    Match this density. If your output reads vaguer than these exemplars — too short, too generic, lacking the specific behavior callouts ("Soft-deletes by setting deletedAt rather than removing the row" not "Deletes the user") — it will be rejected at the Plan 14-04 Task 3 quality gate and you will be re-run.
    ```

    ### Step 3 — scripts/derive-body.mjs implementation

    Standard structure (inputs: nodes.json + per-node frontmatter from .staging/; outputs: .staging/<uuid>.body.json per derivation). Key implementation details:

    - At startup, read all 4 exemplar files from `prompts/exemplars/*.md` and interpolate into the prompt template via simple string replacement of `{{EXEMPLAR_*}}` tokens.
    - Group nodes by L3 surface (parent_hint) for context coherence; parallelize at the L3-group boundary, concurrency=5.
    - Per-node call: `claude -p --json-schema schemas/contract-body.json --system-prompt <interpolated derive-body.txt> --user-prompt <node + source file>`.
    - Hash-skip: if `.staging/<uuid>.body.json` exists and `_progress.json.stage_3_completed_for` includes the uuid, skip.
    - Update `_progress.json` with stage_3_completed_at, stage_3_derived_count.

    Use the same `callClaude` helper from Plan 14-01 (claude-cli-bridge.mjs).

    ```javascript
    // scripts/derive-body.mjs
    import { readFileSync, writeFileSync, existsSync, readdirSync } from 'node:fs';
    import { resolve, dirname } from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { callClaude } from './helpers/claude-cli-bridge.mjs';

    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);

    function buildSystemPrompt() {
      const promptDir = resolve(__dirname, '../prompts');
      const template = readFileSync(resolve(promptDir, 'derive-body.txt'), 'utf8');
      const exemplars = {
        '{{EXEMPLAR_API}}': readFileSync(resolve(promptDir, 'exemplars/api-account-delete.md'), 'utf8'),
        '{{EXEMPLAR_LIB}}': readFileSync(resolve(promptDir, 'exemplars/lib-begin-account-deletion.md'), 'utf8'),
        '{{EXEMPLAR_UI_L3}}': readFileSync(resolve(promptDir, 'exemplars/ui-l3-account-settings.md'), 'utf8'),
        '{{EXEMPLAR_UI_L4}}': readFileSync(resolve(promptDir, 'exemplars/ui-l4-danger-zone.md'), 'utf8'),
      };
      return Object.entries(exemplars).reduce(
        (acc, [token, content]) => acc.replaceAll(token, content),
        template,
      );
    }

    export async function deriveBody(repoPath, options = {}) {
      const stagingDir = resolve(repoPath, '.contracts/.staging');
      const nodes = JSON.parse(readFileSync(resolve(stagingDir, 'nodes.json'), 'utf8'));
      const systemPrompt = buildSystemPrompt();

      // ... (rest matches the previous implementation: group by L3, parallelize, per-node callClaude, write body.json, update progress)
    }

    // CLI entry — supports --sample N flag for Task 3 prompt-iteration loop
    if (import.meta.url === `file://${process.argv[1]}`) {
      const repoPath = process.argv[2] || process.cwd();
      const sampleArg = process.argv.find(a => a.startsWith('--sample='));
      const sampleN = sampleArg ? parseInt(sampleArg.split('=')[1], 10) : null;
      deriveBody(resolve(repoPath), { sampleN }).catch(err => { console.error(err); process.exit(1); });
    }
    ```

    The `--sample N` flag (used by Task 3) processes only the first N nodes (one UI L3, one UI L4, one backend) for quick prompt iteration without running the full pipeline.

    ### Step 4 — derive-body.test.mjs (mocked claude)

    Test fixture: 3 nodes (UI L3 page, UI L4 atom, backend lib). Mock callClaude to return canned `structured_output` per kind branch.
    Asserts:
    - 3 .body.json files written; each has expected fields per kind
    - Backend node body has inputs (≥1) + outputs (≥1) + side_effects (array, may be empty)
    - UI L4 body has examples (may be empty array)
    - Re-running with mocked unchanged state writes 0 files (skip-completed logic)
    - **NEW:** buildSystemPrompt() reads all 4 exemplar files and interpolates them into the template; resulting system prompt contains the literal text "## Intent" at least 4 times (once per exemplar) AND "## Side effects" at least 1 time
  </action>
  <verify>
    `node --test .agents/skills/codebase-to-contracts/scripts/__tests__/derive-body.test.mjs` exits 0.
    `wc -l .agents/skills/codebase-to-contracts/prompts/derive-body.txt` returns ≥150.
    `ls .agents/skills/codebase-to-contracts/prompts/exemplars/ | wc -l` returns 4.
    `cat .agents/skills/codebase-to-contracts/prompts/exemplars/api-account-delete.md | grep -E "## (Intent|Role|Inputs|Outputs|Side effects)" | wc -l` returns 5.
    `cat .agents/skills/codebase-to-contracts/prompts/derive-body.txt | grep -c "{{EXEMPLAR_"` returns 4 (template has 4 interpolation tokens).
  </verify>
  <done>
    Stage 3 implementation complete. derive-body.mjs reads nodes.json + per-node frontmatter from `.staging/`, interpolates 4 verbatim exemplars into the system prompt, calls `claude -p --output-format json --json-schema schemas/contract-body.json` per node (grouped by L3 surface, parallelized at concurrency 5), writes `.staging/<uuid>.body.json` per derivation. Backend nodes get populated `## Inputs / ## Outputs / ## Side effects`; UI L4 atoms get `## Intent / ## Role / ## Examples`. Unit test (mocked) verifies kind-branch logic + exemplar interpolation. The `--sample N` flag enables Task 3's prompt-iteration loop.
  </done>
</task>

<task type="auto">
  <name>Task 2: align-jsx.mjs (Stage 4) — AST walk; outermost JSX element matching; LLM tiebreak; refuse-to-emit on zero match</name>
  <files>
    .agents/skills/codebase-to-contracts/scripts/align-jsx.mjs
    .agents/skills/codebase-to-contracts/scripts/__tests__/align-jsx.test.mjs
    .agents/skills/codebase-to-contracts/scripts/__tests__/fixtures/page-with-jsx.tsx
    .agents/skills/codebase-to-contracts/scripts/__tests__/fixtures/page-multi-element-bad.tsx
  </files>
  <action>
    [Unchanged from previous revision — full Stage 4 implementation per RESEARCH Pattern 4. Walk every L4 UI atom's parent .tsx with @babel/parser using config matching Phase 9 webpack loader EXACTLY (`sourceType: 'module', plugins: ['jsx', 'typescript']`). Find outermost JSXElement fully contained in candidate range. Multi-match → single-call LLM tiebreak (no schema, integer back). Zero-match → process.exit(1) with `_stage4_failures.json` diagnostic. Backend kinds skip (JSX-01 exempt).

    Two fixtures (page-with-jsx.tsx with nested sections, page-multi-element-bad.tsx with sibling outermost candidates), three test cases (single-match returns code_ranges, outermost-detection picks parent over nested, no-match returns error, multi-match triggers tiebreak, alignAllAtoms aborts on any failure with diagnostic JSON written).

    See previous revision of this plan for the full implementation listing — unchanged.]
  </action>
  <verify>
    `node --test .agents/skills/codebase-to-contracts/scripts/__tests__/align-jsx.test.mjs` exits 0.
    `cat .agents/skills/codebase-to-contracts/scripts/align-jsx.mjs | grep "sourceType: 'module', plugins: \['jsx', 'typescript'\]"` matches.
    `diff <(grep -E "sourceType|plugins.*jsx" .agents/skills/codebase-to-contracts/scripts/align-jsx.mjs) <(grep -E "sourceType|plugins.*jsx" contract-ide-demo/contract-uuid-plugin/index.js)` shows compatible parser invocations.
  </verify>
  <done>
    Stage 4 complete: align-jsx.mjs walks every L4 UI atom's parent .tsx with @babel/parser (config matching Phase 9 plugin EXACTLY), finds the outermost JSX element fully contained in the candidate range, sets the atom's `code_ranges`. Multi-match candidates resolve via LLM tiebreak. Zero-match atoms cause Stage 4 to fail loudly. Backend kinds skip. Two fixture-based tests verify outermost-detection + tiebreak path + abort-on-no-match.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — Prose-quality prompt-iteration loop (Yang reviews 3 sample derivations on bootstrap-demo-target)</name>
  <action>Pause for Yang to run derive-body in --sample mode against bootstrap-demo-target, manually review 3 sample outputs (one UI L3, one UI L4, one backend), score each against Phase 9 exemplars on (a) decision specificity, (b) cross-reference density, (c) prose density, and either approve OR loop back to edit derive-body.txt and re-run --sample. The full Stage 3 pipeline does NOT run until Yang approves the prompt. This is the cardinal believability check — the gate that prevents "show me the .contracts/ directory" embarrassment in Q&A.</action>
  <what-built>
    Stage 3 derive-body.mjs + Stage 4 align-jsx.mjs + 4 verbatim exemplars embedded in derive-body.txt.

    Now: Yang validates Stage 3 prose quality on a 3-node sample BEFORE running the full pipeline. The sample run takes <30s and ~$0.30, vs the full run at ~3min and ~$1.50 — so iteration cost is small but the quality bar is set decisively here.
  </what-built>
  <how-to-verify>
    1. Pre-req: Plans 14-01, 14-02, 14-03 complete. bootstrap-demo-target/.contracts/.staging/ has nodes.json + per-node frontmatter.json from Stage 1+2.

    2. Run sample derivation:
       ```
       cd /Users/yang/lahacks
       node .agents/skills/codebase-to-contracts/scripts/derive-body.mjs /Users/yang/lahacks/bootstrap-demo-target --sample=3
       ```
       This processes the first 3 nodes (auto-selected to span: 1 UI L3 page, 1 UI L4 atom, 1 backend route or lib).

    3. For each of the 3 sample `bootstrap-demo-target/.contracts/.staging/<uuid>.body.json` outputs, open it side-by-side with the corresponding exemplar in `.agents/skills/codebase-to-contracts/prompts/exemplars/`. Score each on:

       **(a) Decision specificity** — does the body call out WHICH specific behavior was chosen vs alternatives?
       - Pass: "Soft-deletes by setting deletedAt rather than removing the row, preserving FK references in Invoice / AuditLog."
       - Fail: "Deletes the user from the database."

       **(b) Cross-reference density** — does the body link to other contracts or substrate by name?
       - Pass: "Calls beginAccountDeletion(userId) which enforces the 5 substrate rules captured from the February 2026 deletion incident."
       - Fail: "Performs the deletion logic."

       **(c) Prose density** — does it read like a human wrote it?
       - Pass: paragraph rhythm, parenthetical asides, specific names of variables/functions, varied sentence length
       - Fail: bullet-list-only, repetitive sentence structure, generic verbs ("processes", "handles", "manages")

    4. Decision:
       - **All 3 pass on all 3 axes** → approve; full Stage 3 + Stage 4 run can proceed in Plan 14-06.
       - **Any sample fails any axis** → identify which axis fails most, edit `prompts/derive-body.txt` (typically: strengthen Section B per-kind rules; OR add a 5th exemplar; OR add an explicit "DO NOT" rule like "DO NOT use the word 'manages' in Intent — replace with the specific verb"), then re-run `--sample=3`. Loop until pass.
       - Hard ceiling: 3 prompt iterations. If still failing after 3 loops, surface to planning — may indicate exemplars-only approach is insufficient and we need to add a structured-output schema constraint (e.g., minLength on intent field).

    5. After approval, log to SUMMARY.md: how many iterations, what edits were made, the final 3 sample-output paths for evidence.

    Approve only if all 3 samples pass all 3 axes. The full Stage 3 run downstream (Plan 14-06) inherits this prompt; quality regressions are not catchable later without re-running ~$1.50 of pipeline.

    Reject + iterate if any sample reads generic. The Q&A inset (Plan 14-06 Task 3) will scroll through real .contracts/ files; if those files read like generated boilerplate, the demo loses.
  </how-to-verify>
  <resume-signal>Type "approved" + paste the 3 sample-output absolute paths + iteration count, OR describe which axis failed for which sample and which prompt edit you're about to try</resume-signal>
</task>

</tasks>

<verification>
**Plan-level checks:**

1. `node --test .agents/skills/codebase-to-contracts/scripts/__tests__/derive-body.test.mjs` exits 0
2. `node --test .agents/skills/codebase-to-contracts/scripts/__tests__/align-jsx.test.mjs` exits 0
3. `ls .agents/skills/codebase-to-contracts/prompts/exemplars/ | wc -l` returns 4 (4 verbatim exemplars present)
4. `cat .agents/skills/codebase-to-contracts/prompts/derive-body.txt | grep -c "{{EXEMPLAR_"` returns 4 (template has 4 interpolation tokens)
5. align-jsx.mjs uses parser config matching `contract-ide-demo/contract-uuid-plugin/index.js` exactly
6. align-jsx.mjs has explicit `process.exit(1)` on zero-match failure with `_stage4_failures.json` written
7. derive-body.mjs supports `--sample N` flag for the prose-quality prompt-iteration loop
8. **Task 3 prose-quality gate approved** — 3 sample derivations on bootstrap-demo-target reviewed by Yang and pass on decision specificity, cross-reference density, prose density (logged in SUMMARY.md)
</verification>

<success_criteria>
1. derive-body.mjs runs claude -p --json-schema per node (grouped by L3 surface); emits .staging/<uuid>.body.json per derivation matching contract-body.json schema
2. derive-body.txt embeds 4 verbatim exemplars (UI L3, UI L4, backend API, backend lib) extracted from contract-ide-demo/.contracts/ — load-bearing prose-density anchors
3. Body content respects kind branches: UI L3 (Intent + Role + Notes), UI L4 (Intent + Role + Examples), backend (Intent + Role + Inputs + Outputs + Side effects)
4. Prose density matches Phase 9 exemplars — verified empirically by Yang's Task 3 sample review (NOT just by manual review at demo time)
5. align-jsx.mjs uses @babel/parser with EXACTLY Phase 9's webpack-loader config; emits code_ranges that pass jsx_align_validator.rs
6. align-jsx.mjs refuses to proceed (process.exit 1 + diagnostic JSON) when any L4 UI atom can't align
7. Backend kinds skip JSX alignment (JSX-01 exempt per Phase 9)
8. Multi-match disambiguates via single LLM tiebreak call
9. Two fixture-based unit-test suites pass
10. derive-body.mjs supports `--sample N` flag for cheap prompt iteration
11. **Prose Quality Gate (Task 3) passed** — Yang approved the prompt on 3 sample derivations against bootstrap-demo-target before full pipeline runs in Plan 14-06
12. BOOTSTRAP-02 [proposed] further closure: body content + JSX alignment ready for Stage 5b's atomic emit + validator gate (Plan 14-05)
</success_criteria>

<output>
After completion, create `.planning/phases/14-codebase-to-contracts-bootstrap-skill-demo-application/14-04-SUMMARY.md`. Document:
- derive-body.mjs concurrency setting + observed throughput on the 3-sample run
- Prose Quality Gate outcome:
  - Number of prompt iterations (target: 1; ceiling: 3)
  - What edits were made between iterations (if any)
  - The 3 final sample-output absolute paths
  - Yang's verbatim approval note
- Stage 3 cost on the 3-sample run (extrapolated full-run cost estimate)
- align-jsx.mjs parser config — confirm it MATCHES `contract-ide-demo/contract-uuid-plugin/index.js` line-for-line in the parse() invocation
- Pitfall 7 (validator parity) — verify by running Plan 14-05 Stage 5b validator over .staging output once Stage 4 lands
</output>
