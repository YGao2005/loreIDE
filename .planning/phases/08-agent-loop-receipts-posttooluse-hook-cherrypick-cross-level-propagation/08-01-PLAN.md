---
phase: 08-agent-loop-receipts-posttooluse-hook-cherrypick-cross-level-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contract-ide/src-tauri/Cargo.toml
  - contract-ide/src-tauri/tauri.conf.json
  - contract-ide/src-tauri/src/sidecar/section_parser.rs
  - contract-ide/src-tauri/src/sidecar/frontmatter.rs
  - contract-ide/src-tauri/src/sidecar/mod.rs
  - contract-ide/src-tauri/src/db/migrations.rs
  - contract-ide/src-tauri/binaries/section-parser-cli/Cargo.toml
  - contract-ide/src-tauri/binaries/section-parser-cli/src/main.rs
  - contract-ide/src-tauri/tests/section_parser_tests.rs
  - contract-ide/src-tauri/tests/fixtures/contract_api_l3.md
  - contract-ide/src-tauri/tests/fixtures/contract_ui_l4.md
  - contract-ide/src-tauri/tests/fixtures/contract_beat1_body.md
autonomous: true
requirements:
  - PROP-01

must_haves:
  truths:
    - "ContractFrontmatter struct gains 5 Phase 8 fields (section_hashes, rollup_inputs, rollup_hash, rollup_state, rollup_generation) and round-trips through serde_yaml_ng"
    - "section_parser::parse_sections returns BTreeMap (alphabetically sorted) of H2-section name to faithful source text using pulldown-cmark offset_iter"
    - "section_parser::compute_section_hashes produces stable sha256 hex regardless of section order in the input body"
    - "Two committed fixture contracts (11111111-... API L3, 22222222-... UI L4) round-trip stable section_hashes despite different section ordering"
    - "Literal Beat 1 body from presentation-script.md (## Intent / ## Role / ## Examples with multi-paragraph + GIVEN/WHEN/THEN) is accepted and produces stable hashes"
    - "Reading a v2 contract returns in-memory section_hashes WITHOUT writing back to disk (lazy migration — no bootstrap write storm)"
    - "Migration v3 adds section_hashes / rollup_inputs / rollup_hash / rollup_state / rollup_generation columns to nodes table AND creates rollup_derived table"
    - "section-parser-cli binary in src-tauri/binaries reads markdown body from stdin and writes JSON {section_hashes:{...}} to stdout"
  artifacts:
    - path: "contract-ide/src-tauri/Cargo.toml"
      provides: "pulldown-cmark 0.13 dependency"
      contains: "pulldown-cmark"
    - path: "contract-ide/src-tauri/src/sidecar/section_parser.rs"
      provides: "Canonical Rust section parser (PROP-01)"
      exports: ["parse_sections", "compute_section_hashes", "ParseError"]
      min_lines: 80
    - path: "contract-ide/src-tauri/src/sidecar/frontmatter.rs"
      provides: "Extended ContractFrontmatter with 5 Phase 8 fields (format_version: 3) + read_sidecar_file helper for downstream plans"
      contains: "section_hashes"
      exports: ["parse_sidecar", "write_sidecar", "read_sidecar_file"]
    - path: "contract-ide/src-tauri/tauri.conf.json"
      provides: "section-parser-cli registered in bundle.externalBin alongside mcp-server"
      contains: "section-parser-cli"
    - path: "contract-ide/src-tauri/src/db/migrations.rs"
      provides: "Migration v3 adds Phase 8 columns + rollup_derived table"
      contains: "0003"
    - path: "contract-ide/src-tauri/binaries/section-parser-cli/src/main.rs"
      provides: "stdin->stdout CLI wrapper around section_parser for MCP sidecar IPC"
      min_lines: 20
    - path: "contract-ide/src-tauri/tests/section_parser_tests.rs"
      provides: "Round-trip + Beat 1 body + duplicate-H2 unit tests"
      min_lines: 40
    - path: "contract-ide/src-tauri/tests/fixtures/contract_beat1_body.md"
      provides: "Literal Beat 1 body acceptance fixture"
      contains: "## Intent"
  key_links:
    - from: "contract-ide/src-tauri/src/sidecar/frontmatter.rs"
      to: "contract-ide/src-tauri/src/sidecar/section_parser.rs"
      via: "compute_section_hashes called when frontmatter is serialized at format_version: 3"
      pattern: "compute_section_hashes"
    - from: "contract-ide/src-tauri/binaries/section-parser-cli/src/main.rs"
      to: "contract-ide/src-tauri/src/sidecar/section_parser.rs"
      via: "shared crate or copied module — CLI is built alongside main app and ships as a Tauri binary asset"
      pattern: "compute_section_hashes|parse_sections"
    - from: "contract-ide/src-tauri/src/db/migrations.rs"
      to: "Phase 7 drift_state table"
      via: "rollup_derived schema mirrors drift_state pattern"
      pattern: "rollup_derived"
---

<objective>
Land the propagation foundation: the canonical Rust section parser, the format_version: 3 frontmatter extension with five new fields, the SQLite migration v3, and a section-parser-cli binary that the MCP sidecar can spawn (resolves Open Question 2 from RESEARCH.md). Lazy migration on first write — reading a v2 contract MUST NOT write back to disk.

Purpose: Every other Phase 8 plan reads from this foundation. Rollup detection (08-02) reads section_hashes; PostToolUse hook (08-03) journals against UUIDs persisted under v3; agent loop (08-04) prompts contain section-weighted bodies; cherrypick (08-05) writes via write_contract which now persists section_hashes. PROP-01 is demo-grade: the literal Beat 1 body MUST round-trip through this parser.

**Schema-v3 canonical sections.** The parser accepts arbitrary H2 sections (no "unknown section" rejection — Pitfall 10), but the schema-v3 convention documents FOUR canonical sections that downstream readers expect:
- `## Intent` — user-authored "what this is for" (preserved verbatim by Pass 2 auto-rederive)
- `## Role` — user-authored "where this fits" (preserved verbatim by Pass 2 auto-rederive)
- `## Examples` — observed behavior in GIVEN/WHEN/THEN form (owned by Pass 2 auto-rederive — rewritten on every code change)
- `## Implicit Decisions` — choices the agent made that don't surface in code reading (owned by Pass 2 auto-rederive — newly written when first observed)

Beat 1 fixture body from `presentation-script.md` does NOT currently contain `## Implicit Decisions` — that's fine, it's optional and Pass 2 (08-03) writes it the first time the agent edits code. The parser must round-trip a body containing all four sections (acceptance fixture below).

Output: ContractFrontmatter v3 + parser module + migration v3 + section-parser-cli + comprehensive tests including a four-section round-trip. No new IPC commands yet — those land in 08-02..06.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-agent-loop-receipts-posttooluse-hook-cherrypick-cross-level-propagation/08-CONTEXT.md
@.planning/phases/08-agent-loop-receipts-posttooluse-hook-cherrypick-cross-level-propagation/08-RESEARCH.md
@.planning/research/contract-form/PROPAGATION.md
@.planning/research/contract-form/RESEARCH.md
@.planning/demo/presentation-script.md

# Existing surfaces extended by this plan
@contract-ide/src-tauri/src/sidecar/frontmatter.rs
@contract-ide/src-tauri/src/sidecar/mod.rs
@contract-ide/src-tauri/src/db/migrations.rs
@contract-ide/src-tauri/Cargo.toml
@contract-ide/src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pulldown-cmark dep + canonical section parser module + section-parser-cli binary</name>
  <files>contract-ide/src-tauri/Cargo.toml, contract-ide/src-tauri/src/sidecar/section_parser.rs, contract-ide/src-tauri/src/sidecar/mod.rs, contract-ide/src-tauri/binaries/section-parser-cli/Cargo.toml, contract-ide/src-tauri/binaries/section-parser-cli/src/main.rs, contract-ide/src-tauri/tests/fixtures/contract_beat1_body.md, contract-ide/src-tauri/tests/section_parser_tests.rs</files>
  <action>
    1. Append `pulldown-cmark = "0.13"` to `[dependencies]` in `contract-ide/src-tauri/Cargo.toml`. Confirm `sha2`, `serde`, `serde_yaml_ng`, `thiserror`, `hex` already present (Phase 6/7 shipped them). If `hex` is missing, add `hex = "0.4"`.

    2. Create `src-tauri/src/sidecar/section_parser.rs` exposing:
       - `pub enum ParseError { DuplicateHeading(String) }` (use `thiserror`).
       - `pub fn parse_sections(body: &str) -> Result<BTreeMap<String, String>, ParseError>` — uses `pulldown_cmark::Parser::new_ext(body, Options::all()).into_offset_iter()` to walk `(Event, Range<usize>)` pairs. On `Event::Start(Tag::Heading{level: HeadingLevel::H2, ..})`, capture the next `Event::Text/Code` event(s) until `Event::End(TagEnd::Heading(_))` to assemble the heading title (lowercase trim it for normalization). Track byte ranges between consecutive H2 starts; the section body is `body[end_of_previous_heading_close..start_of_next_heading_or_eof].trim()` (use the offset_iter ranges, NOT regenerated text — pulldown-cmark normalizes whitespace, we need source-faithful bytes). On insertion, if `BTreeMap::insert` returns `Some(_)`, the heading was duplicated → return `ParseError::DuplicateHeading(name)`.
       - `pub fn compute_section_hashes(body: &str) -> Result<BTreeMap<String, String>, ParseError>` — calls `parse_sections`, then for each `(name, text)` pair computes `hex::encode(Sha256::digest(text.as_bytes()))`. Returns BTreeMap (alphabetically sorted by section name — guaranteed by BTreeMap).

       CRITICAL implementation notes (from RESEARCH.md Pattern 1 + Pitfall 1):
       - DO NOT regenerate text from events — `Parser::into_offset_iter` provides source-byte ranges; slice `body[range]` directly.
       - Heading-title extraction: when inside `Heading(H2)`, concatenate all `Event::Text`/`Event::Code` payloads until `End(TagEnd::Heading)`; lowercase + trim the result for the BTreeMap key.
       - BTreeMap is order-stable by key — section_hashes serializes alphabetically regardless of input order. This is the Pitfall 1 fix.
       - Fenced-code-aware behavior: pulldown-cmark `Tag::Heading` is ONLY emitted for actual ATX/Setext headings — `## ` lines inside ` ``` ` fences DO NOT trigger Tag::Heading (verified in pulldown-cmark docs). No extra logic needed.

       Add `pub mod section_parser;` to `src-tauri/src/sidecar/mod.rs`.

       **Also add a `read_sidecar_file` helper to `src-tauri/src/sidecar/frontmatter.rs`** (B4 — three downstream plans need this). Three plans (08-02 rollup engine, 08-05 cherrypick, 08-06 reconcile) want a one-call `(repo, uuid) → (frontmatter, body)` API. Today only `parse_sidecar(content: &str)` and `write_sidecar(fm, body)` exist — callers do `std::fs::read_to_string` themselves. Add:
       ```rust
       /// Read and parse a sidecar by UUID against a repo root. Convenience wrapper
       /// around std::fs::read_to_string + parse_sidecar; the path layout is
       /// `<repo>/.contracts/<uuid>.md` per Phase 2 invariant.
       pub fn read_sidecar_file(repo: &std::path::Path, uuid: &str) -> anyhow::Result<(ContractFrontmatter, String)> {
           let path = repo.join(".contracts").join(format!("{uuid}.md"));
           let content = std::fs::read_to_string(&path)
               .map_err(|e| anyhow::anyhow!("read sidecar {}: {e}", path.display()))?;
           parse_sidecar(&content)
       }
       ```
       Add at minimum one unit test (`reads_existing_sidecar_round_trip`) that writes a fixture sidecar via `write_sidecar`, then loads it back via `read_sidecar_file` and asserts frontmatter + body match.

       **Naming clarity for downstream plans:** the existing serializer is named `write_sidecar(fm, body) -> String`. 08-05 references `build_sidecar_content` — that name does NOT exist. Downstream plans must call `write_sidecar` (08-05 will be updated in this revision). Do NOT rename `write_sidecar` — Phase 2/3/4/5/6/7 callers depend on it.

    3. Create `src-tauri/binaries/section-parser-cli/` as a workspace member or standalone Cargo project:
       - `Cargo.toml` with name = `section-parser-cli`, deps: `pulldown-cmark = "0.13"`, `sha2`, `hex`, `serde_json`, `thiserror`. Include the section_parser.rs as a `[[bin]]` source OR (cleaner) reference the parent crate via a path dependency. Decision: standalone bin in its own dir with the section_parser logic copied or shared via a `mod section_parser;` line; the parent app's `section_parser.rs` must compile under both contexts (no `use crate::` of unrelated modules).
       - `src/main.rs`: reads stdin to a String, calls `compute_section_hashes`, prints `serde_json::json!({"section_hashes": map})` to stdout, exits 0. On parse error, prints `{"error": "<msg>"}` to stdout and exits with code 1.
       - **Wire up `tauri.conf.json` `bundle.externalBin`** (W6 — currently only `binaries/mcp-server` is listed; the section parser CLI must also be bundled). Mirror Phase 5's mcp-server pattern:
         - Build script copies the cargo release artifact to `src-tauri/binaries/section-parser-cli-<target-triple>` (e.g., `section-parser-cli-aarch64-apple-darwin`). Tauri's bundler picks up triple-suffixed binaries automatically.
         - Edit `src-tauri/tauri.conf.json` `bundle.externalBin` array to:
           ```json
           "externalBin": ["binaries/mcp-server", "binaries/section-parser-cli"]
           ```
         - **At runtime the MCP sidecar spawns the bundled CLI via an env var-derived path.** Pattern: at MCP-sidecar launch time, the Tauri app sets the env var `SECTION_PARSER_CLI_PATH` pointing at the bundled binary location (`<resource-dir>/binaries/section-parser-cli-<target-triple>`). The sidecar reads this env var at startup and caches the path; downstream tool implementations spawn it via `child_process.execFileSync(process.env.SECTION_PARSER_CLI_PATH, [], { input: body, encoding: "utf-8" })`. **08-02 must wire the env var injection in the Rust sidecar-launch code** (extending Phase 5's existing mcp-server CommandChild spawn block in `lib.rs` to also pass `SECTION_PARSER_CLI_PATH` via `.env("SECTION_PARSER_CLI_PATH", resolved_path)` on the Command builder). 08-06 references the same env var when wiring the MCP-sidecar→CLI invocation. Document the EXACT spawn invocation pattern + env var name in this task's SUMMARY so 08-02 + 08-06 can copy it verbatim. Resolution at the Rust side: `app.path().resource_dir().unwrap().join("binaries").join(format!("section-parser-cli-{target}"))` — same target-triple resolution Tauri uses for externalBin.
       - 08-06 will reference this spawn path when wiring the MCP-sidecar→CLI invocation; this plan ships the binary + the bundling config.

       NOTE: This binary is the resolution of RESEARCH.md Open Question 2 — IPC mechanism between MCP sidecar (TypeScript) and the canonical Rust parser. Per-call ~5ms; write paths are rare; no shared HTTP server needed.

    4. Create test fixtures under `src-tauri/tests/fixtures/`:
       - `contract_api_l3.md`: a complete v3 contract for an API L3 node with sections in some specific order (e.g., `## Intent`, `## Inputs`, `## Outputs`, `## Examples`, `## Invariants`).
       - `contract_ui_l4.md`: same content set but in different order (e.g., `## Examples` first, then `## Intent`, etc.) — tests order independence per Pitfall 1.
       - `contract_beat1_body.md`: the literal Beat 1 contract body from `.planning/demo/presentation-script.md` — copy `## Intent` (multi-paragraph), `## Role` (single paragraph), `## Examples` (multiple GIVEN/WHEN/THEN blocks). This is the demo-survival fixture per Pitfall 10.

    5. Create `src-tauri/tests/section_parser_tests.rs` with at minimum:
       - `parses_simple_h2_sections` — three sections, asserts BTreeMap keys sorted, each value is faithful source text.
       - `rejects_duplicate_h2` — two `## Intent` blocks → `ParseError::DuplicateHeading`.
       - `ignores_h2_inside_fenced_code` — code block contains `## Looks like a heading` line; parser must NOT treat it as a section.
       - `order_independence_round_trip` — load `contract_api_l3.md` and `contract_ui_l4.md` (same logical content, different order), assert `compute_section_hashes` returns equal maps.
       - `accepts_beat1_demo_body` — load `contract_beat1_body.md`, assert `parse_sections` returns Ok with at least keys `intent`, `role`, `examples`; assert section_hashes are deterministic across two consecutive calls (stability check).
       - `accepts_canonical_four_section_body` — synthesize a body containing `## Intent`, `## Role`, `## Examples`, AND `## Implicit Decisions` (one paragraph each). Assert `parse_sections` returns Ok with all four keys (lowercased: `intent`, `role`, `examples`, `implicit decisions`). Assert `compute_section_hashes` produces stable hashes across two consecutive calls. Round-trip through `write_sidecar` + `parse_sidecar` (after Task 2 lands `format_version: 3` write path) and assert the four section bodies survive byte-identical. **This test pins down the four-section convention 08-03's auto-rederive (Pass 2) writes against.**

    Run `cargo test -p contract-ide section_parser` from `contract-ide/src-tauri/` to verify ALL tests pass before moving on.
  </action>
  <verify>
    From `contract-ide/src-tauri/`:
    - `cargo build --release` (main app builds with new dep).
    - `cargo build -p section-parser-cli --release` OR equivalent for the standalone binary.
    - `echo "## Intent\n\nHello\n\n## Examples\n\nworld" | ./target/release/section-parser-cli` prints valid JSON with two section_hashes entries.
    - `cargo test --test section_parser_tests` passes ALL five tests above.
    - Fixture file `tests/fixtures/contract_beat1_body.md` exists and contains literal `## Intent`, `## Role`, `## Examples` headings.
  </verify>
  <done>
    pulldown-cmark dep added; section_parser module compiles and exports parse_sections + compute_section_hashes; section-parser-cli binary builds and produces correct JSON over stdin/stdout; five unit tests green including the two order-different fixtures producing identical hashes and the literal Beat 1 body parsing successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ContractFrontmatter to format_version 3 with 5 new fields + lazy-migration round-trip</name>
  <files>contract-ide/src-tauri/src/sidecar/frontmatter.rs, contract-ide/src-tauri/tests/fixtures/contract_api_l3.md, contract-ide/src-tauri/tests/section_parser_tests.rs</files>
  <action>
    1. Edit `src-tauri/src/sidecar/frontmatter.rs`:
       - Add `pub struct RollupInput { pub child_uuid: String, pub sections: Vec<String> }` deriving `Debug, Clone, Serialize, Deserialize`.
       - Extend `ContractFrontmatter` with five new fields (per RESEARCH.md Code Example 1):
         - `#[serde(default, skip_serializing_if = "BTreeMap::is_empty")] pub section_hashes: BTreeMap<String, String>` — present at all levels.
         - `#[serde(default, skip_serializing_if = "Vec::is_empty")] pub rollup_inputs: Vec<RollupInput>` — L1/L2/L3 only.
         - `#[serde(skip_serializing_if = "Option::is_none")] pub rollup_hash: Option<String>` — L1/L2/L3 only.
         - `#[serde(skip_serializing_if = "Option::is_none")] pub rollup_state: Option<String>` — `"fresh" | "stale" | "untracked"`.
         - `#[serde(skip_serializing_if = "Option::is_none")] pub rollup_generation: Option<u64>` — monotonic counter.
       - Confirm/keep existing `format_version: u32` field. The sidecar reader does NOT enforce a minimum version on parse (a v2 sidecar must still parse — missing fields default per `serde(default)`).
       - On the WRITE path (existing helper that builds frontmatter for `write_contract`), bump `format_version: 3` and (if level is L0) explicitly clear the four `rollup_*` fields to None / empty Vec; for L1/L2/L3, set them only if the caller provided them (08-02 will populate during rollup detection; 08-03's hook path doesn't touch them). The `section_hashes` field is populated by the writer calling `crate::sidecar::section_parser::compute_section_hashes(&body)` and inserting the result into the frontmatter immediately before YAML serialization.

       LAZY MIGRATION (Pitfall 7): the read path NEVER writes section_hashes back to disk. Reading a v2 contract returns a struct whose section_hashes field is empty by default; callers that need section_hashes call `compute_section_hashes(body)` on the body in memory. The format_version: 3 + section_hashes persistence ONLY happens on a real write through `write_contract`/`write_derived_contract`/`update_contract`.

    2. Add round-trip tests appended to `src-tauri/tests/section_parser_tests.rs` (or a sibling `frontmatter_v3_tests.rs`):
       - `frontmatter_v3_round_trip_l3` — build a frontmatter with all 5 new fields populated, serialize to YAML, parse back, assert byte-equal struct.
       - `frontmatter_v2_reads_without_persisting` — load a sidecar file with `format_version: 2` and no Phase 8 fields; assert `section_hashes` is empty BTreeMap; assert that calling the read path does NOT mutate the file on disk (compare mtime before/after, or read+write+read and ensure file content unchanged).
       - `frontmatter_l0_omits_rollup_fields` — write an L0 frontmatter, confirm serialized YAML does NOT contain `rollup_inputs`, `rollup_hash`, `rollup_state`, or `rollup_generation` keys.
       - `frontmatter_v3_serializes_section_hashes_alphabetically` — round-trip a frontmatter with section_hashes for keys ["zebra", "alpha", "mango"]; serialized YAML lists keys in alphabetical order (BTreeMap guarantee).

    3. Run `cargo test --test section_parser_tests` (or whichever test file holds the new tests) from `src-tauri/`. All four new tests must pass.

    NOTE on serde_yaml_ng field ordering: BTreeMap serializes in key order, but top-level struct fields serialize in declaration order. Place the 5 new fields AT THE END of the ContractFrontmatter struct so existing v2 fields keep their YAML position and v2-written sidecars round-trip byte-identically. Per STATE.md decision lineage: serde_yaml_ng preserves insertion/declaration order; do not break this.
  </action>
  <verify>
    `cargo test` from `contract-ide/src-tauri/` passes all section_parser + frontmatter v3 tests.
    `cargo build --release` from `contract-ide/src-tauri/` succeeds.
    `cargo clippy --all-targets -- -D warnings` from `contract-ide/src-tauri/` is clean.
    Read a Phase 7 sidecar fixture (any pre-existing `.contracts/<uuid>.md`) — manually invoke parse → reserialize → confirm content unchanged byte-for-byte (no v3 fields written).
  </verify>
  <done>
    ContractFrontmatter has 5 new fields under format_version: 3; L0 omits the rollup_* fields on serialize; v2 reads round-trip without persisting v3; section_hashes serializes alphabetically; cargo test + clippy clean; existing Phase 7 sidecars are untouched on disk.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migration v3 — Phase 8 columns + rollup_derived table</name>
  <files>contract-ide/src-tauri/src/db/migrations.rs</files>
  <action>
    1. Open `src-tauri/src/db/migrations.rs`. Add a third `Migration` entry `0003_phase8_propagation_and_receipts` (description text MUST NOT collide with v1/v2 — STATE.md notes migration descriptions are immutable once shipped).

    2. The migration's `sql` field runs:
       ```sql
       -- Add Phase 8 propagation columns to nodes (all nullable; lazy migration — values populated on first write)
       ALTER TABLE nodes ADD COLUMN section_hashes_json TEXT;     -- JSON object {section_name: sha256}
       ALTER TABLE nodes ADD COLUMN rollup_inputs_json TEXT;      -- JSON array of {child_uuid, sections[]}
       ALTER TABLE nodes ADD COLUMN rollup_hash TEXT;
       ALTER TABLE nodes ADD COLUMN rollup_state TEXT;            -- 'fresh' | 'stale' | 'untracked' | NULL (L0)
       ALTER TABLE nodes ADD COLUMN rollup_generation INTEGER NOT NULL DEFAULT 0;

       -- New derived-state table for rollup detection (parallels drift_state pattern from Phase 7)
       CREATE TABLE rollup_derived (
           node_uuid             TEXT PRIMARY KEY REFERENCES nodes(uuid) ON DELETE CASCADE,
           computed_rollup_hash  TEXT NOT NULL,
           stored_rollup_hash    TEXT,
           state                 TEXT NOT NULL,              -- 'fresh' | 'stale' | 'untracked'
           generation_at_check   INTEGER NOT NULL,
           checked_at            TEXT NOT NULL
       );

       CREATE INDEX idx_rollup_derived_state ON rollup_derived(state);

       -- Receipts table EXTENSION — v1 migration (db/migrations.rs:69-83) ALREADY shipped:
       --   receipts(id, session_id, transcript_path, started_at, finished_at, input_tokens,
       --     output_tokens, cache_read_tokens, tool_call_count, nodes_touched,
       --     estimated_cost_usd, raw_summary, created_at)
       -- Phase 8 ADDs three new columns via ALTER TABLE. We REUSE existing column names
       -- where they overlap — code MUST use `tool_call_count` (NOT `tool_calls`) and
       -- `estimated_cost_usd` (NOT `est_cost_usd`). 08-04 INSERTs/SELECTs MUST match
       -- this merged column list exactly.
       ALTER TABLE receipts ADD COLUMN raw_jsonl_path TEXT;
       ALTER TABLE receipts ADD COLUMN parse_status TEXT NOT NULL DEFAULT 'ok';   -- 'ok' | 'fallback_mock'
       ALTER TABLE receipts ADD COLUMN wall_time_ms INTEGER;

       -- receipt_nodes table ALSO already shipped in v1 (db/migrations.rs:85-89) with
       -- PRIMARY KEY (receipt_id, node_uuid) and idx_receipts_node_uuid index. Do NOT
       -- recreate either — they exist.
       ```

       **MERGED RECEIPTS COLUMN LIST (canonical for 08-04):**
       ```
       receipts(
         id                 TEXT PRIMARY KEY,           -- tracking_id from agent run (uuid v4)
         session_id         TEXT NOT NULL,
         transcript_path    TEXT NOT NULL,              -- absolute path to ~/.claude/projects/<key>/<session_id>.jsonl (v1 column)
         started_at         TEXT,                        -- ISO-8601, agent run start (v1 column; first user-line ts)
         finished_at        TEXT,                        -- ISO-8601, agent run end (v1 column; last assistant-line ts)
         input_tokens       INTEGER,
         output_tokens      INTEGER,
         cache_read_tokens  INTEGER,                     -- v1 column; sum of cache_read_input_tokens across assistant lines
         tool_call_count    INTEGER,                     -- v1 column — DO NOT alias as `tool_calls`
         nodes_touched      TEXT,                        -- v1 column; JSON array of UUIDs (alternate to receipt_nodes join)
         estimated_cost_usd REAL,                        -- v1 column — DO NOT alias as `est_cost_usd`
         raw_summary        TEXT,                        -- v1 column; opaque JSON for forward-compat
         created_at         TEXT NOT NULL DEFAULT (datetime('now')),
         raw_jsonl_path     TEXT,                        -- NEW v3 — absolute path to source JSONL (typically same as transcript_path)
         parse_status       TEXT NOT NULL DEFAULT 'ok',  -- NEW v3 — 'ok' | 'fallback_mock'
         wall_time_ms       INTEGER                      -- NEW v3 — Instant::now() delta around spawn (NOT JSONL ts diff)
       )
       ```
       08-04 (or 08-04a) MUST use these column names verbatim in INSERT/SELECT statements.

       NOTE: `started_at` and `finished_at` already exist on v1 schema, so 08-04 should populate THOSE columns rather than introducing a new `ts` column. The `wall_time_ms` is the authoritative wall-clock measurement (W3) and is independent of started_at/finished_at JSONL-derived values. `transcript_path` and `raw_jsonl_path` will typically be the same string — keep both populated for forward-compat (other tools may set transcript_path differently).

    3. Set `kind: MigrationKind::Up` and append this Migration to the Vec returned by `get_migrations()`. Do NOT modify v1 or v2 — they are immutable per STATE.md Plan 01-02 lineage.

    4. Manual verification: launch the Tauri dev build against a clean profile dir (or reset SQLite). After startup, open `~/Library/Application Support/com.contract-ide.app/contract-ide.db` with `sqlite3` and confirm:
       - `PRAGMA table_info(nodes)` lists `section_hashes_json`, `rollup_inputs_json`, `rollup_hash`, `rollup_state`, `rollup_generation`.
       - `.tables` lists `rollup_derived`, `receipts`, `receipt_nodes`.
       - `PRAGMA user_version` or migration tracking confirms v3 ran exactly once.

    NOTE: This plan does NOT wire any Rust code to read/write these new columns — that's 08-02 (rollup) and 08-04 (receipts). This task only ships the schema so subsequent plans can land in parallel waves without DB-shape uncertainty.
  </action>
  <verify>
    `cargo build --release` from `contract-ide/src-tauri/` succeeds.
    `cargo clippy --all-targets -- -D warnings` clean.
    Reset SQLite (delete `~/Library/Application Support/com.contract-ide.app/contract-ide.db`), launch app via `npm run tauri dev`, confirm schema via `sqlite3 <db> ".schema nodes"` and `".schema rollup_derived"` and `".schema receipts"`.
    `npm run tauri build` (debug or release) produces an `.app` bundle without runtime errors at first launch.
  </verify>
  <done>
    Migration v3 lands and runs exactly once on a fresh DB; nodes table has 5 new columns; rollup_derived + receipts + receipt_nodes tables exist; idx_rollup_derived_state and idx_receipt_nodes_node indexes exist; v1 + v2 migrations untouched; cargo build + clippy clean; app launches against fresh DB without error.
  </done>
</task>

</tasks>

<verification>
- All section_parser tests + frontmatter v3 tests pass: `cargo test` from `src-tauri/`.
- Section parser accepts the literal Beat 1 contract body from `presentation-script.md` and produces stable section_hashes.
- Two committed fixtures (`contract_api_l3.md`, `contract_ui_l4.md`) with sections in different orders produce identical section_hashes maps.
- v2 sidecars on disk are NOT rewritten on app launch (lazy migration verified — `find .contracts -name "*.md" -newer <before-launch> | wc -l` returns 0 after launch).
- L0 sidecars do NOT contain rollup_* keys in their YAML frontmatter when rewritten under v3.
- `section-parser-cli` binary in `src-tauri/binaries/` builds and produces correct JSON for `## Intent\n\nFoo\n\n## Examples\n\nBar` input.
- Migration v3 runs exactly once; rollup_derived + receipts + receipt_nodes tables present after first launch on a fresh DB.
- `cargo build --release && cargo clippy --all-targets -- -D warnings` clean from `contract-ide/src-tauri/`.
- `npm run tsc` clean from `contract-ide/` (no TS code changed; smoke gate).
</verification>

<success_criteria>
- ContractFrontmatter v3 round-trips through serde_yaml_ng for L0..L4 (Pitfall 1, Pitfall 7).
- section_parser is the single canonical implementation (no parallel TS impl); Rust + section-parser-cli expose the same compute_section_hashes API.
- BTreeMap (alphabetical sort) ensures order independence — verified by API L3 + UI L4 fixtures producing identical hashes despite different section ordering.
- Beat 1 demo body from `presentation-script.md` is the load-bearing fixture and parses successfully — Pitfall 10 closed.
- Lazy migration verified: reading a v2 sidecar produces v3-shaped struct in memory but does NOT write to disk.
- Migration v3 lands schema for rollup_derived + receipts (so 08-02 and 08-04 can run in their respective waves without DB-shape coordination).
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-loop-receipts-posttooluse-hook-cherrypick-cross-level-propagation/08-01-SUMMARY.md` documenting:
- Final pulldown-cmark API used (offset_iter pattern; any deviations from RESEARCH.md Pattern 1)
- section-parser-cli location and how MCP sidecar will spawn it (path + invocation pattern for 08-02/08-06)
- Whether `receipts` table existed before v3 migration (and the chosen ALTER vs CREATE strategy)
- Lazy migration test result: was any v2 sidecar mutated on launch? (must be 0)
- Any deviation from the file lattice in RESEARCH.md "Recommended Project Structure"
</output>
