---
phase: 13-substrate-ui-demo-polish
plan: 11
type: execute
wave: 5
depends_on: ["13-10"]
files_modified:
  - .planning/demo/runbook-v2.md
  - .planning/demo/live-scenario.md
  - .planning/demo/rehearsal-log.md
  - contract-ide/demo/seeds/blast-radius.json
  - contract-ide/demo/seeds/beat3-verifier.json
  - .planning/phases/13-substrate-ui-demo-polish/13-UAT.md
autonomous: false
requirements:
  - DEMO-04
must_haves:
  truths:
    - "runbook-v2.md is rewritten to mirror presentation-script.md structure: Hook + Beat 1 + Beat 2 + Beat 3 + Beat 4 + Close, with implementation-status annotations per beat and pre-recording/rehearsal checklists updated to the committed scenario"
    - "live-scenario.md is REPLACED — old single-prompt button-color placeholder superseded by the Beat 4 workspace-delete moment as the live moment of the demo (cross-references presentation-script.md instead of duplicating)"
    - "Real Phase 9 UUIDs substituted into blast-radius.json + beat3-verifier.json (placeholders eliminated)"
    - "4-beat demo runs end-to-end 3 times in a row before filming with full Beat 1 → Beat 4 arc; reset-demo.sh restores deterministic state between runs in <15s"
    - "Each rehearsal run logged to rehearsal-log.md with: timestamp, total runtime, per-beat duration, any glitches, decision (record-take or fix-and-retry)"
    - "All 11 SCs verified by walking the demo: Cmd+P intent search, ScreenCard with iframe, ServiceCard structured rendering, vertical chain with edge labels, sidebar replacing L0/L1, substrate-state coloring across new node types, chat archaeology in ≤5s, PR-review intent-drift, mocked Sync, 4-beat reproducibility, verifier with implicit-decisions group + promoted-rule badge"
    - "Fallback beat map documented in runbook-v2.md: which beats still work if specific upstream phases are missing (per 13-RESEARCH.md Risk 5)"
  artifacts:
    - path: ".planning/demo/runbook-v2.md"
      provides: "Rewritten runbook with 4-beat structure + per-beat status + rehearsal checklists"
      contains: "Beat 1"
    - path: ".planning/demo/live-scenario.md"
      provides: "Replaced — points to Beat 4 workspace-delete; references presentation-script.md"
      contains: "Beat 4"
    - path: ".planning/demo/rehearsal-log.md"
      provides: "Log of 3+ end-to-end rehearsals with timestamps + glitches + outcomes"
      contains: "Run 1"
    - path: ".planning/phases/13-substrate-ui-demo-polish/13-UAT.md"
      provides: "Phase 13 UAT script — manual verification of all 11 SCs in order"
      contains: "SC 1"
  key_links:
    - from: ".planning/demo/runbook-v2.md"
      to: ".planning/demo/presentation-script.md"
      via: "Mirrors structure; cross-references for canonical performance details"
      pattern: "presentation-script.md"
    - from: ".planning/demo/live-scenario.md"
      to: ".planning/demo/presentation-script.md § Beat 4"
      via: "References Beat 4 as the live moment; no duplicate prose"
      pattern: "Beat 4"
    - from: "rehearsal-log.md"
      to: "reset-demo.sh + presentation-script.md"
      via: "Logs each rehearsal: reset → walk script → measure → glitches → decision"
      pattern: "reset-demo.sh"
---

<objective>
Close out Phase 13 (and the milestone) by:

1. **Substituting real UUIDs** into the JSON fixtures (replaces placeholders left in plan 13-10)
2. **Rewriting `runbook-v2.md`** to align with the locked 4-beat two-laptop presentation-script.md
3. **Replacing `live-scenario.md`** to point at Beat 4 as the live moment
4. **Running 3 end-to-end rehearsals** with the reset-demo.sh loop, logging each to rehearsal-log.md
5. **Writing 13-UAT.md** — the Phase 13 user-acceptance test script that walks all 11 SCs

Purpose: Per ROADMAP SC 10 + REQUIREMENTS DEMO-04, the milestone is "the demo runs end-to-end 3 times in a row before filming." This is the final gate. Without 3 successful runs, the demo isn't filmable. Plan 13-11 makes that concrete: rehearse, log, fix issues found, repeat until 3 consecutive clean runs.

Per ROADMAP planning notes (absorbed deliverables): "Phase 13 absorbs the runbook rewrite for `runbook-v2.md` (currently disclaimed as structurally outdated, gated on scenario lock — now unblocked) and the live-scenario.md replacement (the live moment is now Beat 4 of presentation-script, not a separate single-prompt artifact)."

Output: Updated fixtures with real UUIDs, rewritten runbook-v2.md + live-scenario.md, rehearsal-log.md with ≥3 successful runs, and 13-UAT.md script for human verification of all 11 SCs.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/CANVAS-PURPOSE.md
@.planning/demo/presentation-script.md
@.planning/demo/scenario-criteria.md
@.planning/phases/13-substrate-ui-demo-polish/13-RESEARCH.md
@.planning/phases/13-substrate-ui-demo-polish/13-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substitute real Phase 9 UUIDs into fixtures + rewrite runbook-v2.md + replace live-scenario.md</name>
  <files>
    contract-ide/demo/seeds/blast-radius.json
    contract-ide/demo/seeds/beat3-verifier.json
    .planning/demo/runbook-v2.md
    .planning/demo/live-scenario.md
  </files>
  <action>
**Step 1 — Substitute UUIDs.**

Read the actual UUIDs Phase 9 generated when seeding the contracts. They live in `.contracts/*.md` frontmatter (`uuid:` field) within the `contract-ide-demo` repo.

Find each placeholder UUID in the fixtures and replace with the real value:

```bash
# Discovery (run from contract-ide-demo repo root):
grep -rh "^uuid:" .contracts/ | awk '{print $2}'

# Manually identify which uuid is which by reading the contract `name` and `kind` fields.
# Then edit the JSON files in contract-ide/demo/seeds/.
```

Specific substitutions needed in `blast-radius.json`:
- `<UUID-account-settings-screen>` → the L3 contract for AccountSettings page
- `<UUID-api-account-delete>` → the api/account/delete endpoint contract
- `<UUID-begin-account-deletion-lib>` → the lib function contract
- `<UUID-stripe-customers-archive>` → the stripe.customers.update participant contract
- `<UUID-mailchimp-suppress>` → the mailchimp.suppress participant contract
- `<UUID-send-deletion-email>` → the email-send lib function contract

Same in `beat3-verifier.json` for `<UUID-account-settings-screen>` (used as `parentSurfaceUuid`).

If any contract is missing from Phase 9's seed work, FILE A BLOCKER:
- Document in 13-UAT.md as "Phase 9 contract gap: <name>"
- Use a temporary placeholder that won't crash but won't render correctly either
- Plan 13-11 rehearsal will fail-fast and surface the blocker

**Step 2 — Rewrite `.planning/demo/runbook-v2.md`.**

Use the Write tool. The new runbook must mirror presentation-script.md's 4-beat structure with implementation-status annotations.

```markdown
# Runbook v2 — 4-Beat Two-Laptop Demo

**Purpose:** Operational handbook for filming the locked 4-beat two-laptop demo per `.planning/demo/presentation-script.md`. This document is the rehearsal + filming reference; the script remains the canonical performance spec.

**Status:** Aligned with the 2026-04-24 visual model lock + scenario lock. Replaces the structurally-outdated previous runbook that referenced an abstract zoom canvas + 3-beat recorded video.

## Pre-flight checklist (run BEFORE every recording attempt)

- [ ] `contract-ide-demo` repo at the locked commit (`git log --oneline -1` matches the demo-locked SHA)
- [ ] Both laptops on stage; projector tested switching between them
- [ ] Reset script executed cleanly within <15s (`bash contract-ide/demo/reset-demo.sh`)
- [ ] App boots; `Cmd+P` returns expected hits for "account settings" and "soft delete"
- [ ] Sidebar shows demo flows (`flow-delete-account`, `flow-delete-workspace`)
- [ ] DemoOrchestrationPanel visible bottom-left (dev mode)
- [ ] Localhost:3000 dev server (Next.js) reachable for ScreenCard iframe (Account Settings page renders)
- [ ] DBPath at `~/Library/Application Support/com.contract-ide.app/contract-ide.db` populated with seed (substrate_nodes count = 18)

## Beat 1 — PM trigger (NT laptop, ~35s)

**Implementation status:** ✅ wiring shipped (Cmd+P plan 13-03; ScreenCard plan 13-05; chip resolution plan 13-05 fallback)

**Steps (per presentation-script.md):**
1. Projector cuts to NT laptop. Brief flash of ticket #4471.
2. NT presses `⌘P`, types `account settings danger`. (≥80% top-1 precision validated by plan 13-03 test fixture; otherwise reduces to "type fully and click hit".)
3. NT picks `AccountSettings.DangerZone`. Canvas transitions to L3 trigger view (rendered iframe). Danger Zone chip auto-focuses (uses `focusedAtomUuid` from plan 13-03 → halo on the chip in plan 13-05).
4. NT clicks the chip directly. Inspector opens with simplified Copy Mode editor.
5. NT types Intent + Role + Examples (per § Beat 1 of script — verbatim).
6. NT clicks `Delegate to agent`. Status pill: "Sent to agent."

**Demo orchestration:** None — this beat runs through normal app surfaces. Validate the chip-resolution chain end-to-end during rehearsal.

**Glitch playbook:**
- Cmd+P returns wrong top-1 → use second-place hit (still in top-3 acceptable per script)
- Iframe shows "unreachable" → start dev server before next take; consider warming the dev server before recording

## Beat 2 — Recorded comparison (~75s)

**Implementation status:** RECORDED — not live. Generate the recording before filming day per § Beat 2.

**Steps:**
1. Projector cuts to full-screen recorded comparison: Contract IDE left pane vs bare Claude Code right pane.
2. Substrate query streams 5 hits per § Beat 2.
3. Both agents complete. Rubric panel shows 5/0 ✓/✗.
4. Receipt banner: Contract IDE ~1,400 tokens / 3 tool calls / 5/5 honored vs Bare ~7,200 / 22 / 0/5.

**Demo orchestration:** This beat is recorded video — replay during the live demo. Recording quality is the dependency.

## Beat 3 — Developer review (T laptop, ~70s)

**Implementation status:** ✅ Sync animation (plan 13-09); ✅ VerifierPanel with implicit-decisions group + orange flag (plan 13-09); ✅ Citation halo wiring (plan 13-07); STAGED via demo-orchestration panel.

**Steps:**
1. Projector cuts to T laptop. FlowChainLayout already shows the delete-account vertical chain.
2. T clicks `Sync` (in canvas toolbar — see plan 13-09).
3. **DEMO ORCHESTRATION:** Sync triggers blast-radius — trigger card pulses, then service cards pulse 50ms apart down chain.
4. Sidebar (or side-panel rendered by another component) shows 6-line intent summary with citations.
5. T clicks `[source]` on Stripe-archive line. SourceArchaeologyModal opens (plan 13-07) AND the corresponding service card halos.
6. T clicks `Verify against intent`. **DEMO ORCHESTRATION:** click "Beat 3: Verifier results" in DemoOrchestrationPanel → loads beat3-verifier.json fixture.
7. VerifierPanel renders: 6 ✓ honors + ℹ implicit decisions (3 rows) + ⚠ orange flag.
8. Orange-flag halo lands on the SCREEN CARD (Account Settings).
9. T clicks the flag. Side panel opens with priority history (Q4-2025 → 2026-04-24 shift).
10. T types narrowing note + Accept + Merge. (For demo: typing is real but the substrate write is staged.)

**Demo orchestration:** Beat 3 has TWO orchestration moments — Sync click (real IPC) + "Beat 3: Verifier results" (DemoOrchestrationPanel button to load fixture).

**Glitch playbook:**
- Sync animation doesn't fire → check substrate store hydration (Pitfall 3); use DemoOrchestrationPanel "Sync animation" button as backup.
- Orange flag halos a service card instead of screen card → BUG — verify `parentSurfaceUuid` in beat3-verifier.json matches the actual screen-card uuid in seeded contracts.
- Verifier doesn't appear → click "Beat 3: Verifier results" again; if still nothing, check console for fixture load error.

## Beat 4 — Closed loop (T laptop + recorded inset, ~35s)

**Implementation status:** ✅ HarvestPanel (plan 13-09); ✅ promoted-from-implicit badge (plan 13-09); ⚠ recorded inset (filmed separately).

**Steps:**
1. Notification slides in: "5 nodes captured from this session..."
2. New ticket flashes (workspace-delete enterprise customer).
3. T types prompt in Claude Code terminal: `add a delete-workspace button to the team settings page`.
4. (RECORDED INSET runs concurrently in another window: bare Claude failing on same prompt.)
5. Substrate query streams; agent writes 5-file change first try.
6. Canvas updates: `flow-delete-workspace` chain renders (vertical chain similar to morning's). Ghost-ref participants for stripe + mailchimp.
7. **DEMO ORCHESTRATION:** Click "Beat 4: Harvest panel" in DemoOrchestrationPanel → emits substrate:nodes-added event with 3 new rules.
8. HarvestPanel slides in bottom-right with 3 entries; one carries `[⌃ promoted from implicit]` badge.

**Demo orchestration:** Beat 4 has ONE orchestration moment — the harvest panel trigger.

**Glitch playbook:**
- Workspace-delete flow chain doesn't render → check that Phase 9 seeded `flow-delete-workspace` contract with valid members.
- HarvestPanel doesn't appear → click "Beat 4: Harvest panel" button again; if event subscription is broken, the 2s poll fallback should pick it up within 2 seconds.
- Promoted badge missing → check beat4-harvest.json — the third entry must have `promoted_from_implicit: true`.

## Close (~10s)

**Implementation status:** N/A — speakers in frame.

## Fallback beat map (per 13-RESEARCH.md Risk 5)

If specific upstream phases are missing or broken at film time, what beats still work?

| Missing | Beats still functional |
|---|---|
| Phase 9 BABEL-01 (data-contract-uuid injection) | Beats 2 (recorded), 3, 4 — chip overlay falls back to default-position chips |
| Phase 9 FLOW-01 (members) | Beats 2 (recorded) — flow chain doesn't assemble; fall back to recording the chain ahead of time |
| Phase 9 BACKEND-FM-01 | Beats 2 (recorded), 3 (verifier still works), 4 — service cards show "no schema declared" placeholders, demo readability degraded |
| Phase 11 distiller | Beats 2 (recorded), 3 (Sync STAGED), 4 — substrate is hand-seeded so retrieval still works |
| Phase 12 supersession engine | Beats 2 (recorded), 4 — Beat 3 orange flag is STAGED via beat3-verifier.json fixture so works regardless |
| Phase 10 session watcher | Beats 1, 2 (recorded), 3 — Beat 4 harvest panel is STAGED via DemoOrchestrationPanel button |

In a worst-case (multiple phases missing), all four beats still demonstrate via DemoOrchestrationPanel staging — the script gracefully degrades to a "scripted demo" rather than a "live demo" but visually identical.

## Filming day routine

1. **2h before:** Reset script run cleanly 3 times in a row. Log results in rehearsal-log.md. ZERO red flags allowed before filming.
2. **30m before:** Both laptops on table; projector tested; both running `npm run tauri dev` OR the `.app` bundle.
3. **5m before:** Final reset on each laptop; verify Cmd+P precision query.
4. **Take 1:** Run full script. If glitch, note in rehearsal-log.md and reset for take 2.
5. **Up to 5 takes:** Pick best take in post.

## Recording-day decisions matrix

| Glitch severity | Decision |
|---|---|
| Tiny visual glitch (<1s, off-camera) | Continue take |
| Moderate (1-3s, on-camera but recoverable) | Continue, may use in post |
| Major (>3s, breaks narrative) | Cut, reset, redo |
| Demo-orchestration button missed | Cut, reset (no good way to recover live) |

## What this runbook does NOT cover

- Audio mixing, lower-third graphics, post-production: filming-team responsibility
- B-roll: covered by `.planning/demo/scenario-criteria.md` if needed
- Pitch slides: separate deck, owned by `.planning/PITCH.md`

---

*Runbook revised 2026-04-25 (Phase 13 plan 11). See `presentation-script.md` for canonical performance prose; this runbook is the operational handbook.*
```

**Step 3 — Replace `.planning/demo/live-scenario.md`.**

```markdown
# Live Scenario — Beat 4 Workspace Delete

**Status:** SUPERSEDED 2026-04-25 by `.planning/demo/presentation-script.md` § Beat 4. The "live moment" of the demo is now Beat 4 of the 4-beat structure (workspace-delete prompt on T's laptop with substrate compounding visible), NOT a separate single-prompt artifact.

This file is preserved as a redirect for any external reference; the canonical content lives in the script.

**See:** `.planning/demo/presentation-script.md` § Beat 4 — Closed loop (3:15–3:50)

**Why replaced:** The original `live-scenario.md` was placeholder content (button-color brand-voice scenario) for an open scenario decision. The committed delete-account → workspace-delete scenario locked 2026-04-24 absorbed the live moment into Beat 4 of the 4-beat structure. Per ROADMAP planning note: "Phase 13 absorbs ... the live-scenario.md replacement (the live moment is now Beat 4 of presentation-script, not a separate single-prompt artifact)."

**For the live moment specifically:** Beat 4 includes:
- T types `add a delete-workspace button to the team settings page` in Claude Code terminal
- Substrate query surfaces all 5 morning rules
- Agent writes 5-file change first try
- HarvestPanel renders 3 new rules (one promoted-from-implicit)

That entire arc is the live moment, replacing the original "single prompt" framing. See script for verbatim staging.
```

**Avoid:**
- DO NOT duplicate prose from presentation-script.md into runbook-v2.md — runbook is operational; script is canonical performance.
- DO NOT delete the original live-scenario.md outright — the redirect-style replacement preserves any external references and explains the supersession.
- DO NOT proceed to rehearsal without UUID substitution — placeholders cause Sync to fire against empty state (Pitfall 3).
  </action>
  <verify>
`grep -c '<UUID-' contract-ide/demo/seeds/blast-radius.json` returns 0 (all placeholders substituted).
`grep -c '<UUID-' contract-ide/demo/seeds/beat3-verifier.json` returns 0.
`jq -e '.trigger_uuid | length > 8' contract-ide/demo/seeds/blast-radius.json` exits 0 (uuid is real).
`grep -c "Beat 1\\|Beat 2\\|Beat 3\\|Beat 4" .planning/demo/runbook-v2.md` returns at least 4.
`grep -c "Beat 4" .planning/demo/live-scenario.md` returns at least 1.
`grep -c "presentation-script.md" .planning/demo/runbook-v2.md` returns at least 2.
  </verify>
  <done>
Real UUIDs substituted into both fixtures. Runbook-v2.md rewritten with 4-beat structure + per-beat status + glitch playbook + fallback map. live-scenario.md replaced with redirect-style file pointing at presentation-script § Beat 4.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run 3 consecutive end-to-end rehearsals + log to rehearsal-log.md + write 13-UAT.md</name>
  <files>(human verification — no files written by this task)</files>
  <action>
**Built (subject of verification):**
Three (or more) consecutive clean rehearsals of the 4-beat demo using reset-demo.sh between each. Each rehearsal logged to rehearsal-log.md with timestamp / total runtime / per-beat duration / any glitches / decision (record-take or fix-and-retry). 13-UAT.md script for human verification of all 11 SCs.

**Verification protocol — execute and observe:**
**Step A — Write `.planning/phases/13-substrate-ui-demo-polish/13-UAT.md`.**

Use Write to create:

```markdown
# Phase 13 UAT — 11 Success Criteria + DEMO-04

**Purpose:** Manual verification of all 11 Phase 13 success criteria + DEMO-04 reproducibility. Walks the demo arc end-to-end and asserts each SC is satisfied. Run after plan 13-11 fixture substitution + runbook rewrite; precedes filming.

**Status:** Pending. Run when ready to gate Phase 13 closure.

## Setup

1. `bash contract-ide/demo/reset-demo.sh` — verify <15s elapsed
2. App boots; demo repo open

## SC 1 — Cmd+P by intent
- [ ] Press Cmd+P → palette opens (no Print dialog)
- [ ] Type "account settings danger" → top-1 hit is the AccountSettings.DangerZone atom contract OR the AccountSettings flow
- [ ] Run `npx vitest run cmdp-precision` → ≥8/10 (≥80% top-1)
- [ ] Selecting a flow lands at L2 chain view; selecting an atom lands at L3 with chip auto-focused

## SC 2 — ScreenCard with iframe + atom chip overlay
- [ ] Open AccountSettings flow chain
- [ ] ScreenCard shows live iframe of /account/settings
- [ ] Atom chips render at correct positions over iframe components
- [ ] Click empty Danger Zone region → chip resolves to AccountSettings.DangerZone atom contract
- [ ] Resize window → chip positions update

## SC 3 — ServiceCard / EndpointCard
- [ ] Service cards in chain show method badge (POST / DELETE)
- [ ] Method colors correct (POST green, DELETE red)
- [ ] Method+path in monospace
- [ ] Inputs / Outputs / Side effects sections render from contract frontmatter
- [ ] Atom chips beside cards show correct state coloring

## SC 4 — Vertical participant chain layout
- [ ] Flow chain renders top-to-bottom in members order
- [ ] 1 trigger card + 6+ participant cards visible
- [ ] Edges render call-shape labels matching prev.Outputs → next.Inputs
- [ ] FPS stays ≥50 during pan (Chrome DevTools Performance tab)
- [ ] Two-flow case: focused flow shows live iframe; non-focused shows screenshot

## SC 5 — Sidebar replacing L0/L1
- [ ] Sidebar shows area tree (NOT lens switcher)
- [ ] Per-area badges show drift / rollup-stale / intent-drifted counts
- [ ] Areas expand to show flows
- [ ] Clicking a flow drives canvas to its L2 view

## SC 6 — Substrate-state coloring
- [ ] Drift state renders red on cards/chips
- [ ] Rollup-stale renders amber
- [ ] Intent-drifted renders distinct orange (not amber!)
- [ ] Superseded renders muted orange
- [ ] Precedence: red > orange > amber > gray verified by inspection

## SC 7 — Chat archaeology
- [ ] Click `[source]` citation pill on any substrate node
- [ ] Modal opens within ≤500ms (well under 5s budget)
- [ ] Verbatim quote renders inline
- [ ] Citation in Beat 3 sidebar additionally halos the corresponding service card / chip

## SC 8 — PR-review intent-drift
- [ ] Cmd+Shift+P → PRReviewPanel slides in
- [ ] Paste sample diff → click Analyze
- [ ] Affected nodes highlight orange (intent_drifted)
- [ ] Explanation sidebar lists affected by participant in ≤30s readable
- [ ] Cancel button restores all highlights cleanly

## SC 9 — Mocked Sync affordance
- [ ] Click Sync button on canvas
- [ ] Trigger card pulses first
- [ ] Service cards pulse in invocation order with 50ms stagger
- [ ] Pulse fades after ~1.5s; substrate state settles to fresh

## SC 10 — 4-beat reproducibility
- [ ] Reset script restores deterministic state in <15s
- [ ] Walked Beat 1 → Beat 4 in <4 minutes
- [ ] 3 consecutive runs documented in rehearsal-log.md

## SC 11 — Verifier with implicit-decisions + promoted badge
- [ ] Click DemoOrchestrationPanel "Beat 3: Verifier results"
- [ ] VerifierPanel shows 6 ✓ honor rows
- [ ] ℹ "Implicit decisions" group shows exactly 3 rows: 24h email-link / audit_log / async cleanup
- [ ] ⚠ orange flag halos the SCREEN CARD (parent surface), not a service card
- [ ] Click DemoOrchestrationPanel "Beat 4: Harvest panel"
- [ ] HarvestPanel shows 3 new rules; one carries `[⌃ promoted from implicit]` amber badge

## DEMO-04 — Per-beat acceptance criteria

For each beat, verify the script line items pass.

[Beat 1 — Beat 4 acceptance criteria; copy from script as needed]

## Sign-off

- Run 1 timestamp: _____ Total: _____ Glitches: _____
- Run 2 timestamp: _____ Total: _____ Glitches: _____
- Run 3 timestamp: _____ Total: _____ Glitches: _____

[ ] All 11 SCs verified
[ ] 3 consecutive clean runs achieved
[ ] No blockers remaining
[ ] Phase 13 closed; ready for filming
```

**Step B — Write `.planning/demo/rehearsal-log.md` initial version + run 3 rehearsals.**

```markdown
# 4-Beat Demo Rehearsal Log

**Purpose:** Track each rehearsal run of the 4-beat demo until 3 consecutive clean runs are achieved.

## Run 1 — [date/time]

- Reset elapsed: ___s
- Total runtime: ___:___
- Per-beat duration: B1: ___s | B2: ___s | B3: ___s | B4: ___s
- Glitches: [list]
- Decision: [continue | reset and retry]

## Run 2 — [date/time]
[same format]

## Run 3 — [date/time]
[same format]

## Sign-off

- [ ] 3 consecutive runs completed
- [ ] Zero blocking glitches in runs 2-3
- [ ] Reset script reliable (<15s every time)
- [ ] Ready to film

## Glitch summary across all runs

[List any recurring issues + fixes applied between runs]
```

**Step C — RUN THE 3 REHEARSALS.**

This step is HUMAN-DRIVEN — the user runs the demo end-to-end and logs results. The rehearsal flow:

1. `bash contract-ide/demo/reset-demo.sh`. Time it. Note elapsed.
2. Open the demo repo in the relaunched app.
3. Walk Beats 1-4 of presentation-script.md exactly. Time each beat.
4. Note any glitches or visual issues.
5. After each run, append to rehearsal-log.md.
6. Reset and repeat.

Continue until 3 consecutive runs have ZERO blocking glitches. Common issues to expect:
- UUID substitution missed → Sync animation fires against empty state
- Phase 9 contract gap → ScreenCard shows "no schema declared"
- Substrate not populated → Cmd+P returns no hits

Fix issues between runs, document fixes in rehearsal-log.md, restart counter on each fix.

**Step D — Sign off 13-UAT.md.**

After 3 clean runs, complete the 13-UAT.md checklist. Any unchecked SC = blocker. Document blockers and the fix path before filming.

**Avoid:**
- DO NOT skip the rehearsal because "it worked once" — DEMO-04 is explicit: 3 in a row.
- DO NOT count runs with glitches toward the 3 — counter resets on glitch.
- DO NOT modify presentation-script.md during rehearsal — script is canonical; if the implementation can't match script, the implementation is wrong (file an issue).
  </action>
  <verify>
**Step A — Write `.planning/phases/13-substrate-ui-demo-polish/13-UAT.md`.**

Use Write to create:

```markdown
# Phase 13 UAT — 11 Success Criteria + DEMO-04

**Purpose:** Manual verification of all 11 Phase 13 success criteria + DEMO-04 reproducibility. Walks the demo arc end-to-end and asserts each SC is satisfied. Run after plan 13-11 fixture substitution + runbook rewrite; precedes filming.

**Status:** Pending. Run when ready to gate Phase 13 closure.

## Setup

1. `bash contract-ide/demo/reset-demo.sh` — verify <15s elapsed
2. App boots; demo repo open

## SC 1 — Cmd+P by intent
- [ ] Press Cmd+P → palette opens (no Print dialog)
- [ ] Type "account settings danger" → top-1 hit is the AccountSettings.DangerZone atom contract OR the AccountSettings flow
- [ ] Run `npx vitest run cmdp-precision` → ≥8/10 (≥80% top-1)
- [ ] Selecting a flow lands at L2 chain view; selecting an atom lands at L3 with chip auto-focused

## SC 2 — ScreenCard with iframe + atom chip overlay
- [ ] Open AccountSettings flow chain
- [ ] ScreenCard shows live iframe of /account/settings
- [ ] Atom chips render at correct positions over iframe components
- [ ] Click empty Danger Zone region → chip resolves to AccountSettings.DangerZone atom contract
- [ ] Resize window → chip positions update

## SC 3 — ServiceCard / EndpointCard
- [ ] Service cards in chain show method badge (POST / DELETE)
- [ ] Method colors correct (POST green, DELETE red)
- [ ] Method+path in monospace
- [ ] Inputs / Outputs / Side effects sections render from contract frontmatter
- [ ] Atom chips beside cards show correct state coloring

## SC 4 — Vertical participant chain layout
- [ ] Flow chain renders top-to-bottom in members order
- [ ] 1 trigger card + 6+ participant cards visible
- [ ] Edges render call-shape labels matching prev.Outputs → next.Inputs
- [ ] FPS stays ≥50 during pan (Chrome DevTools Performance tab)
- [ ] Two-flow case: focused flow shows live iframe; non-focused shows screenshot

## SC 5 — Sidebar replacing L0/L1
- [ ] Sidebar shows area tree (NOT lens switcher)
- [ ] Per-area badges show drift / rollup-stale / intent-drifted counts
- [ ] Areas expand to show flows
- [ ] Clicking a flow drives canvas to its L2 view

## SC 6 — Substrate-state coloring
- [ ] Drift state renders red on cards/chips
- [ ] Rollup-stale renders amber
- [ ] Intent-drifted renders distinct orange (not amber!)
- [ ] Superseded renders muted orange
- [ ] Precedence: red > orange > amber > gray verified by inspection

## SC 7 — Chat archaeology
- [ ] Click `[source]` citation pill on any substrate node
- [ ] Modal opens within ≤500ms (well under 5s budget)
- [ ] Verbatim quote renders inline
- [ ] Citation in Beat 3 sidebar additionally halos the corresponding service card / chip

## SC 8 — PR-review intent-drift
- [ ] Cmd+Shift+P → PRReviewPanel slides in
- [ ] Paste sample diff → click Analyze
- [ ] Affected nodes highlight orange (intent_drifted)
- [ ] Explanation sidebar lists affected by participant in ≤30s readable
- [ ] Cancel button restores all highlights cleanly

## SC 9 — Mocked Sync affordance
- [ ] Click Sync button on canvas
- [ ] Trigger card pulses first
- [ ] Service cards pulse in invocation order with 50ms stagger
- [ ] Pulse fades after ~1.5s; substrate state settles to fresh

## SC 10 — 4-beat reproducibility
- [ ] Reset script restores deterministic state in <15s
- [ ] Walked Beat 1 → Beat 4 in <4 minutes
- [ ] 3 consecutive runs documented in rehearsal-log.md

## SC 11 — Verifier with implicit-decisions + promoted badge
- [ ] Click DemoOrchestrationPanel "Beat 3: Verifier results"
- [ ] VerifierPanel shows 6 ✓ honor rows
- [ ] ℹ "Implicit decisions" group shows exactly 3 rows: 24h email-link / audit_log / async cleanup
- [ ] ⚠ orange flag halos the SCREEN CARD (parent surface), not a service card
- [ ] Click DemoOrchestrationPanel "Beat 4: Harvest panel"
- [ ] HarvestPanel shows 3 new rules; one carries `[⌃ promoted from implicit]` amber badge

## DEMO-04 — Per-beat acceptance criteria

For each beat, verify the script line items pass.

[Beat 1 — Beat 4 acceptance criteria; copy from script as needed]

## Sign-off

- Run 1 timestamp: _____ Total: _____ Glitches: _____
- Run 2 timestamp: _____ Total: _____ Glitches: _____
- Run 3 timestamp: _____ Total: _____ Glitches: _____

[ ] All 11 SCs verified
[ ] 3 consecutive clean runs achieved
[ ] No blockers remaining
[ ] Phase 13 closed; ready for filming
```

**Step B — Write `.planning/demo/rehearsal-log.md` initial version + run 3 rehearsals.**

```markdown
# 4-Beat Demo Rehearsal Log

**Purpose:** Track each rehearsal run of the 4-beat demo until 3 consecutive clean runs are achieved.

## Run 1 — [date/time]

- Reset elapsed: ___s
- Total runtime: ___:___
- Per-beat duration: B1: ___s | B2: ___s | B3: ___s | B4: ___s
- Glitches: [list]
- Decision: [continue | reset and retry]

## Run 2 — [date/time]
[same format]

## Run 3 — [date/time]
[same format]

## Sign-off

- [ ] 3 consecutive runs completed
- [ ] Zero blocking glitches in runs 2-3
- [ ] Reset script reliable (<15s every time)
- [ ] Ready to film

## Glitch summary across all runs

[List any recurring issues + fixes applied between runs]
```

**Step C — RUN THE 3 REHEARSALS.**

This step is HUMAN-DRIVEN — the user runs the demo end-to-end and logs results. The rehearsal flow:

1. `bash contract-ide/demo/reset-demo.sh`. Time it. Note elapsed.
2. Open the demo repo in the relaunched app.
3. Walk Beats 1-4 of presentation-script.md exactly. Time each beat.
4. Note any glitches or visual issues.
5. After each run, append to rehearsal-log.md.
6. Reset and repeat.

Continue until 3 consecutive runs have ZERO blocking glitches. Common issues to expect:
- UUID substitution missed → Sync animation fires against empty state
- Phase 9 contract gap → ScreenCard shows "no schema declared"
- Substrate not populated → Cmd+P returns no hits

Fix issues between runs, document fixes in rehearsal-log.md, restart counter on each fix.

**Step D — Sign off 13-UAT.md.**

After 3 clean runs, complete the 13-UAT.md checklist. Any unchecked SC = blocker. Document blockers and the fix path before filming.

**Avoid:**
- DO NOT skip the rehearsal because "it worked once" — DEMO-04 is explicit: 3 in a row.
- DO NOT count runs with glitches toward the 3 — counter resets on glitch.
- DO NOT modify presentation-script.md during rehearsal — script is canonical; if the implementation can't match script, the implementation is wrong (file an issue).
  </verify>
  <done>
Type "approved" once 3 consecutive clean rehearsals are logged in rehearsal-log.md AND 13-UAT.md is signed off. Type "blocker:<description>" if any SC fails or rehearsal can't reach 3 clean runs.
  </done>
</task>

</tasks>

<verification>
- All UUID placeholders substituted (zero `<UUID-` strings in fixtures)
- runbook-v2.md mirrors presentation-script.md 4-beat structure
- live-scenario.md is the redirect-style replacement
- rehearsal-log.md exists with 3+ logged runs
- 13-UAT.md exists with all 11 SCs checked off
- DEMO-04 acceptance: 3 consecutive runs documented
</verification>

<success_criteria>
- [ ] Real Phase 9 UUIDs substituted into blast-radius.json + beat3-verifier.json (no `<UUID-` placeholders left)
- [ ] runbook-v2.md rewritten with 4-beat structure + per-beat status + glitch playbook + fallback beat map
- [ ] live-scenario.md replaced with redirect-style content
- [ ] rehearsal-log.md tracks ≥3 consecutive clean runs
- [ ] 13-UAT.md created with all 11 SC checklist items + DEMO-04 sign-off section
- [ ] Human signs off after 3 clean rehearsals OR identifies blockers preventing closure
</success_criteria>

<output>
After completion, create `.planning/phases/13-substrate-ui-demo-polish/13-11-SUMMARY.md` documenting:
- Final reset-demo.sh elapsed time across 3 runs (median)
- Total demo runtime average (target 4:00 ± 5s)
- Glitches encountered and fixes applied
- Any SC that required mitigation per fallback beat map
- Final phase 13 closure status: filming-ready or blocked

This summary is the milestone close — once Phase 13 is sealed, the demo is filmable.
</output>