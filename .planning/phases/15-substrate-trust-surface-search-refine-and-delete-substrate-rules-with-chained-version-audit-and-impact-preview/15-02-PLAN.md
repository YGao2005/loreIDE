---
phase: 15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - contract-ide/src/components/command-palette/IntentPalette.tsx
  - contract-ide/src/components/command-palette/IntentPaletteHit.tsx
  - contract-ide/src/components/command-palette/__tests__/IntentPalette.test.tsx
  - contract-ide/src-tauri/src/commands/substrate.rs
  - contract-ide/src/ipc/substrate.ts
  # SubstrateNodeSummary.applies_when extension (folded in from 15-03 to keep the
  # commands/substrate.rs + ipc/substrate.ts edit window in this plan's wave):
  # see Task 1 subtask "Extend SubstrateNodeSummary with applies_when".
autonomous: true
requirements:
  - TRUST-01  # Cmd+P substrate filter chip + FTS5 + DeepSeek rerank → SourceArchaeologyModal in <2s

must_haves:
  truths:
    - "User opens Cmd+P, sees three filter chips at the top of the dialog: All · Contracts · Code · Substrate (chip-row sits above the search input)"
    - "When the Substrate chip is active, the IPC find_substrate_by_intent receives a kind_filter='substrate' parameter and returns ONLY substrate hits (constraint/decision/open_question/resolved_question/attempt rows from substrate_nodes WHERE invalid_at IS NULL)"
    - "Substrate hits are produced via the existing FTS5 substrate_nodes_fts MATCH path AND passed through the existing DeepSeek listwise reranker in retrieval/rerank.rs without modification (zero new deps; reuse the Phase 11 primitive)"
    - "TRUTH ABOUT THE FTS5 INDEXED COLUMN SET: substrate_nodes_fts indexes only text + applies_when + scope (per migration v6 — verified). It does NOT index source_quote (= verbatim_quote in the TS wire). TRUST-01's 'verbatim_quote' relevance is served by the DeepSeek listwise reranker's semantic scoring step, NOT by the FTS5 candidate generation step. The FTS5 stage is the wide candidate net (text/applies_when/scope keyword match); the reranker is the precision filter that surfaces verbatim-quote-relevant hits. Per RESEARCH §5.4 + Open Question 3 we explicitly do NOT add source_quote to the FTS5 schema (would require DROP+RECREATE the virtual table — risky on a populated DB). This split is load-bearing for TRUST-01 SC1 and is the reason the precision SC is measured END-TO-END (post-rerank) and not on the raw FTS candidate set."
    - "Selecting a substrate hit from the Substrate-filtered palette opens SourceArchaeologyModal directly (calls useCitationStore.openCitationUuid(hit.uuid)), NOT selectNode(parent_uuid) which is the current default for substrate hits in the All view"
    - "Total time from keystroke (Cmd+P with Substrate chip selected, query 'why email confirmation') to the first readable verbatim quote rendering inside SourceArchaeologyModal is <2s on the demo SQLite snapshot — measured by adding a console.time/timeEnd guard around (a) the IPC round-trip and (b) the modal mount + getSubstrateNodeDetail fetch"
    - "All chip / Contracts chip / Code chip preserve the existing behaviour exactly (no regressions to plan 13-03's per-kind navigation contract — flow→L2, L4 atom→L3+chip-halo, contract→pushParent)"
    - "The kind_filter parameter on find_substrate_by_intent is OPTIONAL — calls without it (existing call sites in contract palette + future code) continue to behave as today (mixed-kind results)"
    - "Vitest cases cover: (a) Substrate chip activates kind_filter on IPC call, (b) substrate-hit click routes to openCitationUuid not selectNode, (c) chip state persists across re-opens of the dialog within a single session"
  artifacts:
    - path: "contract-ide/src/components/command-palette/IntentPalette.tsx"
      provides: "Filter chip row (All · Contracts · Code · Substrate) above the cmdk search input; chip selection state is local to IntentPalette; debounced query passes kind_filter to IPC when chip != All"
    - path: "contract-ide/src-tauri/src/commands/substrate.rs"
      provides: "find_substrate_by_intent gains optional kind_filter: Option<String> parameter; when Some('substrate'), the function skips the contracts FTS scan and runs only the substrate_nodes_fts path; when Some('contracts'), it skips the substrate scan; when None or 'all', current behaviour. ALSO: SubstrateNodeSummary struct gains pub applies_when: Option<String>; get_substrate_node_detail SELECT extends to include applies_when (folded from 15-03 — same file edit)."
    - path: "contract-ide/src/ipc/substrate.ts"
      provides: "TS wrapper findSubstrateByIntent(query, limit, kindFilter?) — backward-compatible signature; kindFilter defaults to undefined which maps to 'all' on the Rust side. ALSO: SubstrateNodeSummary interface gains applies_when: string | null."
    - path: "contract-ide/src/components/command-palette/__tests__/IntentPalette.test.tsx"
      provides: "Vitest cases proving chip behaviour, IPC parameter wiring, and substrate-hit routing override"
      min_lines: 60
  key_links:
    - from: "contract-ide/src/components/command-palette/IntentPalette.tsx"
      to: "contract-ide/src/store/citation.ts (useCitationStore.openCitationUuid)"
      via: "When the Substrate chip is active AND user selects a substrate hit, call openCitationUuid(hit.uuid) instead of the default selectNode(hit.parent_uuid) routing"
      pattern: "openCitationUuid"
    - from: "contract-ide/src/components/command-palette/IntentPalette.tsx"
      to: "contract-ide/src-tauri/src/commands/substrate.rs::find_substrate_by_intent"
      via: "Pass kindFilter through the TS wrapper findSubstrateByIntent(query, 10, chipState === 'substrate' ? 'substrate' : undefined)"
      pattern: "findSubstrateByIntent.*substrate"
---

<objective>
Add a `Substrate` filter chip to the existing Cmd+P palette so users can search by rationale (verbatim_quote / summary / applies_when), and route the click to open `SourceArchaeologyModal` directly. Reuse the existing FTS5 + DeepSeek rerank primitive — zero new retrieval code.

Purpose: TRUST-01 closes the demo's "how do I find a rule by why-it-exists?" gap. Without it, users can locate atoms (L4 nodes) by intent today, but cannot reach the underlying substrate rule by typing the rationale question. This plan makes "why email confirmation" a one-keystroke path to the verbatim Feb-2026 incident quote inside SourceArchaeologyModal.

Output: Filter chip row in IntentPalette, kind_filter parameter on `find_substrate_by_intent`, override routing for substrate-only hits to open the archaeology modal, and vitest coverage of all three behaviours.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-RESEARCH.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-01-PLAN.md

# Files to read carefully before editing
@contract-ide/src/components/command-palette/IntentPalette.tsx
@contract-ide/src/components/command-palette/IntentPaletteHit.tsx
@contract-ide/src-tauri/src/commands/substrate.rs
@contract-ide/src/ipc/substrate.ts
@contract-ide/src/store/citation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — find_substrate_by_intent gains optional kind_filter parameter, dispatches FTS scans accordingly</name>
  <files>
    contract-ide/src-tauri/src/commands/substrate.rs
    contract-ide/src/ipc/substrate.ts
  </files>
  <action>
Open `commands/substrate.rs` and locate `find_substrate_by_intent` (line ~455 per RESEARCH §5.4). Its current signature aggregates contract FTS5 (`nodes_fts`) AND substrate FTS5 (`substrate_nodes_fts`).

Add a new optional parameter `kind_filter: Option<String>` (after the existing parameters; serde default to None). Behaviour:
- `kind_filter == None` OR `Some("all")` → current behaviour (both FTS scans + reranker over the merged candidate set)
- `kind_filter == Some("substrate")` → skip the contract FTS scan entirely; run only the substrate_nodes_fts MATCH path (lines ~600-650 in the existing file). Pass the substrate-only candidates into `llm_rerank` unchanged.
- `kind_filter == Some("contracts")` → skip the substrate FTS scan; run only the contract FTS scan + rerank. (Wire this branch even though TRUST-01 only mandates Substrate — symmetric API is cheaper than a one-direction toggle.)
- `kind_filter == Some("code")` → for now, route to the same path as Some("contracts") and add a `// TODO: Phase 16 code-only filter` comment. (TRUST-01 mentions Code chip but only Substrate is the load-bearing TRUST-01 SC; the Code chip can no-op gracefully.)

CRITICAL: All `WHERE invalid_at IS NULL` predicates on substrate_nodes (lines ~602, ~644 — read the file to find them) MUST be preserved. The chained-version model from plan 15-01 means tombstoned/refined-and-superseded rows must NOT appear in palette results.

Update the function's docstring to mention the new parameter and the `<2s` SC. Bind the new column substrate_rules_json from receipts is NOT relevant here (different concern — that's plan 15-04).

Open `src/ipc/substrate.ts` and extend the TS wrapper:

```ts
export async function findSubstrateByIntent(
  query: string,
  limit = 10,
  kindFilter?: 'substrate' | 'contracts' | 'code' | 'all',
): Promise<SubstrateIntentHit[]> {
  return invoke('find_substrate_by_intent', { query, limit, kindFilter });
}
```

The `kindFilter` arg is optional and defaults to undefined → Rust receives None → existing behaviour. Existing call sites in IntentPalette continue to compile unchanged.

Run `cargo check -p contract-ide` and `pnpm tsc --noEmit` (from contract-ide directory) to confirm types match.

DO NOT modify `retrieval/rerank.rs` — the reranker accepts a candidate slice and is filter-agnostic per RESEARCH §Standard Stack.

**SUBTASK — Extend SubstrateNodeSummary with applies_when (folded in from 15-03 because both touch commands/substrate.rs + ipc/substrate.ts):**

The existing `SubstrateNodeSummary` struct in `commands/substrate.rs` (line ~84) does NOT include `applies_when` as a field, and `get_substrate_node_detail` (line ~270) does NOT include it in the SQL SELECT. Plan 15-03's `RefineRuleEditor` needs `initialAppliesWhen` pre-populated from this detail call. Add the field NOW so plan 15-03 can consume it without re-touching this file in a different wave:

1. Add `pub applies_when: Option<String>,` to the struct (alphabetical placement next to `confidence` is fine; serde rename_all="snake_case" already handles wire format).
2. Update both SQL SELECTs in `get_substrate_node_detail` (the `has_intent_col == true` and `false` branches) to include `applies_when` in the column list — example: `SELECT uuid, node_type, text, applies_when, source_session_id, ...`.
3. Read the column in the row-extraction block: `let applies_when: Option<String> = r.try_get("applies_when").ok();`.
4. Populate the new field in the returned `SubstrateNodeSummary { ... applies_when, ... }` literal.
5. Audit `get_substrate_states_for_canvas` (the OTHER constructor of `SubstrateNodeSummary` at line ~239): set `applies_when: None` (or extend its SELECT too if cheap) — the canvas does not need this field but the struct literal must compile.
6. Update the TS interface in `src/ipc/substrate.ts` (line ~35) to add `applies_when: string | null;` to the `SubstrateNodeSummary` interface.

**Verify subtask:** `cargo check -p contract-ide` exits 0; `pnpm tsc --noEmit` exits 0 (proves the TS interface change is consumed cleanly); a manual `getSubstrateNodeDetail('con-settings-no-modal-interrupts-2025-Q4')` from the IDE devtools console returns an object whose `.applies_when` is the seeded string (not undefined).
  </action>
  <verify>
`cargo check -p contract-ide` exits 0. `pnpm tsc --noEmit` exits 0. Add a `cargo test` exercising `find_substrate_by_intent("email confirmation", 5, Some("substrate"))` against an in-memory pool seeded with one substrate row and one contract row — assert ONLY the substrate row is returned.

**FTS5 candidate-set precision sanity check (load-bearing for TRUST-01 SC1 — see must_haves truth re: FTS5 indexed columns being text+applies_when+scope only, NOT verbatim_quote):**

After the seed loads (run `bash contract-ide/demo/reset-demo.sh` first), open the on-disk SQLite DB (`~/Library/Application Support/com.contract-ide.app/contracts.db`) with the sqlite3 CLI and run the substrate FTS MATCH path directly for 6 demo-seed rationale queries:

```bash
for q in "email confirmation" "soft delete" "anonymize invoices" "stripe customer" "mailchimp suppress" "no modal interrupts"; do
  echo "=== $q ==="
  sqlite3 ~/Library/Application\ Support/com.contract-ide.app/contracts.db \
    "SELECT s.uuid, substr(s.text, 1, 70) FROM substrate_nodes s
       JOIN substrate_nodes_fts f ON f.rowid = s.rowid
       WHERE substrate_nodes_fts MATCH '$q' AND s.invalid_at IS NULL LIMIT 5;"
done
```

Confirm ≥ 5/6 queries surface the expected rule (per scenario-criteria.md § 6 mapping: "email confirmation"→dec-confirm-via-email-link; "soft delete"→dec-soft-delete-30day-grace; "anonymize invoices"→con-anonymize-not-delete-tax-held; "stripe customer"→con-stripe-customer-archive; "mailchimp suppress"→con-mailing-list-suppress-not-delete; "no modal interrupts"→con-settings-no-modal-interrupts) WITHIN THE FTS CANDIDATE SET (top 5 unranked). This is the gate proving FTS5 over text+applies_when+scope is sufficient candidate generation BEFORE the reranker. The reranker (downstream, plan 15-02 IPC end-to-end test in Task 2 verify) does the final precision lift to ≥ 80% top-1 in plan 15-06 Round 2.

If any query returns 0 hits or surfaces an unexpected rule, the gap is in the seed (text/applies_when wording), NOT in this plan — escalate to the seed maintainer. Do NOT add source_quote to the FTS schema (RESEARCH Open Question 3 lock).
  </verify>
  <done>
find_substrate_by_intent accepts kind_filter; substrate-only mode returns only substrate hits and continues to filter `WHERE invalid_at IS NULL` (so chained-version tombstones do not surface); existing call sites without the parameter behave identically; TS wrapper compiles. SubstrateNodeSummary.applies_when is wired through Rust struct + SQL SELECT + TS interface (folded subtask). FTS5 candidate-set sanity check passes ≥ 5/6 on the 6 demo-seed rationale queries.
  </done>
</task>

<task type="auto">
  <name>Task 2: IntentPalette filter chip row + Substrate-hit routing override + console.time profiling guard</name>
  <files>
    contract-ide/src/components/command-palette/IntentPalette.tsx
    contract-ide/src/components/command-palette/IntentPaletteHit.tsx
  </files>
  <action>
Open `IntentPalette.tsx`. Above the cmdk Command.Input, add a chip row using either shadcn Toggle group or a simple flex of `<button>`s styled as pill chips. Four chips: `All` (default), `Contracts`, `Code`, `Substrate`. Use `useState<'all'|'contracts'|'code'|'substrate'>('all')` for selection. Active chip gets a darker background; others muted.

When a chip is clicked, set chipState AND retrigger the debounced query effect so results refresh immediately (do NOT wait for the next keystroke). Pass the chipState value into `findSubstrateByIntent(query, 10, chipState === 'all' ? undefined : chipState)`.

For the navigation contract per RESEARCH §5.4 + STATE.md plan 13-03 notes: the existing per-kind handler in IntentPalette routes substrate hits to `selectNode(parent_uuid)` (which opens the parent contract atom inspector). When the Substrate chip is active, OVERRIDE this so substrate hits route to `useCitationStore.getState().openCitationUuid(hit.uuid)` instead. Reasoning: TRUST-01 SC says "Selecting a hit opens the existing SourceArchaeologyModal in <2s" — the modal is already wired to fire on `useCitationStore.openCitationUuid` per Phase 13 plan 07.

When the All chip is active, preserve the existing behaviour (substrate→selectNode(parent_uuid)) for backward compatibility — TRUST-01 only mandates the override under Substrate filter mode.

Keep `shouldFilter={false}` on the Command.Dialog (IPC is the ranker per plan 13-03 — checker N7).

Wrap the IPC call + the modal mount in `console.time('substrate-cmdp-roundtrip')` / `console.timeEnd(...)` markers (gated on `import.meta.env.DEV`) so the <2s SC is measurable during plan 15-06 UAT. Place the `console.timeEnd` at the moment `getSubstrateNodeDetail` resolves inside SourceArchaeologyModal — this requires a small wiring trick: store the start timestamp on `useCitationStore` (or in a window global gated on DEV) and have the modal log the elapsed time on first paint of the verbatim quote. Acceptable alternative: only time the IPC round-trip + the openCitationUuid call (the modal mount + detail fetch is constant — RESEARCH says <300ms on localhost SQLite).

Open `IntentPaletteHit.tsx` and add a small `Substrate` badge (or use the existing kind badge) for substrate hits so users can visually distinguish them in the All view. Skip if a kind label is already present.

DO NOT touch `useGraphStore.selectNode` — the override is purely client-side at the click handler.

Persist chipState within the open Command.Dialog session only — when the dialog closes and reopens, reset to 'all'. (No need for a Zustand store; `useState` is fine.)
  </action>
  <verify>
Manual: Cmd+P opens the palette → chip row visible above the input. Click `Substrate` → background of the chip darkens. Type "email confirmation" → results show only substrate hits. Click a hit → SourceArchaeologyModal opens (NOT the inspector). Click `All` → mixed results return. Time from keystroke to verbatim quote rendered <2s (measure via DevTools Performance or the console.time markers).
  </verify>
  <done>
Filter chip row visible; Substrate chip filters to substrate-only results; substrate-hit click under Substrate chip opens SourceArchaeologyModal; All chip preserves existing behaviour; <2s end-to-end measured at least once during dev.
  </done>
</task>

<task type="auto">
  <name>Task 3: Vitest cases — chip wiring + IPC kind_filter passthrough + substrate-hit routing override</name>
  <files>
    contract-ide/src/components/command-palette/__tests__/IntentPalette.test.tsx
  </files>
  <action>
Add a vitest spec (or extend the existing one if present — check `__tests__/` directory) covering:

1. **Chip click triggers IPC retry with kind_filter:** Mock `findSubstrateByIntent` (vi.mock the IPC wrapper). Render IntentPalette open=true. Type a query. Wait for debounce. Click the `Substrate` chip. Assert `findSubstrateByIntent` is called twice — first without kind_filter, then with `'substrate'` — and the second call's results are what renders.

2. **Substrate-hit click under Substrate chip routes to openCitationUuid, not selectNode:** Mock both `useCitationStore` and `useGraphStore`. Click `Substrate` chip → simulate a hit click → assert `useCitationStore.openCitationUuid` was called with the hit UUID; assert `useGraphStore.selectNode` was NOT called.

3. **Substrate-hit click under All chip preserves existing routing:** Click a substrate hit while `All` chip is active → assert `useGraphStore.selectNode` (or whatever the existing routing function is — read the current code to match) was called; assert `openCitationUuid` was NOT called.

4. **Chip state resets between dialog opens:** Open dialog, click `Substrate`, close dialog, reopen — assert `All` chip is active again.

Use `@testing-library/react` + `userEvent` per existing project conventions. Run `pnpm test -- IntentPalette` and confirm all three cases pass.

DO NOT add new mocks for the reranker or the FTS path — those are tested separately in the Rust suite (Task 1 verify).
  </action>
  <verify>
`pnpm test -- IntentPalette` exits 0; all 4 new cases pass alongside any existing IntentPalette tests; full vitest suite still green (`pnpm test`).
  </verify>
  <done>
Vitest coverage proves chip wiring + IPC parameter + routing override + chip state reset; full test suite green.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p contract-ide` and `cargo test -p contract-ide` exit 0
- `pnpm tsc --noEmit` exits 0 from contract-ide directory
- `pnpm test` exits 0 (full vitest suite green; IntentPalette suite includes 4 new cases)
- Manual smoke: Cmd+P → Substrate chip → query "email confirmation" → SourceArchaeologyModal opens within 2s with the verbatim Feb-2026 quote visible
- All chip behaviour is unchanged from plan 13-03 (substrate hits route to selectNode under All)
</verification>

<success_criteria>
- Filter chip row visible above the cmdk input with All / Contracts / Code / Substrate
- Substrate chip filters IPC results to substrate-only and routes hit clicks to SourceArchaeologyModal
- IPC kind_filter is backward-compatible (existing call sites without it work unchanged)
- <2s end-to-end SC met on the demo SQLite snapshot (measured via console.time markers in dev)
- Phase 12 supersession queries continue to read chain heads correctly because the WHERE invalid_at IS NULL predicate stays in the substrate FTS path
</success_criteria>

<output>
After completion, create `15-02-SUMMARY.md` documenting:
- Final IntentPalette chip row markup (snippet)
- find_substrate_by_intent signature change (Rust + TS)
- The substrate-hit routing override behaviour and where it lives (line in IntentPalette)
- console.time marker locations for the <2s UAT
</output>
