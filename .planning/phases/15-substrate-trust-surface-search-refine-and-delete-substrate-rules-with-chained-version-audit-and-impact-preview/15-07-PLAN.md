---
phase: 15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview
plan: 07
type: execute
wave: 6
supersedes: 15-06
depends_on:
  - 15-01
  - 15-02
  - 15-03
  - 15-04
  - 15-05
files_modified:
  - .planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-UAT.md
autonomous: false

requirements:
  - TRUST-01  # Cmd+P substrate search precision SC verified live on demo seed
  - TRUST-02  # Refine path verified end-to-end via Cmd+P → modal → Refine → History
  - TRUST-03  # Delete + impact preview + Substrate Health + Restore verified via modal-driven paths
  - TRUST-04  # Audit trail visible in History tab + substrate_edits sqlite3 inspection

must_haves:
  truths:
    - "Six of the seven Phase 15 ROADMAP success criteria can be verified via UI-independent paths today (Cmd+P palette + SourceArchaeologyModal-driven Refine/Delete + SubstrateHealthDialog-driven Restore + sqlite3 inspection of substrate_edits + receipts). Only SC7 (Beat 3 fixture-to-real conversion) defers to the sync-review surface build because the floating-panel VerifierPanel that 15-06 was wiring no longer exists."
    - "Cmd+P substrate-search precision harness runs against the seeded demo SQLite snapshot — 10 ambient queries scored manually for top-1 hit, ≥ 0.80 required for TRUST-01 SC1 PASS."
    - "Refine path verified by typing a substrate-rationale query in Cmd+P → Substrate chip → click hit → SourceArchaeologyModal mounts → Refine button → editor → Save → modal switches to History tab → 2-version chain visible. No floating panel needed."
    - "Delete path verified by Cmd+P → Substrate → click hit → Delete this rule button → confirm dialog with 5-radio reason picker + impact preview (atom_count + recent_prompt_count) → Confirm → modal closes + DOM toast. Subsequent Cmd+P with Substrate filter does NOT return the deleted rule (FTS5 trigger from 15-01 fired correctly)."
    - "Restore path verified by SubstrateStatusIndicator footer 🪦 N tombstoned badge → click → SubstrateHealthDialog → row shows kind / parsed reason / actor / time → Restore button → row disappears + toast → Cmd+P with Substrate filter returns the restored rule again."
    - "Audit trail (TRUST-04 SC3) verified by sqlite3 inspection: SELECT kind, before_text, after_text, reason, actor FROM substrate_edits WHERE rule_uuid='<uuid>' ORDER BY edited_at — kind sequence reflects the actual refine/delete/restore actions taken in the UAT walkthrough."
    - "Phase 12 supersession compatibility (SC6) verified explicitly via the refine-stripe-archive + Delegate-to-agent + inspect-receipts-substrate-rules-json flow from 15-06's regression check, with the head UUID appearing in receipts.substrate_rules_json and the tombstoned origin UUID NOT appearing. This is the load-bearing forensic check that proves 15-01's composer→delegate→agent→receipts threading + WHERE invalid_at IS NULL chain-head selection both work post-refine."
    - "Reset-demo.sh continues to apply cleanly under v8 schema in <15s (SC perf gate). Verified once, not 5x — the 5x rehearsal stability gate is part of SC7 which is deferred to the sync-review build."
    - "SC7 (Beat 3 fixture-to-real conversion) is explicitly DEFERRED in 15-UAT.md with a precise specification of what the sync-review surface task must implement: useVerifierStore.acceptFlag(ruleUuid) action + flag-row click handler that registers setOnRefineSuccess(uuid => acceptFlag(uuid)) THEN calls openCitation(row.ruleUuid). The receiving end of the callback (SourceArchaeologyModal invoking onRefineSuccess(originalUuid) BEFORE re-pointing to the new chain head) is already wired in plan 15-03; closeCitation auto-clears the callback. Whoever builds the sync-review flag section inherits a 10-line wiring task with the receiving-end handshake already in place."
  artifacts:
    - path: ".planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-UAT.md"
      provides: "Executable UAT harness with 6 PASS-able SCs (1, 2, 3, 4, 5, 6) + SC7 deferred-with-spec section. Each verifiable SC has: command/click sequence, expected observation, sqlite3 evidence query, marker placeholder for the user to fill in (PASS/FAIL/blocked) after running."
      min_lines: 200
  key_links:
    - from: "Plan 15-07 SC7 deferred-spec section"
      to: "Whoever builds sync-review flag-row click handler"
      via: "10-line wiring stub: setOnRefineSuccess(uuid => useVerifierStore.getState().acceptFlag(uuid)); openCitation(row.ruleUuid). Plan 15-03 wired the receiving end (SourceArchaeologyModal calls onRefineSuccess synchronously after Save, BEFORE re-pointing); closeCitation auto-clears."
      pattern: "setOnRefineSuccess.*acceptFlag.*openCitation"
---

<objective>
Replace stale 15-06 with a partial-UAT closure that captures everything that is verifiable today (6 of 7 Phase 15 SCs) via the existing Cmd+P + SourceArchaeologyModal + SubstrateHealthDialog paths, and codify the remaining SC7 (Beat 3 fixture-to-real) as a small deferred wiring spec that the sync-review build will execute.

Purpose: 15-06 assumed the floating-panel VerifierPanel architecture that has since been replaced by the sync-review page surface (see `.planning/HANDOFF-sync-review-surface.md`). Plans 15-01 through 15-05 are NOT stale — they built a fully working substrate trust surface that lives inside `SourceArchaeologyModal` + `Cmd+P` + `SubstrateStatusIndicator` and is testable end-to-end without any reference to the old VerifierPanel. This plan harvests that closure today rather than waiting on the sync-review surface, while leaving SC7 as a precise hand-off note for whoever builds the new flag-row component.

Output: `15-UAT.md` covering 6 verifiable SCs + 1 deferred SC + Phase 12 regression evidence + reset perf evidence. Phase 15 closes at 6/7 SCs PASS pending sync-review surface to land the seventh.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/HANDOFF-sync-review-surface.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-01-SUMMARY.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-02-SUMMARY.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-03-SUMMARY.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-04-SUMMARY.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run partial UAT against the live IDE — fill in 15-UAT.md markers for SC1–6 + Phase 12 regression + reset perf</name>
  <files>
    .planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-UAT.md
  </files>
  <action>
The 15-UAT.md scaffold already contains the executable harness for each SC. Open the IDE, walk each SC's section, run the listed commands and click sequences, and fill in the PASS/FAIL/blocked marker + evidence excerpt + reproducibility note for each.

UAT execution flow (in order):

  1. **SC0 — Reset perf gate.** Run `time bash contract-ide/demo/reset-demo.sh`. Confirm <15s. Record wall time. Confirm migration v8 has applied (sqlite3 PRAGMA user_version returns 8).

  2. **SC1 — Cmd+P substrate-search precision.** Run all 10 queries from the harness; score top-1 hits manually. ≥ 0.80 = PASS.

  3. **SC2 — Refine via modal.** Cmd+P → Substrate chip → click any seeded rule → SourceArchaeologyModal opens → Refine → editor expands → type narrowed text + reason → Save → modal switches to History tab → 2-version chain visible. Verify chain order (oldest → newest), Current badge on head, side-by-side before/after pre-blocks.

  4. **SC3 — Audit trail.** sqlite3 inspect substrate_edits for the rule UUID after SC2. kind='refine' row exists with correct before_text/after_text/actor/reason.

  5. **SC4 — Delete via modal.** Cmd+P → Substrate → click another rule → Delete this rule button → confirm dialog → reason picker (5 options) + impact preview (atom_count, recent_prompt_count) → pick Hallucinated → Confirm → modal closes + toast. Cmd+P → Substrate → search the deleted rule's text → confirm NOT present (FTS5 trigger fired).

  6. **SC5 — Restore via SubstrateHealthDialog.** SubstrateStatusIndicator footer shows 🪦 N tombstoned badge. Click → dialog → row visible with kind/parsed-reason/actor/time. Restore → row disappears + toast. Cmd+P → Substrate → search the restored rule's text → present again.

  7. **SC6 — Phase 12 regression check.** This is the load-bearing forensic step. Cmd+P → Substrate → "stripe archive" → click hit (con-stripe-customer-archive-2026-02-22) → Refine → narrow to "for users with active subscriptions only" + reason "Phase 15 SC6 regression check". Save. Capture HEAD_UUID via:

         sqlite3 ~/Library/Application\ Support/com.contract-ide.app/contracts.db \
           "SELECT uuid FROM substrate_nodes
            WHERE prev_version_uuid='con-stripe-customer-archive-2026-02-22'
              AND invalid_at IS NULL;"

     Trigger a Delegate-to-agent run with prompt "Implement workspace deletion that archives the Stripe customer instead of deleting it." Wait for receipt.

         sqlite3 ~/Library/Application\ Support/com.contract-ide.app/contracts.db \
           "SELECT id, substrate_rules_json FROM receipts
            ORDER BY created_at DESC LIMIT 1;"

     PASS criteria: substrate_rules_json contains HEAD_UUID and does NOT contain con-stripe-customer-archive-2026-02-22.

  8. **SC7 — DEFERRED.** Mark as Deferred-with-spec; do not attempt to verify against the (deleted) VerifierPanel. The 15-UAT.md scaffold contains the precise wiring stub spec for the sync-review build to inherit.

After running, update 15-UAT.md frontmatter `status: pending` → `status: partial-pass` (6/7) or `status: failed` (with which SCs failed). Keep `sc7_status: deferred-pending-sync-review-surface`.
  </action>
  <verify>
- 6 of 7 SCs marked PASS in 15-UAT.md (SC0/perf + SC1/precision + SC2/refine + SC3/audit + SC4/delete + SC5/restore + SC6/Phase 12 regression)
- SC7 marked deferred with the wiring spec preserved
- Frontmatter status updated to partial-pass or failed
- No regression in `cargo test` or `pnpm test`
  </verify>
  <done>
Type "approved" with the 7-SC scorecard (PASS/FAIL/deferred per SC) + Cmd+P precision score + Phase 12 regression check result. Or describe specific issues to address before approval.
  </done>
</task>

</tasks>

<verification>
- 6 of 7 Phase 15 ROADMAP SCs marked PASS in 15-UAT.md (only SC7 deferred)
- SC1 Cmd+P substrate-search precision ≥ 0.80 on the 10-query harness
- SC6 Phase 12 regression check: head UUID appears in receipts.substrate_rules_json post-refine; tombstoned origin UUID does not
- substrate_edits table has the expected kind sequence (refine + delete + restore) after the UAT walkthrough
- Reset-demo.sh completes in <15s (timed)
- 15-UAT.md frontmatter `status: partial-pass` (6/7) and `sc7_status: deferred-pending-sync-review-surface`
- No regression in `cargo test` or `pnpm test`
</verification>

<success_criteria>
- Phase 15 closes at 6/7 SCs PASS, with SC7 carved out as a small wiring task for whoever builds the sync-review surface
- 15-UAT.md captures the SC7 wiring spec (10-line stub) so the receiving-end handshake (already in 15-03) is consumed correctly
- STATE.md + ROADMAP.md reflect Phase 15 partial-pass status with the SC7 deferral noted
</success_criteria>

<output>
After UAT completes, create `15-07-SUMMARY.md` documenting:
- The 7-SC scorecard from 15-UAT.md (verbatim)
- Cmd+P precision score
- Phase 12 regression check evidence (HEAD_UUID + receipts.substrate_rules_json content)
- substrate_edits kind sequence captured
- Reset-demo.sh wall time
- Reference to the SC7 wiring stub for the sync-review build
- Commit hashes for plan 15-07
</output>
