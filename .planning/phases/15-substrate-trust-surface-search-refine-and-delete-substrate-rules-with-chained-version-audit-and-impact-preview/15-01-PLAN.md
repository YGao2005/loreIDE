---
phase: 15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contract-ide/src-tauri/src/db/migrations.rs
  - contract-ide/src-tauri/src/delegate/composer.rs
  - contract-ide/src-tauri/src/db/scanner.rs
  - contract-ide/demo/seeds/substrate.sqlite.seed.sql
  - contract-ide/src-tauri/tests/migration_v8_chain_smoke.rs
autonomous: true
requirements:
  - TRUST-02  # chained-version schema (prev_version_uuid) — substrate_nodes ALTER + Phase 12 supersession query parity audit
  - TRUST-04  # substrate_edits audit table written atomically — table created in this migration

must_haves:
  truths:
    - "Migration v8 lands and bumps PRAGMA user_version to 8 on next app launch"
    - "substrate_nodes table has new nullable columns: prev_version_uuid (TEXT REFERENCES substrate_nodes(uuid)) and invalidated_reason (TEXT)"
    - "substrate_edits audit table exists with the exact column set: edit_id PK, rule_uuid, prev_version_uuid, new_version_uuid, actor, edited_at, before_text, after_text, reason, kind ∈ {refine, delete, restore}"
    - "current_substrate_view SQL view exists and equals SELECT * FROM substrate_nodes WHERE invalid_at IS NULL"
    - "FTS5 UPDATE trigger substrate_nodes_au is replaced so that tombstoned rows (invalid_at NOT NULL after update) are removed from substrate_nodes_fts and not re-indexed"
    - "receipts table has a new substrate_rules_json TEXT column (nullable) and delegate/composer.rs persists the substrate hit UUIDs alongside every receipt write so TRUST-03 impact preview can count real receipts"
    - "Phase 12 supersession queries (fact_engine, intent_engine, walker, retrieval candidates, commands/substrate.rs FTS+invalid_at) compile and pass cargo test under v8 — WHERE invalid_at IS NULL semantics correctly select the chain head"
    - "demo/seeds/substrate.sqlite.seed.sql still applies cleanly under v8 (its DELETE FROM substrate_nodes still works; its INSERTs do not need to mention the two new nullable columns)"
    - "A rust integration smoke test exercises the chain primitive end-to-end: insert seed row → INSERT new row with prev_version_uuid → UPDATE old row invalid_at → assert substrate_nodes_fts no longer matches old row's text and current_substrate_view returns only the new row"
  artifacts:
    - path: "contract-ide/src-tauri/src/db/migrations.rs"
      provides: "Migration v8 SQL block: ALTER TABLE substrate_nodes ADD COLUMN prev_version_uuid + ALTER TABLE substrate_nodes ADD COLUMN invalidated_reason + CREATE TABLE substrate_edits + CREATE VIEW current_substrate_view + DROP/CREATE TRIGGER substrate_nodes_au + ALTER TABLE receipts ADD COLUMN substrate_rules_json"
      contains: "version: 8"
    - path: "contract-ide/src-tauri/src/delegate/composer.rs"
      provides: "Composer writes the substrate hit UUIDs JSON onto receipts.substrate_rules_json on every Delegate-to-agent run so the TRUST-03 impact preview returns real recent-prompts counts"
    - path: "contract-ide/src-tauri/tests/migration_v8_chain_smoke.rs"
      provides: "Integration smoke proving (a) chain INSERT + invalidate works in single transaction, (b) FTS tombstone leakage is fixed, (c) current_substrate_view returns only WHERE invalid_at IS NULL"
      min_lines: 80
    - path: "contract-ide/demo/seeds/substrate.sqlite.seed.sql"
      provides: "Seed continues to load cleanly under v8 (no schema drift); optionally seeds NULL into the two new nullable columns explicitly to keep the file self-documenting"
  key_links:
    - from: "contract-ide/src-tauri/src/db/migrations.rs"
      to: "FTS5 substrate_nodes_fts"
      via: "DROP TRIGGER substrate_nodes_au; CREATE TRIGGER substrate_nodes_au AFTER UPDATE ON substrate_nodes BEGIN INSERT INTO substrate_nodes_fts(...) VALUES('delete', ...); INSERT INTO substrate_nodes_fts(...) SELECT ... WHERE new.invalid_at IS NULL; END"
      pattern: "WHERE new.invalid_at IS NULL"
    - from: "contract-ide/src-tauri/src/delegate/composer.rs"
      to: "receipts.substrate_rules_json"
      via: "JSON-encode the substrate hit UUIDs and bind onto the receipts INSERT alongside existing fields"
      pattern: "substrate_rules_json"
---

<objective>
Land the v8 SQLite migration that makes Phase 15's chained-version model possible, and extend `delegate/composer.rs` so the TRUST-03 impact preview can count real recent receipts. This is the foundation every other Phase 15 plan reads from.

Purpose: Phase 15's entire build sits on top of (a) `substrate_nodes.prev_version_uuid` self-FK, (b) `substrate_edits` audit table, (c) FTS5 tombstone removal, and (d) receipt-side substrate-hit persistence. Without these, refine/delete/restore IPCs can't be written, and the impact preview can only show a misleading proxy.

Output: Migration v8 SQL block in `migrations.rs`, composer.rs extension that persists substrate hit UUIDs onto receipts, an integration smoke test proving chain semantics + FTS tombstone removal hold, and a seed file confirmed clean under v8.
</objective>

<execution_context>
@/Users/yang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-RESEARCH.md

# Files to read carefully before editing
@contract-ide/src-tauri/src/db/migrations.rs
@contract-ide/src-tauri/src/delegate/composer.rs
@contract-ide/src-tauri/src/supersession/queries.rs
@contract-ide/demo/seeds/substrate.sqlite.seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration v8 — chained-version primitives + audit table + FTS tombstone fix + current view + receipts.substrate_rules_json</name>
  <files>
    contract-ide/src-tauri/src/db/migrations.rs
    contract-ide/demo/seeds/substrate.sqlite.seed.sql
  </files>
  <action>
Append a new `Migration { version: 8, description: "phase15_substrate_trust_surface", kind: MigrationKind::Up, sql: r#" ... "# }` entry to the `migrations()` Vec in `migrations.rs` AFTER the existing v7 entry. The SQL block must contain, in order:

1. `ALTER TABLE substrate_nodes ADD COLUMN prev_version_uuid TEXT REFERENCES substrate_nodes(uuid);`
2. `ALTER TABLE substrate_nodes ADD COLUMN invalidated_reason TEXT;`
3. `CREATE TABLE IF NOT EXISTS substrate_edits ( edit_id TEXT PRIMARY KEY, rule_uuid TEXT NOT NULL, prev_version_uuid TEXT, new_version_uuid TEXT, actor TEXT NOT NULL, edited_at TEXT NOT NULL, before_text TEXT, after_text TEXT, reason TEXT NOT NULL, kind TEXT NOT NULL CHECK(kind IN ('refine', 'delete', 'restore')) );`
4. `CREATE INDEX IF NOT EXISTS idx_substrate_edits_rule_uuid ON substrate_edits(rule_uuid);`
5. `CREATE INDEX IF NOT EXISTS idx_substrate_edits_edited_at ON substrate_edits(edited_at);`
6. `CREATE VIEW IF NOT EXISTS current_substrate_view AS SELECT * FROM substrate_nodes WHERE invalid_at IS NULL;`
7. `DROP TRIGGER IF EXISTS substrate_nodes_au;`
8. The replacement `CREATE TRIGGER substrate_nodes_au AFTER UPDATE ON substrate_nodes BEGIN INSERT INTO substrate_nodes_fts(substrate_nodes_fts, rowid, uuid, text, applies_when, scope) VALUES ('delete', old.rowid, old.uuid, old.text, old.applies_when, old.scope); INSERT INTO substrate_nodes_fts(rowid, uuid, text, applies_when, scope) SELECT new.rowid, new.uuid, new.text, new.applies_when, new.scope WHERE new.invalid_at IS NULL; END;`
9. `ALTER TABLE receipts ADD COLUMN substrate_rules_json TEXT;`

Use `IF NOT EXISTS` everywhere SQLite supports it. Do NOT touch v6 or v7 — they are immutable per Phase 1 Plan 02 lock (Pitfall 5).

After writing v8, open `demo/seeds/substrate.sqlite.seed.sql` and confirm its INSERT statements continue to compile under the new schema (the two new substrate_nodes columns are nullable so existing INSERTs are still valid). Do NOT add the new columns to the seed — keep the seed minimal; defaults handle them.

WHY this exact ordering: ALTER TABLE must precede CREATE VIEW (the view references the new columns indirectly via SELECT *); DROP TRIGGER must precede the replacement CREATE TRIGGER; the trigger replacement is the load-bearing fix for Pitfall 1 (FTS tombstone leakage) per RESEARCH §Pattern 4. The `INSERT INTO ... SELECT ... WHERE` syntax is the standard SQLite content-table pattern — verified in v6 trigger pattern.

DO NOT use `ALTER TABLE substrate_nodes_fts` to add columns — FTS5 does not support add-column ALTER. The reranker handles verbatim-quote relevance per RESEARCH Open Question 3.

Run `cargo check -p contract-ide` to confirm Rust compiles.
  </action>
  <verify>
`cargo check -p contract-ide` exits 0. Then launch the app once (or run a unit test that loads the pool) so the migration applies; run `sqlite3 ~/Library/Application\ Support/com.contract-ide.app/contracts.db "PRAGMA user_version;"` and confirm output is `8`. Run `sqlite3 ... ".schema substrate_edits"` and confirm the table exists with all expected columns. Run `sqlite3 ... ".schema current_substrate_view"` and confirm the view exists.
  </verify>
  <done>
Migration v8 lands; PRAGMA user_version = 8; substrate_edits table + current_substrate_view exist; replacement substrate_nodes_au trigger present (verifiable via `sqlite3 ... ".schema substrate_nodes_au"`); receipts table has substrate_rules_json column (`PRAGMA table_info(receipts);`); seed SQL continues to apply without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: composer.rs — persist substrate hit UUIDs onto receipts.substrate_rules_json on every Delegate-to-agent write</name>
  <files>
    contract-ide/src-tauri/src/delegate/composer.rs
  </files>
  <action>
Read `delegate/composer.rs` to find the location where the receipt is INSERTed after a Delegate-to-agent run. The composer already has the `Vec<SubstrateHit>` (or equivalent) that was passed into the prompt — locate it (likely a struct field on the composer state or a function parameter), and serialize the hit UUIDs into a JSON array `["uuid1","uuid2",...]` via `serde_json::to_string(&uuids).unwrap_or_else(|_| "[]".to_string())`.

Then extend the existing receipt INSERT to bind that JSON onto the new `substrate_rules_json` column. If the INSERT is built via sqlx::query with positional bindings, append the new bind in the matching position. If the INSERT lists columns explicitly, add `substrate_rules_json` to the column list and the value to the VALUES list (DO NOT use a positional swap — preserve existing column order).

If the composer dispatches multiple receipts (e.g., one per node touched), populate substrate_rules_json identically on each — every receipt from the same agent run sees the same substrate hits.

If the composer code path is conditional (some receipts may be written without substrate hits — e.g., from `commands/agent.rs` path), only populate substrate_rules_json when hits are present; leave it NULL otherwise. The TRUST-03 impact preview query MUST tolerate NULL (treat NULL as "no hits recorded for this receipt" — counts as zero matches).

Add a unit test in `delegate/composer.rs` (or a new test file under `src-tauri/tests/`) that constructs a fake composer state with 3 substrate hit UUIDs, calls the receipt-write code path, then queries `SELECT substrate_rules_json FROM receipts WHERE id = ?` and asserts the JSON parses to the expected 3 UUIDs.

WHY this exact behaviour: TRUST-03 SC4 requires "recent agent prompts in past 7 days that included the rule (count)" with a real count, not a misleading proxy. RESEARCH Open Question 1 nails Option B (add column + persist) as the correct choice. Without this column populated at write time, the impact preview's recent-prompts count is structurally impossible.
  </action>
  <verify>
`cargo test -p contract-ide composer` passes; the new unit test passes. Then trigger a real Delegate-to-agent run from the IDE (or call the test fixture), open the SQLite DB, and run `SELECT substrate_rules_json FROM receipts ORDER BY created_at DESC LIMIT 1;` — confirm the JSON is non-NULL and parses to a list of UUIDs that match the substrate hits in the prompt.
  </verify>
  <done>
Every Delegate-to-agent receipt write persists substrate_rules_json; the column is queryable for impact preview counts in plan 15-04; existing receipts without hits stay NULL and don't crash the count query.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration smoke test — chain insert + FTS tombstone fix + current_substrate_view + Phase 12 query parity</name>
  <files>
    contract-ide/src-tauri/tests/migration_v8_chain_smoke.rs
  </files>
  <action>
Create a new integration test file `src-tauri/tests/migration_v8_chain_smoke.rs` that:

1. Spins up a fresh in-memory SQLite pool with all migrations applied through v8 (use the existing test helper if there is one; otherwise replicate the pattern from existing tests under `src-tauri/tests/` or `src-tauri/src/db/`).
2. INSERTs a seed row into `substrate_nodes` with `node_type='constraint'`, text='soft delete with grace', applies_when='account purge', valid_at=now, invalid_at=NULL, prev_version_uuid=NULL.
3. Verifies the seed row appears in `substrate_nodes_fts` MATCH 'soft' AND in `current_substrate_view`.
4. Within a single transaction (`pool.begin()` → `tx.commit()`):
   - INSERT a new row with `prev_version_uuid = old_uuid`, text='hard delete only after 30-day grace'
   - UPDATE old row: SET invalid_at=now, invalidated_reason='refined: narrowing scope'
   - INSERT a `substrate_edits` row kind='refine' with the right before_text/after_text/reason/actor
5. After commit, asserts:
   - `substrate_nodes_fts` MATCH 'soft delete with grace' returns ZERO rows (Pitfall 1 fix proven — old text no longer indexed because trigger removed it AND did not re-insert because invalid_at IS NOT NULL)
   - `substrate_nodes_fts` MATCH 'hard delete' returns the new row
   - `current_substrate_view` returns ONE row (the new head); old row absent
   - `substrate_edits` count is 1; the row has correct kind='refine', actor, before_text='soft delete with grace', after_text='hard delete only after 30-day grace'
6. Audits Phase 12 query parity: runs the exact `WHERE invalid_at IS NULL` predicate from `supersession/queries.rs::fetch_current_substrate_nodes` (or the closest equivalent — read the file to copy the predicate verbatim) and confirms it returns the new row, NOT the old row. This is the regression guard that TRUST-02 SC6 (Phase 12 compatibility) demands.

Use `sqlx::query` + `try_get` for the assertions. Use `chrono::Utc::now().to_rfc3339()` for timestamps (Graphiti issue #893 guard — RESEARCH Don't Hand-Roll table).

WHY each assertion: assertion (5a) closes Pitfall 1 (FTS tombstone leakage); (5b) proves the new INSERT trigger fired correctly on the chain head; (5c) proves current_substrate_view works; (5d) proves the audit row write is atomic with the substrate writes (TRUST-04); (6) proves Phase 12 supersession queries continue to read the chain head correctly.
  </action>
  <verify>
`cargo test -p contract-ide --test migration_v8_chain_smoke` exits 0. Verify by running once with the FTS trigger fix INTENTIONALLY broken (set invalid_at to NULL in the trigger guard) — assertion (5a) MUST fail. Restore the trigger fix and the test MUST pass. This proves the test actually exercises the trigger-replacement, not a tautology.
  </verify>
  <done>
Smoke test passes against the v8 migration; intentionally breaking the trigger guard fails the test (proving the test is load-bearing); Phase 12 query parity assertion passes; the substrate_edits row is committed atomically alongside the chain writes.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p contract-ide` exits 0
- `cargo test -p contract-ide` exits 0 (full suite still green)
- `cargo test -p contract-ide --test migration_v8_chain_smoke` exits 0
- Launching the app once + reopening triggers the migration; PRAGMA user_version reports 8
- `.schema substrate_edits`, `.schema current_substrate_view`, and `.schema substrate_nodes_au` all reflect the v8 definitions
- `PRAGMA table_info(receipts);` shows the new substrate_rules_json column
- Manual trigger of a Delegate-to-agent run persists the substrate hit UUIDs JSON onto the new column
- `demo/seeds/substrate.sqlite.seed.sql` continues to apply cleanly (run `bash contract-ide/demo/reset-demo.sh` end-to-end and confirm the app boots and the seed loads)
</verification>

<success_criteria>
- Migration v8 lands and is idempotent (re-running app does not error or re-apply)
- substrate_edits table + current_substrate_view + replacement substrate_nodes_au trigger + receipts.substrate_rules_json column all present after first launch
- FTS tombstone leakage is provably fixed (smoke test asserts MATCH on the old text returns zero rows post-tombstone)
- composer.rs persists substrate hit UUIDs onto every Delegate receipt write
- All Phase 12 queries continue to compile and pass under v8 — `cargo test -p contract-ide` is green
- Demo reset script continues to apply cleanly post-migration
</success_criteria>

<output>
After completion, create `.planning/phases/15-substrate-trust-surface-search-refine-and-delete-substrate-rules-with-chained-version-audit-and-impact-preview/15-01-SUMMARY.md` documenting:
- Final v8 SQL block (verbatim, for downstream-plan reference)
- Composer.rs change locus (file + line range)
- Integration smoke test path + the assertions that proved the FTS tombstone fix
- Any deviations or downstream notes for plans 15-02..15-06
</output>
