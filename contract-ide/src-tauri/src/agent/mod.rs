/// Agent module — prompt assembler + agent loop primitives.
///
/// This module provides the backend building blocks that both the Phase 8
/// chat panel (08-04b) and Phase 11's Delegate button call without
/// re-implementation.
pub mod prompt_assembler;
